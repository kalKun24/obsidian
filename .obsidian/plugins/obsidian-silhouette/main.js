"use strict";
/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target, mod));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// node_modules/owlelia/dist/vo.js
var require_vo = __commonJS({
  "node_modules/owlelia/dist/vo.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PrimitiveValueObject = exports.ValueObject = exports.AbstractValueObject = void 0;
    var deepEqual = (a, b) => JSON.stringify(a) === JSON.stringify(b);
    var AbstractValueObject = class {
      constructor(_value) {
        this._value = Object.freeze(_value);
      }
      equals(vo) {
        return vo == null ? false : deepEqual(this._value, vo._value);
      }
    };
    exports.AbstractValueObject = AbstractValueObject;
    var ValueObject3 = class extends AbstractValueObject {
    };
    exports.ValueObject = ValueObject3;
    var PrimitiveValueObject = class extends AbstractValueObject {
      unwrap() {
        return this._value;
      }
    };
    exports.PrimitiveValueObject = PrimitiveValueObject;
  }
});

// node_modules/owlelia/dist/error.js
var require_error = __commonJS({
  "node_modules/owlelia/dist/error.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BaseError = void 0;
    var BaseError4 = class extends Error {
      constructor(message) {
        super(message);
        this.name = new.target.name;
      }
      to(clazz) {
        return new clazz(this.message);
      }
    };
    exports.BaseError = BaseError4;
  }
});

// node_modules/owlelia/dist/entity.js
var require_entity = __commonJS({
  "node_modules/owlelia/dist/entity.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Entity = void 0;
    var Entity2 = class {
      constructor(id, props) {
        this._id = id;
        this._props = props;
      }
      equals(entity) {
        return entity == null ? false : this._id === entity._id;
      }
    };
    exports.Entity = Entity2;
  }
});

// node_modules/dayjs/dayjs.min.js
var require_dayjs_min = __commonJS({
  "node_modules/dayjs/dayjs.min.js"(exports, module2) {
    !function(t, e) {
      typeof exports == "object" && typeof module2 != "undefined" ? module2.exports = e() : typeof define == "function" && define.amd ? define(e) : (t = typeof globalThis != "undefined" ? globalThis : t || self).dayjs = e();
    }(exports, function() {
      "use strict";
      var t = 1e3, e = 6e4, n = 36e5, r = "millisecond", i = "second", s = "minute", u = "hour", a = "day", o = "week", f = "month", h = "quarter", c = "year", d = "date", l = "Invalid Date", $ = /^(\d{4})[-/]?(\d{1,2})?[-/]?(\d{0,2})[Tt\s]*(\d{1,2})?:?(\d{1,2})?:?(\d{1,2})?[.:]?(\d+)?$/, y = /\[([^\]]+)]|Y{1,4}|M{1,4}|D{1,2}|d{1,4}|H{1,2}|h{1,2}|a|A|m{1,2}|s{1,2}|Z{1,2}|SSS/g, M = { name: "en", weekdays: "Sunday_Monday_Tuesday_Wednesday_Thursday_Friday_Saturday".split("_"), months: "January_February_March_April_May_June_July_August_September_October_November_December".split("_"), ordinal: function(t2) {
        var e2 = ["th", "st", "nd", "rd"], n2 = t2 % 100;
        return "[" + t2 + (e2[(n2 - 20) % 10] || e2[n2] || e2[0]) + "]";
      } }, m = function(t2, e2, n2) {
        var r2 = String(t2);
        return !r2 || r2.length >= e2 ? t2 : "" + Array(e2 + 1 - r2.length).join(n2) + t2;
      }, v = { s: m, z: function(t2) {
        var e2 = -t2.utcOffset(), n2 = Math.abs(e2), r2 = Math.floor(n2 / 60), i2 = n2 % 60;
        return (e2 <= 0 ? "+" : "-") + m(r2, 2, "0") + ":" + m(i2, 2, "0");
      }, m: function t2(e2, n2) {
        if (e2.date() < n2.date())
          return -t2(n2, e2);
        var r2 = 12 * (n2.year() - e2.year()) + (n2.month() - e2.month()), i2 = e2.clone().add(r2, f), s2 = n2 - i2 < 0, u2 = e2.clone().add(r2 + (s2 ? -1 : 1), f);
        return +(-(r2 + (n2 - i2) / (s2 ? i2 - u2 : u2 - i2)) || 0);
      }, a: function(t2) {
        return t2 < 0 ? Math.ceil(t2) || 0 : Math.floor(t2);
      }, p: function(t2) {
        return { M: f, y: c, w: o, d: a, D: d, h: u, m: s, s: i, ms: r, Q: h }[t2] || String(t2 || "").toLowerCase().replace(/s$/, "");
      }, u: function(t2) {
        return t2 === void 0;
      } }, g = "en", D = {};
      D[g] = M;
      var p = function(t2) {
        return t2 instanceof _;
      }, S = function t2(e2, n2, r2) {
        var i2;
        if (!e2)
          return g;
        if (typeof e2 == "string") {
          var s2 = e2.toLowerCase();
          D[s2] && (i2 = s2), n2 && (D[s2] = n2, i2 = s2);
          var u2 = e2.split("-");
          if (!i2 && u2.length > 1)
            return t2(u2[0]);
        } else {
          var a2 = e2.name;
          D[a2] = e2, i2 = a2;
        }
        return !r2 && i2 && (g = i2), i2 || !r2 && g;
      }, w = function(t2, e2) {
        if (p(t2))
          return t2.clone();
        var n2 = typeof e2 == "object" ? e2 : {};
        return n2.date = t2, n2.args = arguments, new _(n2);
      }, O = v;
      O.l = S, O.i = p, O.w = function(t2, e2) {
        return w(t2, { locale: e2.$L, utc: e2.$u, x: e2.$x, $offset: e2.$offset });
      };
      var _ = function() {
        function M2(t2) {
          this.$L = S(t2.locale, null, true), this.parse(t2);
        }
        var m2 = M2.prototype;
        return m2.parse = function(t2) {
          this.$d = function(t3) {
            var e2 = t3.date, n2 = t3.utc;
            if (e2 === null)
              return new Date(NaN);
            if (O.u(e2))
              return new Date();
            if (e2 instanceof Date)
              return new Date(e2);
            if (typeof e2 == "string" && !/Z$/i.test(e2)) {
              var r2 = e2.match($);
              if (r2) {
                var i2 = r2[2] - 1 || 0, s2 = (r2[7] || "0").substring(0, 3);
                return n2 ? new Date(Date.UTC(r2[1], i2, r2[3] || 1, r2[4] || 0, r2[5] || 0, r2[6] || 0, s2)) : new Date(r2[1], i2, r2[3] || 1, r2[4] || 0, r2[5] || 0, r2[6] || 0, s2);
              }
            }
            return new Date(e2);
          }(t2), this.$x = t2.x || {}, this.init();
        }, m2.init = function() {
          var t2 = this.$d;
          this.$y = t2.getFullYear(), this.$M = t2.getMonth(), this.$D = t2.getDate(), this.$W = t2.getDay(), this.$H = t2.getHours(), this.$m = t2.getMinutes(), this.$s = t2.getSeconds(), this.$ms = t2.getMilliseconds();
        }, m2.$utils = function() {
          return O;
        }, m2.isValid = function() {
          return !(this.$d.toString() === l);
        }, m2.isSame = function(t2, e2) {
          var n2 = w(t2);
          return this.startOf(e2) <= n2 && n2 <= this.endOf(e2);
        }, m2.isAfter = function(t2, e2) {
          return w(t2) < this.startOf(e2);
        }, m2.isBefore = function(t2, e2) {
          return this.endOf(e2) < w(t2);
        }, m2.$g = function(t2, e2, n2) {
          return O.u(t2) ? this[e2] : this.set(n2, t2);
        }, m2.unix = function() {
          return Math.floor(this.valueOf() / 1e3);
        }, m2.valueOf = function() {
          return this.$d.getTime();
        }, m2.startOf = function(t2, e2) {
          var n2 = this, r2 = !!O.u(e2) || e2, h2 = O.p(t2), l2 = function(t3, e3) {
            var i2 = O.w(n2.$u ? Date.UTC(n2.$y, e3, t3) : new Date(n2.$y, e3, t3), n2);
            return r2 ? i2 : i2.endOf(a);
          }, $2 = function(t3, e3) {
            return O.w(n2.toDate()[t3].apply(n2.toDate("s"), (r2 ? [0, 0, 0, 0] : [23, 59, 59, 999]).slice(e3)), n2);
          }, y2 = this.$W, M3 = this.$M, m3 = this.$D, v2 = "set" + (this.$u ? "UTC" : "");
          switch (h2) {
            case c:
              return r2 ? l2(1, 0) : l2(31, 11);
            case f:
              return r2 ? l2(1, M3) : l2(0, M3 + 1);
            case o:
              var g2 = this.$locale().weekStart || 0, D2 = (y2 < g2 ? y2 + 7 : y2) - g2;
              return l2(r2 ? m3 - D2 : m3 + (6 - D2), M3);
            case a:
            case d:
              return $2(v2 + "Hours", 0);
            case u:
              return $2(v2 + "Minutes", 1);
            case s:
              return $2(v2 + "Seconds", 2);
            case i:
              return $2(v2 + "Milliseconds", 3);
            default:
              return this.clone();
          }
        }, m2.endOf = function(t2) {
          return this.startOf(t2, false);
        }, m2.$set = function(t2, e2) {
          var n2, o2 = O.p(t2), h2 = "set" + (this.$u ? "UTC" : ""), l2 = (n2 = {}, n2[a] = h2 + "Date", n2[d] = h2 + "Date", n2[f] = h2 + "Month", n2[c] = h2 + "FullYear", n2[u] = h2 + "Hours", n2[s] = h2 + "Minutes", n2[i] = h2 + "Seconds", n2[r] = h2 + "Milliseconds", n2)[o2], $2 = o2 === a ? this.$D + (e2 - this.$W) : e2;
          if (o2 === f || o2 === c) {
            var y2 = this.clone().set(d, 1);
            y2.$d[l2]($2), y2.init(), this.$d = y2.set(d, Math.min(this.$D, y2.daysInMonth())).$d;
          } else
            l2 && this.$d[l2]($2);
          return this.init(), this;
        }, m2.set = function(t2, e2) {
          return this.clone().$set(t2, e2);
        }, m2.get = function(t2) {
          return this[O.p(t2)]();
        }, m2.add = function(r2, h2) {
          var d2, l2 = this;
          r2 = Number(r2);
          var $2 = O.p(h2), y2 = function(t2) {
            var e2 = w(l2);
            return O.w(e2.date(e2.date() + Math.round(t2 * r2)), l2);
          };
          if ($2 === f)
            return this.set(f, this.$M + r2);
          if ($2 === c)
            return this.set(c, this.$y + r2);
          if ($2 === a)
            return y2(1);
          if ($2 === o)
            return y2(7);
          var M3 = (d2 = {}, d2[s] = e, d2[u] = n, d2[i] = t, d2)[$2] || 1, m3 = this.$d.getTime() + r2 * M3;
          return O.w(m3, this);
        }, m2.subtract = function(t2, e2) {
          return this.add(-1 * t2, e2);
        }, m2.format = function(t2) {
          var e2 = this, n2 = this.$locale();
          if (!this.isValid())
            return n2.invalidDate || l;
          var r2 = t2 || "YYYY-MM-DDTHH:mm:ssZ", i2 = O.z(this), s2 = this.$H, u2 = this.$m, a2 = this.$M, o2 = n2.weekdays, f2 = n2.months, h2 = function(t3, n3, i3, s3) {
            return t3 && (t3[n3] || t3(e2, r2)) || i3[n3].slice(0, s3);
          }, c2 = function(t3) {
            return O.s(s2 % 12 || 12, t3, "0");
          }, d2 = n2.meridiem || function(t3, e3, n3) {
            var r3 = t3 < 12 ? "AM" : "PM";
            return n3 ? r3.toLowerCase() : r3;
          }, $2 = { YY: String(this.$y).slice(-2), YYYY: this.$y, M: a2 + 1, MM: O.s(a2 + 1, 2, "0"), MMM: h2(n2.monthsShort, a2, f2, 3), MMMM: h2(f2, a2), D: this.$D, DD: O.s(this.$D, 2, "0"), d: String(this.$W), dd: h2(n2.weekdaysMin, this.$W, o2, 2), ddd: h2(n2.weekdaysShort, this.$W, o2, 3), dddd: o2[this.$W], H: String(s2), HH: O.s(s2, 2, "0"), h: c2(1), hh: c2(2), a: d2(s2, u2, true), A: d2(s2, u2, false), m: String(u2), mm: O.s(u2, 2, "0"), s: String(this.$s), ss: O.s(this.$s, 2, "0"), SSS: O.s(this.$ms, 3, "0"), Z: i2 };
          return r2.replace(y, function(t3, e3) {
            return e3 || $2[t3] || i2.replace(":", "");
          });
        }, m2.utcOffset = function() {
          return 15 * -Math.round(this.$d.getTimezoneOffset() / 15);
        }, m2.diff = function(r2, d2, l2) {
          var $2, y2 = O.p(d2), M3 = w(r2), m3 = (M3.utcOffset() - this.utcOffset()) * e, v2 = this - M3, g2 = O.m(this, M3);
          return g2 = ($2 = {}, $2[c] = g2 / 12, $2[f] = g2, $2[h] = g2 / 3, $2[o] = (v2 - m3) / 6048e5, $2[a] = (v2 - m3) / 864e5, $2[u] = v2 / n, $2[s] = v2 / e, $2[i] = v2 / t, $2)[y2] || v2, l2 ? g2 : O.a(g2);
        }, m2.daysInMonth = function() {
          return this.endOf(f).$D;
        }, m2.$locale = function() {
          return D[this.$L];
        }, m2.locale = function(t2, e2) {
          if (!t2)
            return this.$L;
          var n2 = this.clone(), r2 = S(t2, e2, true);
          return r2 && (n2.$L = r2), n2;
        }, m2.clone = function() {
          return O.w(this.$d, this);
        }, m2.toDate = function() {
          return new Date(this.valueOf());
        }, m2.toJSON = function() {
          return this.isValid() ? this.toISOString() : null;
        }, m2.toISOString = function() {
          return this.$d.toISOString();
        }, m2.toString = function() {
          return this.$d.toUTCString();
        }, M2;
      }(), T = _.prototype;
      return w.prototype = T, [["$ms", r], ["$s", i], ["$m", s], ["$H", u], ["$W", a], ["$M", f], ["$y", c], ["$D", d]].forEach(function(t2) {
        T[t2[1]] = function(e2) {
          return this.$g(e2, t2[0], t2[1]);
        };
      }), w.extend = function(t2, e2) {
        return t2.$i || (t2(e2, _, w), t2.$i = true), w;
      }, w.locale = S, w.isDayjs = p, w.unix = function(t2) {
        return w(1e3 * t2);
      }, w.en = D[g], w.Ls = D, w.p = {}, w;
    });
  }
});

// node_modules/dayjs/locale/ja.js
var require_ja = __commonJS({
  "node_modules/dayjs/locale/ja.js"(exports, module2) {
    !function(e, _) {
      typeof exports == "object" && typeof module2 != "undefined" ? module2.exports = _(require_dayjs_min()) : typeof define == "function" && define.amd ? define(["dayjs"], _) : (e = typeof globalThis != "undefined" ? globalThis : e || self).dayjs_locale_ja = _(e.dayjs);
    }(exports, function(e) {
      "use strict";
      function _(e2) {
        return e2 && typeof e2 == "object" && "default" in e2 ? e2 : { default: e2 };
      }
      var t = _(e), d = { name: "ja", weekdays: "\u65E5\u66DC\u65E5_\u6708\u66DC\u65E5_\u706B\u66DC\u65E5_\u6C34\u66DC\u65E5_\u6728\u66DC\u65E5_\u91D1\u66DC\u65E5_\u571F\u66DC\u65E5".split("_"), weekdaysShort: "\u65E5_\u6708_\u706B_\u6C34_\u6728_\u91D1_\u571F".split("_"), weekdaysMin: "\u65E5_\u6708_\u706B_\u6C34_\u6728_\u91D1_\u571F".split("_"), months: "1\u6708_2\u6708_3\u6708_4\u6708_5\u6708_6\u6708_7\u6708_8\u6708_9\u6708_10\u6708_11\u6708_12\u6708".split("_"), monthsShort: "1\u6708_2\u6708_3\u6708_4\u6708_5\u6708_6\u6708_7\u6708_8\u6708_9\u6708_10\u6708_11\u6708_12\u6708".split("_"), ordinal: function(e2) {
        return e2 + "\u65E5";
      }, formats: { LT: "HH:mm", LTS: "HH:mm:ss", L: "YYYY/MM/DD", LL: "YYYY\u5E74M\u6708D\u65E5", LLL: "YYYY\u5E74M\u6708D\u65E5 HH:mm", LLLL: "YYYY\u5E74M\u6708D\u65E5 dddd HH:mm", l: "YYYY/MM/DD", ll: "YYYY\u5E74M\u6708D\u65E5", lll: "YYYY\u5E74M\u6708D\u65E5 HH:mm", llll: "YYYY\u5E74M\u6708D\u65E5(ddd) HH:mm" }, meridiem: function(e2) {
        return e2 < 12 ? "\u5348\u524D" : "\u5348\u5F8C";
      }, relativeTime: { future: "%s\u5F8C", past: "%s\u524D", s: "\u6570\u79D2", m: "1\u5206", mm: "%d\u5206", h: "1\u6642\u9593", hh: "%d\u6642\u9593", d: "1\u65E5", dd: "%d\u65E5", M: "1\u30F6\u6708", MM: "%d\u30F6\u6708", y: "1\u5E74", yy: "%d\u5E74" } };
      return t.default.locale(d, null, true), d;
    });
  }
});

// node_modules/dayjs/plugin/customParseFormat.js
var require_customParseFormat = __commonJS({
  "node_modules/dayjs/plugin/customParseFormat.js"(exports, module2) {
    !function(e, t) {
      typeof exports == "object" && typeof module2 != "undefined" ? module2.exports = t() : typeof define == "function" && define.amd ? define(t) : (e = typeof globalThis != "undefined" ? globalThis : e || self).dayjs_plugin_customParseFormat = t();
    }(exports, function() {
      "use strict";
      var e = { LTS: "h:mm:ss A", LT: "h:mm A", L: "MM/DD/YYYY", LL: "MMMM D, YYYY", LLL: "MMMM D, YYYY h:mm A", LLLL: "dddd, MMMM D, YYYY h:mm A" }, t = /(\[[^[]*\])|([-_:/.,()\s]+)|(A|a|YYYY|YY?|MM?M?M?|Do|DD?|hh?|HH?|mm?|ss?|S{1,3}|z|ZZ?)/g, n = /\d\d/, r = /\d\d?/, i = /\d*[^-_:/,()\s\d]+/, o = {}, s = function(e2) {
        return (e2 = +e2) + (e2 > 68 ? 1900 : 2e3);
      };
      var a = function(e2) {
        return function(t2) {
          this[e2] = +t2;
        };
      }, f = [/[+-]\d\d:?(\d\d)?|Z/, function(e2) {
        (this.zone || (this.zone = {})).offset = function(e3) {
          if (!e3)
            return 0;
          if (e3 === "Z")
            return 0;
          var t2 = e3.match(/([+-]|\d\d)/g), n2 = 60 * t2[1] + (+t2[2] || 0);
          return n2 === 0 ? 0 : t2[0] === "+" ? -n2 : n2;
        }(e2);
      }], h = function(e2) {
        var t2 = o[e2];
        return t2 && (t2.indexOf ? t2 : t2.s.concat(t2.f));
      }, u = function(e2, t2) {
        var n2, r2 = o.meridiem;
        if (r2) {
          for (var i2 = 1; i2 <= 24; i2 += 1)
            if (e2.indexOf(r2(i2, 0, t2)) > -1) {
              n2 = i2 > 12;
              break;
            }
        } else
          n2 = e2 === (t2 ? "pm" : "PM");
        return n2;
      }, d = { A: [i, function(e2) {
        this.afternoon = u(e2, false);
      }], a: [i, function(e2) {
        this.afternoon = u(e2, true);
      }], S: [/\d/, function(e2) {
        this.milliseconds = 100 * +e2;
      }], SS: [n, function(e2) {
        this.milliseconds = 10 * +e2;
      }], SSS: [/\d{3}/, function(e2) {
        this.milliseconds = +e2;
      }], s: [r, a("seconds")], ss: [r, a("seconds")], m: [r, a("minutes")], mm: [r, a("minutes")], H: [r, a("hours")], h: [r, a("hours")], HH: [r, a("hours")], hh: [r, a("hours")], D: [r, a("day")], DD: [n, a("day")], Do: [i, function(e2) {
        var t2 = o.ordinal, n2 = e2.match(/\d+/);
        if (this.day = n2[0], t2)
          for (var r2 = 1; r2 <= 31; r2 += 1)
            t2(r2).replace(/\[|\]/g, "") === e2 && (this.day = r2);
      }], M: [r, a("month")], MM: [n, a("month")], MMM: [i, function(e2) {
        var t2 = h("months"), n2 = (h("monthsShort") || t2.map(function(e3) {
          return e3.slice(0, 3);
        })).indexOf(e2) + 1;
        if (n2 < 1)
          throw new Error();
        this.month = n2 % 12 || n2;
      }], MMMM: [i, function(e2) {
        var t2 = h("months").indexOf(e2) + 1;
        if (t2 < 1)
          throw new Error();
        this.month = t2 % 12 || t2;
      }], Y: [/[+-]?\d+/, a("year")], YY: [n, function(e2) {
        this.year = s(e2);
      }], YYYY: [/\d{4}/, a("year")], Z: f, ZZ: f };
      function c(n2) {
        var r2, i2;
        r2 = n2, i2 = o && o.formats;
        for (var s2 = (n2 = r2.replace(/(\[[^\]]+])|(LTS?|l{1,4}|L{1,4})/g, function(t2, n3, r3) {
          var o2 = r3 && r3.toUpperCase();
          return n3 || i2[r3] || e[r3] || i2[o2].replace(/(\[[^\]]+])|(MMMM|MM|DD|dddd)/g, function(e2, t3, n4) {
            return t3 || n4.slice(1);
          });
        })).match(t), a2 = s2.length, f2 = 0; f2 < a2; f2 += 1) {
          var h2 = s2[f2], u2 = d[h2], c2 = u2 && u2[0], l = u2 && u2[1];
          s2[f2] = l ? { regex: c2, parser: l } : h2.replace(/^\[|\]$/g, "");
        }
        return function(e2) {
          for (var t2 = {}, n3 = 0, r3 = 0; n3 < a2; n3 += 1) {
            var i3 = s2[n3];
            if (typeof i3 == "string")
              r3 += i3.length;
            else {
              var o2 = i3.regex, f3 = i3.parser, h3 = e2.slice(r3), u3 = o2.exec(h3)[0];
              f3.call(t2, u3), e2 = e2.replace(u3, "");
            }
          }
          return function(e3) {
            var t3 = e3.afternoon;
            if (t3 !== void 0) {
              var n4 = e3.hours;
              t3 ? n4 < 12 && (e3.hours += 12) : n4 === 12 && (e3.hours = 0), delete e3.afternoon;
            }
          }(t2), t2;
        };
      }
      return function(e2, t2, n2) {
        n2.p.customParseFormat = true, e2 && e2.parseTwoDigitYear && (s = e2.parseTwoDigitYear);
        var r2 = t2.prototype, i2 = r2.parse;
        r2.parse = function(e3) {
          var t3 = e3.date, r3 = e3.utc, s2 = e3.args;
          this.$u = r3;
          var a2 = s2[1];
          if (typeof a2 == "string") {
            var f2 = s2[2] === true, h2 = s2[3] === true, u2 = f2 || h2, d2 = s2[2];
            h2 && (d2 = s2[2]), o = this.$locale(), !f2 && d2 && (o = n2.Ls[d2]), this.$d = function(e4, t4, n3) {
              try {
                if (["x", "X"].indexOf(t4) > -1)
                  return new Date((t4 === "X" ? 1e3 : 1) * e4);
                var r4 = c(t4)(e4), i3 = r4.year, o2 = r4.month, s3 = r4.day, a3 = r4.hours, f3 = r4.minutes, h3 = r4.seconds, u3 = r4.milliseconds, d3 = r4.zone, l2 = new Date(), m2 = s3 || (i3 || o2 ? 1 : l2.getDate()), M2 = i3 || l2.getFullYear(), Y = 0;
                i3 && !o2 || (Y = o2 > 0 ? o2 - 1 : l2.getMonth());
                var p = a3 || 0, v = f3 || 0, D = h3 || 0, g = u3 || 0;
                return d3 ? new Date(Date.UTC(M2, Y, m2, p, v, D, g + 60 * d3.offset * 1e3)) : n3 ? new Date(Date.UTC(M2, Y, m2, p, v, D, g)) : new Date(M2, Y, m2, p, v, D, g);
              } catch (e5) {
                return new Date("");
              }
            }(t3, a2, r3), this.init(), d2 && d2 !== true && (this.$L = this.locale(d2).$L), u2 && t3 != this.format(a2) && (this.$d = new Date("")), o = {};
          } else if (a2 instanceof Array)
            for (var l = a2.length, m = 1; m <= l; m += 1) {
              s2[1] = a2[m - 1];
              var M = n2.apply(this, s2);
              if (M.isValid()) {
                this.$d = M.$d, this.$L = M.$L, this.init();
                break;
              }
              m === l && (this.$d = new Date(""));
            }
          else
            i2.call(this, e3);
        };
      };
    });
  }
});

// node_modules/owlelia/dist/bundle/datetime.js
var require_datetime = __commonJS({
  "node_modules/owlelia/dist/bundle/datetime.js"(exports) {
    "use strict";
    var __importDefault2 = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DateTime = void 0;
    var vo_1 = require_vo();
    var dayjs_1 = __importDefault2(require_dayjs_min());
    require_ja();
    var customParseFormat_1 = __importDefault2(require_customParseFormat());
    dayjs_1.default.extend(customParseFormat_1.default);
    var pad002 = (v) => String(v).padStart(2, "0");
    function keyBy(xs, toKey) {
      const result = {};
      for (const x of xs) {
        result[toKey(x)] = x;
      }
      return result;
    }
    function toHHmmss2(seconds) {
      const hour = seconds / (60 * 60) | 0;
      const min = seconds % (60 * 60) / 60 | 0;
      const sec = seconds % 60;
      return `${pad002(hour)}:${pad002(min)}:${pad002(sec)}`;
    }
    var DateTime9 = class extends vo_1.ValueObject {
      static setHolidays(...dates) {
        this.holidays = dates.map(DateTime9.of);
        this.holidayByDisplayDate = keyBy(this.holidays, (x) => x.displayDate);
      }
      static of(value) {
        return new DateTime9((0, dayjs_1.default)(typeof value === "number" ? value * 1e3 : value));
      }
      static from(value, format) {
        return new DateTime9((0, dayjs_1.default)(value, format));
      }
      static now() {
        return new DateTime9((0, dayjs_1.default)());
      }
      static today() {
        return new DateTime9((0, dayjs_1.default)().startOf("day"));
      }
      static yesterday() {
        return DateTime9.today().minusDays(1);
      }
      static tomorrow() {
        return DateTime9.today().plusDays(1);
      }
      static isValid(value, format) {
        return (0, dayjs_1.default)(value, format).format(format) === value;
      }
      clone() {
        return new DateTime9(this._value.clone());
      }
      toDate(end) {
        let bd = this.midnight();
        let ed = end.midnight();
        if (bd.equals(ed)) {
          return [bd, ed];
        }
        let dates = [bd];
        const reverse = ed.isBefore(bd);
        while (!bd.equals(ed)) {
          bd = reverse ? bd.minusDays(1) : bd.plusDays(1);
          dates.push(bd);
        }
        return dates;
      }
      replaceYear(year) {
        return new DateTime9(this._value.year(year));
      }
      replaceMonth(month) {
        return new DateTime9(this._value.month(month - 1));
      }
      replaceDay(day) {
        return new DateTime9(this._value.date(day));
      }
      replaceHour(hour) {
        return new DateTime9(this._value.hour(hour));
      }
      replaceMinute(minute) {
        return new DateTime9(this._value.minute(minute));
      }
      replaceSecond(second) {
        return new DateTime9(this._value.second(second));
      }
      midnight() {
        return new DateTime9(this._value.hour(0).minute(0).second(0).millisecond(0));
      }
      plusMonths(months) {
        return new DateTime9(this._value.add(months, "month"));
      }
      plusDays(days2) {
        return new DateTime9(this._value.add(days2, "day"));
      }
      plusWeekdays(weekdays) {
        let d = this.clone();
        while (weekdays > 0) {
          d = d.plusDays(1);
          if (d.isWeekday) {
            weekdays--;
          }
        }
        return d;
      }
      plusHours(hours) {
        return new DateTime9(this._value.add(hours, "hour"));
      }
      plusMinutes(minutes) {
        return new DateTime9(this._value.add(minutes, "minute"));
      }
      plusSeconds(seconds) {
        return new DateTime9(this._value.add(seconds, "second"));
      }
      minusMonth(months) {
        return new DateTime9(this._value.subtract(months, "month"));
      }
      minusDays(days2) {
        return new DateTime9(this._value.subtract(days2, "day"));
      }
      minusHours(hours) {
        return new DateTime9(this._value.subtract(hours, "hour"));
      }
      minusMinutes(minutes) {
        return new DateTime9(this._value.subtract(minutes, "minute"));
      }
      minusSeconds(seconds) {
        return new DateTime9(this._value.subtract(seconds, "second"));
      }
      endOfMonth() {
        return new DateTime9(this._value.endOf("month"));
      }
      overwriteDate(date) {
        return new DateTime9(this._value.year(date._value.year()).month(date._value.month()).date(date._value.date()));
      }
      diffMonths(date) {
        return this._value.startOf("month").diff(date._value.startOf("month"), "month");
      }
      diffDays(date) {
        return this._value.startOf("day").diff(date._value.startOf("day"), "day");
      }
      diffHours(date) {
        return this._value.startOf("hour").diff(date._value.startOf("hour"), "hour");
      }
      diffMinutes(date) {
        return this._value.startOf("minute").diff(date._value.startOf("minute"), "minute");
      }
      diffSeconds(date) {
        return this._value.startOf("second").diff(date._value.startOf("second"), "second");
      }
      diffMinutesFromNow() {
        return DateTime9.now()._value.diff(this._value, "minute");
      }
      diffSecondsFromNow() {
        return DateTime9.now()._value.diff(this._value, "second");
      }
      displayDiffFromNow() {
        return toHHmmss2(DateTime9.now()._value.diff(this._value, "second"));
      }
      displayDiffFromNowJapanese() {
        const hours = this._value.hour();
        const minutes = this._value.minute();
        const seconds = this._value.second();
        return [
          hours && `${hours}\u6642\u9593`,
          minutes && `${minutes}\u5206`,
          hours === 0 && minutes === 0 && `${seconds}\u79D2`
        ].filter((x) => x).join("");
      }
      within(seconds) {
        return DateTime9.now()._value.diff(this._value, "second") <= seconds;
      }
      equals(date, ignoreTime = false) {
        return ignoreTime ? this._value.isSame(date._value, "date") : this._value.isSame(date._value);
      }
      isAfter(date, ignoreTime = false) {
        return ignoreTime ? this._value.isAfter(date._value, "date") : this._value.isAfter(date._value);
      }
      isBefore(date, ignoreTime = false) {
        return ignoreTime ? this._value.isBefore(date._value, "date") : this._value.isBefore(date._value);
      }
      isAfterOrEquals(date, ignoreTime = false) {
        return this.isAfter(date, ignoreTime) || this.equals(date, ignoreTime);
      }
      isBeforeOrEquals(date, ignoreTime = false) {
        return this.isBefore(date, ignoreTime) || this.equals(date, ignoreTime);
      }
      between(begin, end, option = { includeBegin: true, includeEnd: true, ignoreTime: false }) {
        return (option.includeBegin ? this.isAfterOrEquals(begin, option.ignoreTime) : this.isAfter(begin, option.ignoreTime)) && (option.includeEnd ? this.isBeforeOrEquals(end, option.ignoreTime) : this.isBefore(end, option.ignoreTime));
      }
      format(template) {
        return this._value.format(template);
      }
      get year() {
        return this._value.year();
      }
      get month() {
        return this._value.month() + 1;
      }
      get day() {
        return this._value.date();
      }
      get yearMonth() {
        return this.year * 100 + this.month;
      }
      get hour() {
        return this._value.hour();
      }
      get minute() {
        return this._value.minute();
      }
      get second() {
        return this._value.second();
      }
      get isStartOfDay() {
        return this._value.startOf("day").isSame(this._value);
      }
      get isMonday() {
        return this._value.day() === 1;
      }
      get isTuesday() {
        return this._value.day() === 2;
      }
      get isWednesday() {
        return this._value.day() === 3;
      }
      get isThursday() {
        return this._value.day() === 4;
      }
      get isFriday() {
        return this._value.day() === 5;
      }
      get isSaturday() {
        return this._value.day() === 6;
      }
      get isSunday() {
        return this._value.day() === 0;
      }
      get isHoliday() {
        return !!DateTime9.holidayByDisplayDate[this.displayDate];
      }
      get isWeekday() {
        return !(this.isSaturday || this.isSunday || this.isHoliday);
      }
      get nthDayOfWeek() {
        return Math.ceil(this.day / 7);
      }
      get date() {
        return this._value.toDate();
      }
      get unix() {
        return this._value.unix();
      }
      get rfc3339() {
        return this._value.format("YYYY-MM-DDTHH:mm:ssZ");
      }
      get rfc3339WithoutTimezone() {
        return this._value.format("YYYY-MM-DDTHH:mm:ss");
      }
      get displayTime() {
        return this._value.format("HH:mm:ss");
      }
      get displayTimeWithoutSeconds() {
        return this._value.format("HH:mm");
      }
      get displayDate() {
        return this._value.format("YYYY-MM-DD");
      }
      get displayDateFull() {
        return this._value.format("YYYY-MM-DD (ddd)");
      }
      get displayDateTime() {
        return this._value.format("YYYY-MM-DD HH:mm:ss");
      }
      get displayDateTimeWithoutSeconds() {
        return this._value.format("YYYY-MM-DD HH:mm");
      }
      get yyyyMMdd() {
        return this._value.format("YYYYMMDD");
      }
      get yyyyMMddHHmmss() {
        return this._value.format("YYYYMMDDHHmmss");
      }
    };
    exports.DateTime = DateTime9;
  }
});

// node_modules/owlelia/dist/bundle/utils.js
var require_utils = __commonJS({
  "node_modules/owlelia/dist/bundle/utils.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isEmpty = exports.round = void 0;
    function round(n, decimalPlace) {
      const x = Math.pow(10, decimalPlace);
      return Math.round(n * x) / x;
    }
    exports.round = round;
    function isEmpty(arg) {
      return [Object, Array].includes((arg || {}).constructor) && !Object.entries(arg || {}).length;
    }
    exports.isEmpty = isEmpty;
  }
});

// node_modules/owlelia/dist/bundle/values.js
var require_values = __commonJS({
  "node_modules/owlelia/dist/bundle/values.js"(exports) {
    "use strict";
    var __awaiter2 = exports && exports.__awaiter || function(thisArg, _arguments, P, generator) {
      function adopt(value) {
        return value instanceof P ? value : new P(function(resolve) {
          resolve(value);
        });
      }
      return new (P || (P = Promise))(function(resolve, reject) {
        function fulfilled(value) {
          try {
            step(generator.next(value));
          } catch (e) {
            reject(e);
          }
        }
        function rejected(value) {
          try {
            step(generator["throw"](value));
          } catch (e) {
            reject(e);
          }
        }
        function step(result) {
          result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
        }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
      });
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.LiquidValue = void 0;
    var utils_1 = require_utils();
    var LiquidValue = class {
      constructor(value, loading = false, error = null) {
        this.value = value;
        this.loading = loading;
        this.error = error;
        this.initialValue = value;
      }
      isEmpty() {
        return (0, utils_1.isEmpty)(this.value);
      }
      load(asyncFunc, option) {
        return __awaiter2(this, void 0, void 0, function* () {
          if (option === null || option === void 0 ? void 0 : option.clearValueBeforeLoading) {
            this.value = this.initialValue;
          }
          if (option === null || option === void 0 ? void 0 : option.clearErrorBeforeLoading) {
            this.error = null;
          }
          if (!(option === null || option === void 0 ? void 0 : option.silent)) {
            this.loading = true;
          }
          const ret = yield asyncFunc();
          if (ret.isErr()) {
            if (!(option === null || option === void 0 ? void 0 : option.keepValueIfError)) {
              this.value = this.initialValue;
            }
            this.error = ret.error;
          } else {
            this.error = null;
            this.value = ret.value;
          }
          if (!(option === null || option === void 0 ? void 0 : option.silent)) {
            this.loading = false;
          }
        });
      }
    };
    exports.LiquidValue = LiquidValue;
  }
});

// node_modules/owlelia/dist/result.js
var require_result = __commonJS({
  "node_modules/owlelia/dist/result.js"(exports) {
    "use strict";
    var __awaiter2 = exports && exports.__awaiter || function(thisArg, _arguments, P, generator) {
      function adopt(value) {
        return value instanceof P ? value : new P(function(resolve) {
          resolve(value);
        });
      }
      return new (P || (P = Promise))(function(resolve, reject) {
        function fulfilled(value) {
          try {
            step(generator.next(value));
          } catch (e) {
            reject(e);
          }
        }
        function rejected(value) {
          try {
            step(generator["throw"](value));
          } catch (e) {
            reject(e);
          }
        }
        function step(result) {
          result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
        }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
      });
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.fromPromise = exports.aggregate = exports.ok = exports.err = void 0;
    var Ok = class {
      constructor(value) {
        this.value = value;
        this._type = "ok";
      }
      unwrap() {
        return [this.value, void 0];
      }
      isErr() {
        return false;
      }
      isOk() {
        return true;
      }
      get _err() {
        return void 0;
      }
      get _ok() {
        return this.value;
      }
      mapErr(_functor) {
        return new Ok(this.value);
      }
      map(functor) {
        return new Ok(functor(this.value));
      }
      biMap(functor, errFunctor) {
        return this.mapErr(errFunctor).map(functor);
      }
      fold(functor, _errFunctor) {
        return functor(this.value);
      }
      or(value) {
        return this.value;
      }
      orUndefined() {
        return this.value;
      }
      orNull() {
        return this.value;
      }
      orThrow() {
        return this.value;
      }
    };
    var Err = class {
      constructor(error) {
        this.error = error;
        this._type = "err";
      }
      unwrap() {
        return [void 0, this.error];
      }
      isErr() {
        return true;
      }
      isOk() {
        return false;
      }
      get _err() {
        return this.error;
      }
      get _ok() {
        return void 0;
      }
      mapErr(functor) {
        return new Err(functor(this.error));
      }
      map(functor) {
        return new Err(this.error);
      }
      biMap(functor, errFunctor) {
        return this.mapErr(errFunctor).map(functor);
      }
      fold(functor, errFunctor) {
        return errFunctor(this.error);
      }
      or(value) {
        return value;
      }
      orUndefined() {
        return void 0;
      }
      orNull() {
        return null;
      }
      orThrow() {
        throw this.error;
      }
    };
    var err = (error) => new Err(error);
    exports.err = err;
    var ok = (value) => new Ok(value);
    exports.ok = ok;
    function aggregate(results) {
      const errors = results.filter((x) => x.isErr()).flatMap((x) => x._err);
      const values = results.filter((x) => x.isOk()).map((x) => x._ok);
      return errors.length > 0 ? (0, exports.err)(errors) : (0, exports.ok)(values);
    }
    exports.aggregate = aggregate;
    function fromPromise3(promise) {
      return __awaiter2(this, void 0, void 0, function* () {
        try {
          return (0, exports.ok)(yield promise);
        } catch (e) {
          return (0, exports.err)(e);
        }
      });
    }
    exports.fromPromise = fromPromise3;
  }
});

// node_modules/owlelia/dist/index.js
var require_dist = __commonJS({
  "node_modules/owlelia/dist/index.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      o[k2] = m[k];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m)
        if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p))
          __createBinding2(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.LiquidValue = exports.DateTime = exports.BaseError = exports.Entity = exports.PrimitiveValueObject = exports.ValueObject = void 0;
    var vo_1 = require_vo();
    Object.defineProperty(exports, "ValueObject", { enumerable: true, get: function() {
      return vo_1.ValueObject;
    } });
    Object.defineProperty(exports, "PrimitiveValueObject", { enumerable: true, get: function() {
      return vo_1.PrimitiveValueObject;
    } });
    var error_1 = require_error();
    Object.defineProperty(exports, "BaseError", { enumerable: true, get: function() {
      return error_1.BaseError;
    } });
    var entity_1 = require_entity();
    Object.defineProperty(exports, "Entity", { enumerable: true, get: function() {
      return entity_1.Entity;
    } });
    var datetime_1 = require_datetime();
    Object.defineProperty(exports, "DateTime", { enumerable: true, get: function() {
      return datetime_1.DateTime;
    } });
    var values_1 = require_values();
    Object.defineProperty(exports, "LiquidValue", { enumerable: true, get: function() {
      return values_1.LiquidValue;
    } });
    __exportStar2(require_result(), exports);
    __exportStar2(require_utils(), exports);
  }
});

// node_modules/tslib/tslib.js
var require_tslib = __commonJS({
  "node_modules/tslib/tslib.js"(exports, module2) {
    var __extends2;
    var __assign2;
    var __rest2;
    var __decorate2;
    var __param2;
    var __metadata2;
    var __awaiter2;
    var __generator2;
    var __exportStar2;
    var __values2;
    var __read2;
    var __spread2;
    var __spreadArrays2;
    var __spreadArray2;
    var __await2;
    var __asyncGenerator2;
    var __asyncDelegator2;
    var __asyncValues2;
    var __makeTemplateObject2;
    var __importStar2;
    var __importDefault2;
    var __classPrivateFieldGet2;
    var __classPrivateFieldSet2;
    var __classPrivateFieldIn2;
    var __createBinding2;
    (function(factory) {
      var root = typeof global === "object" ? global : typeof self === "object" ? self : typeof this === "object" ? this : {};
      if (typeof define === "function" && define.amd) {
        define("tslib", ["exports"], function(exports2) {
          factory(createExporter(root, createExporter(exports2)));
        });
      } else if (typeof module2 === "object" && typeof module2.exports === "object") {
        factory(createExporter(root, createExporter(module2.exports)));
      } else {
        factory(createExporter(root));
      }
      function createExporter(exports2, previous) {
        if (exports2 !== root) {
          if (typeof Object.create === "function") {
            Object.defineProperty(exports2, "__esModule", { value: true });
          } else {
            exports2.__esModule = true;
          }
        }
        return function(id, v) {
          return exports2[id] = previous ? previous(id, v) : v;
        };
      }
    })(function(exporter) {
      var extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
        d.__proto__ = b;
      } || function(d, b) {
        for (var p in b)
          if (Object.prototype.hasOwnProperty.call(b, p))
            d[p] = b[p];
      };
      __extends2 = function(d, b) {
        if (typeof b !== "function" && b !== null)
          throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() {
          this.constructor = d;
        }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
      };
      __assign2 = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s)
            if (Object.prototype.hasOwnProperty.call(s, p))
              t[p] = s[p];
        }
        return t;
      };
      __rest2 = function(s, e) {
        var t = {};
        for (var p in s)
          if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
            t[p] = s[p];
        if (s != null && typeof Object.getOwnPropertySymbols === "function")
          for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
              t[p[i]] = s[p[i]];
          }
        return t;
      };
      __decorate2 = function(decorators, target, key, desc) {
        var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
        if (typeof Reflect === "object" && typeof Reflect.decorate === "function")
          r = Reflect.decorate(decorators, target, key, desc);
        else
          for (var i = decorators.length - 1; i >= 0; i--)
            if (d = decorators[i])
              r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
        return c > 3 && r && Object.defineProperty(target, key, r), r;
      };
      __param2 = function(paramIndex, decorator) {
        return function(target, key) {
          decorator(target, key, paramIndex);
        };
      };
      __metadata2 = function(metadataKey, metadataValue) {
        if (typeof Reflect === "object" && typeof Reflect.metadata === "function")
          return Reflect.metadata(metadataKey, metadataValue);
      };
      __awaiter2 = function(thisArg, _arguments, P, generator) {
        function adopt(value) {
          return value instanceof P ? value : new P(function(resolve) {
            resolve(value);
          });
        }
        return new (P || (P = Promise))(function(resolve, reject) {
          function fulfilled(value) {
            try {
              step(generator.next(value));
            } catch (e) {
              reject(e);
            }
          }
          function rejected(value) {
            try {
              step(generator["throw"](value));
            } catch (e) {
              reject(e);
            }
          }
          function step(result) {
            result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
          }
          step((generator = generator.apply(thisArg, _arguments || [])).next());
        });
      };
      __generator2 = function(thisArg, body) {
        var _ = { label: 0, sent: function() {
          if (t[0] & 1)
            throw t[1];
          return t[1];
        }, trys: [], ops: [] }, f, y, t, g;
        return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
          return this;
        }), g;
        function verb(n) {
          return function(v) {
            return step([n, v]);
          };
        }
        function step(op) {
          if (f)
            throw new TypeError("Generator is already executing.");
          while (_)
            try {
              if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done)
                return t;
              if (y = 0, t)
                op = [op[0] & 2, t.value];
              switch (op[0]) {
                case 0:
                case 1:
                  t = op;
                  break;
                case 4:
                  _.label++;
                  return { value: op[1], done: false };
                case 5:
                  _.label++;
                  y = op[1];
                  op = [0];
                  continue;
                case 7:
                  op = _.ops.pop();
                  _.trys.pop();
                  continue;
                default:
                  if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                    _ = 0;
                    continue;
                  }
                  if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                    _.label = op[1];
                    break;
                  }
                  if (op[0] === 6 && _.label < t[1]) {
                    _.label = t[1];
                    t = op;
                    break;
                  }
                  if (t && _.label < t[2]) {
                    _.label = t[2];
                    _.ops.push(op);
                    break;
                  }
                  if (t[2])
                    _.ops.pop();
                  _.trys.pop();
                  continue;
              }
              op = body.call(thisArg, _);
            } catch (e) {
              op = [6, e];
              y = 0;
            } finally {
              f = t = 0;
            }
          if (op[0] & 5)
            throw op[1];
          return { value: op[0] ? op[1] : void 0, done: true };
        }
      };
      __exportStar2 = function(m, o) {
        for (var p in m)
          if (p !== "default" && !Object.prototype.hasOwnProperty.call(o, p))
            __createBinding2(o, m, p);
      };
      __createBinding2 = Object.create ? function(o, m, k, k2) {
        if (k2 === void 0)
          k2 = k;
        var desc = Object.getOwnPropertyDescriptor(m, k);
        if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
          desc = { enumerable: true, get: function() {
            return m[k];
          } };
        }
        Object.defineProperty(o, k2, desc);
      } : function(o, m, k, k2) {
        if (k2 === void 0)
          k2 = k;
        o[k2] = m[k];
      };
      __values2 = function(o) {
        var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
        if (m)
          return m.call(o);
        if (o && typeof o.length === "number")
          return {
            next: function() {
              if (o && i >= o.length)
                o = void 0;
              return { value: o && o[i++], done: !o };
            }
          };
        throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
      };
      __read2 = function(o, n) {
        var m = typeof Symbol === "function" && o[Symbol.iterator];
        if (!m)
          return o;
        var i = m.call(o), r, ar = [], e;
        try {
          while ((n === void 0 || n-- > 0) && !(r = i.next()).done)
            ar.push(r.value);
        } catch (error) {
          e = { error };
        } finally {
          try {
            if (r && !r.done && (m = i["return"]))
              m.call(i);
          } finally {
            if (e)
              throw e.error;
          }
        }
        return ar;
      };
      __spread2 = function() {
        for (var ar = [], i = 0; i < arguments.length; i++)
          ar = ar.concat(__read2(arguments[i]));
        return ar;
      };
      __spreadArrays2 = function() {
        for (var s = 0, i = 0, il = arguments.length; i < il; i++)
          s += arguments[i].length;
        for (var r = Array(s), k = 0, i = 0; i < il; i++)
          for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
        return r;
      };
      __spreadArray2 = function(to, from, pack) {
        if (pack || arguments.length === 2)
          for (var i = 0, l = from.length, ar; i < l; i++) {
            if (ar || !(i in from)) {
              if (!ar)
                ar = Array.prototype.slice.call(from, 0, i);
              ar[i] = from[i];
            }
          }
        return to.concat(ar || Array.prototype.slice.call(from));
      };
      __await2 = function(v) {
        return this instanceof __await2 ? (this.v = v, this) : new __await2(v);
      };
      __asyncGenerator2 = function(thisArg, _arguments, generator) {
        if (!Symbol.asyncIterator)
          throw new TypeError("Symbol.asyncIterator is not defined.");
        var g = generator.apply(thisArg, _arguments || []), i, q = [];
        return i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
          return this;
        }, i;
        function verb(n) {
          if (g[n])
            i[n] = function(v) {
              return new Promise(function(a, b) {
                q.push([n, v, a, b]) > 1 || resume(n, v);
              });
            };
        }
        function resume(n, v) {
          try {
            step(g[n](v));
          } catch (e) {
            settle(q[0][3], e);
          }
        }
        function step(r) {
          r.value instanceof __await2 ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
        }
        function fulfill(value) {
          resume("next", value);
        }
        function reject(value) {
          resume("throw", value);
        }
        function settle(f, v) {
          if (f(v), q.shift(), q.length)
            resume(q[0][0], q[0][1]);
        }
      };
      __asyncDelegator2 = function(o) {
        var i, p;
        return i = {}, verb("next"), verb("throw", function(e) {
          throw e;
        }), verb("return"), i[Symbol.iterator] = function() {
          return this;
        }, i;
        function verb(n, f) {
          i[n] = o[n] ? function(v) {
            return (p = !p) ? { value: __await2(o[n](v)), done: n === "return" } : f ? f(v) : v;
          } : f;
        }
      };
      __asyncValues2 = function(o) {
        if (!Symbol.asyncIterator)
          throw new TypeError("Symbol.asyncIterator is not defined.");
        var m = o[Symbol.asyncIterator], i;
        return m ? m.call(o) : (o = typeof __values2 === "function" ? __values2(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
          return this;
        }, i);
        function verb(n) {
          i[n] = o[n] && function(v) {
            return new Promise(function(resolve, reject) {
              v = o[n](v), settle(resolve, reject, v.done, v.value);
            });
          };
        }
        function settle(resolve, reject, d, v) {
          Promise.resolve(v).then(function(v2) {
            resolve({ value: v2, done: d });
          }, reject);
        }
      };
      __makeTemplateObject2 = function(cooked, raw) {
        if (Object.defineProperty) {
          Object.defineProperty(cooked, "raw", { value: raw });
        } else {
          cooked.raw = raw;
        }
        return cooked;
      };
      var __setModuleDefault = Object.create ? function(o, v) {
        Object.defineProperty(o, "default", { enumerable: true, value: v });
      } : function(o, v) {
        o["default"] = v;
      };
      __importStar2 = function(mod) {
        if (mod && mod.__esModule)
          return mod;
        var result = {};
        if (mod != null) {
          for (var k in mod)
            if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k))
              __createBinding2(result, mod, k);
        }
        __setModuleDefault(result, mod);
        return result;
      };
      __importDefault2 = function(mod) {
        return mod && mod.__esModule ? mod : { "default": mod };
      };
      __classPrivateFieldGet2 = function(receiver, state, kind, f) {
        if (kind === "a" && !f)
          throw new TypeError("Private accessor was defined without a getter");
        if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver))
          throw new TypeError("Cannot read private member from an object whose class did not declare it");
        return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
      };
      __classPrivateFieldSet2 = function(receiver, state, value, kind, f) {
        if (kind === "m")
          throw new TypeError("Private method is not writable");
        if (kind === "a" && !f)
          throw new TypeError("Private accessor was defined without a setter");
        if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver))
          throw new TypeError("Cannot write private member to an object whose class did not declare it");
        return kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value), value;
      };
      __classPrivateFieldIn2 = function(state, receiver) {
        if (receiver === null || typeof receiver !== "object" && typeof receiver !== "function")
          throw new TypeError("Cannot use 'in' operator on non-object");
        return typeof state === "function" ? receiver === state : state.has(receiver);
      };
      exporter("__extends", __extends2);
      exporter("__assign", __assign2);
      exporter("__rest", __rest2);
      exporter("__decorate", __decorate2);
      exporter("__param", __param2);
      exporter("__metadata", __metadata2);
      exporter("__awaiter", __awaiter2);
      exporter("__generator", __generator2);
      exporter("__exportStar", __exportStar2);
      exporter("__createBinding", __createBinding2);
      exporter("__values", __values2);
      exporter("__read", __read2);
      exporter("__spread", __spread2);
      exporter("__spreadArrays", __spreadArrays2);
      exporter("__spreadArray", __spreadArray2);
      exporter("__await", __await2);
      exporter("__asyncGenerator", __asyncGenerator2);
      exporter("__asyncDelegator", __asyncDelegator2);
      exporter("__asyncValues", __asyncValues2);
      exporter("__makeTemplateObject", __makeTemplateObject2);
      exporter("__importStar", __importStar2);
      exporter("__importDefault", __importDefault2);
      exporter("__classPrivateFieldGet", __classPrivateFieldGet2);
      exporter("__classPrivateFieldSet", __classPrivateFieldSet2);
      exporter("__classPrivateFieldIn", __classPrivateFieldIn2);
    });
  }
});

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => SilhouettePlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian7 = require("obsidian");

// src/settings.ts
var import_obsidian = require("obsidian");

// src/settings-helper.ts
var TextComponentEvent;
((TextComponentEvent2) => {
  function onChange(component, handler) {
    component.inputEl.addEventListener("change", async (ev) => {
      if (!(ev.target instanceof HTMLInputElement)) {
        return;
      }
      handler(ev.target.value);
    });
    return component;
  }
  TextComponentEvent2.onChange = onChange;
})(TextComponentEvent || (TextComponentEvent = {}));

// src/settings.ts
var DEFAULT_SETTINGS = {
  taskFilePath: "",
  holidayFilePath: "",
  fileDateFormat: "",
  timerStorageFilePath: "",
  startNextTaskAutomaticallyAfterDone: false
};
var SilhouetteSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    new import_obsidian.Setting(containerEl).setName("\u7E70\u308A\u8FD4\u3057\u30BF\u30B9\u30AF\u30D5\u30A1\u30A4\u30EB\u306E\u30D1\u30B9").setDesc("\u7E70\u308A\u8FD4\u3057\u30BF\u30B9\u30AF\u30D5\u30A1\u30A4\u30EB\u306E\u30D1\u30B9\u3092Vault root\u304B\u3089\u306E\u76F8\u5BFE\u30D1\u30B9\u3068\u3057\u3066\u6307\u5B9A\u3057\u307E\u3059\u3002").addText((text2) => TextComponentEvent.onChange(text2, async (value) => {
      this.plugin.settings.taskFilePath = value;
      await this.plugin.saveSettings();
    }).setPlaceholder("ex: taskfile.md").setValue(this.plugin.settings.taskFilePath));
    new import_obsidian.Setting(containerEl).setName("\u4F11\u65E5\u8A2D\u5B9A\u30D5\u30A1\u30A4\u30EB\u306E\u30D1\u30B9").setDesc("\u4F11\u65E5\u8A2D\u5B9A\u30D5\u30A1\u30A4\u30EB\u306E\u30D1\u30B9\u3092Vault root\u304B\u3089\u306E\u76F8\u5BFE\u30D1\u30B9\u3068\u3057\u3066\u6307\u5B9A\u3057\u307E\u3059\u3002").addText((text2) => TextComponentEvent.onChange(text2, async (value) => {
      this.plugin.settings.holidayFilePath = value;
      await this.plugin.saveSettings();
    }).setPlaceholder("ex: holiday.md").setValue(this.plugin.settings.holidayFilePath));
    new import_obsidian.Setting(containerEl).setName("\u30D5\u30A1\u30A4\u30EB\u306E\u65E5\u4ED8\u30D5\u30A9\u30FC\u30DE\u30C3\u30C8").setDesc("\u30BF\u30B9\u30AF\u3092\u633F\u5165\u3059\u308B\u65E5\u4ED8\u3092\u5224\u65AD\u3059\u308B\u305F\u3081\u306B\u5FC5\u8981\u306A\u30D5\u30A1\u30A4\u30EB\u540D\u306E\u30D5\u30A9\u30FC\u30DE\u30C3\u30C8\u3092\u6307\u5B9A\u3057\u307E\u3059\u3002").addText((text2) => TextComponentEvent.onChange(text2, async (value) => {
      this.plugin.settings.fileDateFormat = value;
      await this.plugin.saveSettings();
    }).setPlaceholder("ex: MM-DD-YYYY").setValue(this.plugin.settings.fileDateFormat));
    new import_obsidian.Setting(containerEl).setName("\u8A08\u6E2C\u72B6\u614B\u3092\u8A18\u9332\u3057\u305FJSON\u30D5\u30A1\u30A4\u30EB\u306E\u30D1\u30B9").setDesc("\u8A08\u6E2C\u4E2D\u306E\u72B6\u614B\u3092\u8A18\u9332\u3057\u305FJSON\u30D5\u30A1\u30A4\u30EB\u3092\u4FDD\u5B58\u3059\u308B\u30D1\u30B9\u3092\u3001Vault root\u304B\u3089\u306E\u76F8\u5BFE\u30D1\u30B9\u3068\u3057\u3066\u6307\u5B9A\u3057\u307E\u3059\u3002\u6307\u5B9A\u3057\u306A\u304B\u3063\u305F\u5834\u5408\u306F`.obsidian/plugins/silhouette/timer.json`\u306B\u4FDD\u5B58\u3055\u308C\u307E\u3059\u3002").addText((text2) => TextComponentEvent.onChange(text2, async (value) => {
      this.plugin.settings.timerStorageFilePath = value;
      await this.plugin.saveSettings();
    }).setPlaceholder("ex: timer.json").setValue(this.plugin.settings.timerStorageFilePath));
    new import_obsidian.Setting(containerEl).setName("\u5B8C\u4E86\u3057\u305F\u3089\u6B21\u306E\u30BF\u30B9\u30AF\u3092\u81EA\u52D5\u3067\u8A08\u6E2C\u958B\u59CB\u3059\u308B").setDesc("\u6709\u52B9\u306B\u3059\u308B\u3068\u300Ecycle bullet/checkbox\u300F\u30B3\u30DE\u30F3\u30C9\u3067\u30BF\u30B9\u30AF\u3092\u5B8C\u4E86\u3057\u305F\u3042\u3068\u3001\u6B21\u306E\u884C\u306B\u672A\u5B8C\u4E86\u306E\u30BF\u30B9\u30AF\u304C\u5B58\u5728\u3059\u308B\u306A\u3089\u3001\u6B21\u306E\u884C\u306E\u30BF\u30B9\u30AF\u3092\u81EA\u52D5\u3067\u8A08\u6E2C\u958B\u59CB\u3057\u307E\u3059\u3002").addToggle((tc) => {
      tc.setValue(this.plugin.settings.startNextTaskAutomaticallyAfterDone).onChange(async (value) => {
        this.plugin.settings.startNextTaskAutomaticallyAfterDone = value;
        await this.plugin.saveSettings();
      });
    });
  }
};

// src/app-helper.ts
var import_obsidian2 = require("obsidian");

// src/errors.ts
var ExhaustiveError = class extends Error {
  constructor(value, message = `Unsupported type: ${value}`) {
    super(message);
  }
};

// src/app-helper.ts
function createCommand(leaf) {
  switch (leaf) {
    case "same-tab":
      return "editor:follow-link";
    case "new-tab":
      return "editor:open-link-in-new-leaf";
    default:
      throw new ExhaustiveError(leaf);
  }
}
var AppHelper = class {
  constructor(app) {
    this.unsafeApp = app;
  }
  async exists(path) {
    return await this.unsafeApp.vault.adapter.exists(path);
  }
  async deleteFile(path) {
    await this.unsafeApp.vault.adapter.remove(path);
  }
  async deleteFileIfExists(path) {
    if (await this.exists(path)) {
      await this.deleteFile(path);
    }
  }
  async loadFile(path) {
    if (!await this.exists(path)) {
      throw Error(`The file is not found: ${path}`);
    }
    return this.unsafeApp.vault.adapter.read(path);
  }
  async loadJson(path) {
    return JSON.parse(await this.loadFile(path));
  }
  async saveJson(path, data) {
    await this.unsafeApp.vault.adapter.write(path, JSON.stringify(data, null, 2));
  }
  getActiveFile() {
    return this.unsafeApp.workspace.getActiveFile();
  }
  getActiveMarkdownView() {
    return this.unsafeApp.workspace.getActiveViewOfType(import_obsidian2.MarkdownView);
  }
  getActiveMarkdownEditor() {
    var _a, _b;
    return (_b = (_a = this.getActiveMarkdownView()) == null ? void 0 : _a.editor) != null ? _b : null;
  }
  getActiveFileContent() {
    const editor = this.getActiveMarkdownEditor();
    if (!editor) {
      return null;
    }
    return editor.getValue();
  }
  getActiveLine() {
    const editor = this.getActiveMarkdownEditor();
    if (!editor) {
      return null;
    }
    return editor.getLine(editor.getCursor().line);
  }
  getActiveNextLine() {
    const editor = this.getActiveMarkdownEditor();
    if (!editor) {
      return null;
    }
    return editor.getLine(editor.getCursor().line + 1);
  }
  moveNextLine() {
    const editor = this.getActiveMarkdownEditor();
    if (!editor) {
      return;
    }
    const { line } = editor.getCursor();
    return editor.setCursor({ line: line + 1, ch: 0 });
  }
  openLinkInActiveLine(option) {
    const editor = this.getActiveMarkdownEditor();
    if (!editor) {
      return;
    }
    const line = this.getActiveLine();
    if (!line) {
      return;
    }
    const linksMatches = Array.from(line.matchAll(/(?<link>\[\[[^\]]+]])|[ |\n|^|\(](?<url>https?:\/\/[^ )\n]+)/g));
    const firstIndex = linksMatches.map((x) => x.index).at(0);
    if (firstIndex === void 0) {
      return;
    }
    editor.setCursor({ line: editor.getCursor().line, ch: firstIndex + 3 });
    this.executeCoreCommand(createCommand(option.leaf));
  }
  insertStringToActiveFile(str) {
    var _a;
    (_a = this.getActiveMarkdownEditor()) == null ? void 0 : _a.replaceSelection(str);
  }
  replaceStringInActiveLine(str, option) {
    const editor = this.getActiveMarkdownEditor();
    if (!editor) {
      return;
    }
    const { line, ch } = editor.getCursor();
    editor.setLine(line, str);
    const afterCh = (option == null ? void 0 : option.cursor) === "last" ? str.length : Math.min(ch, str.length - 1);
    editor.setCursor({ line, ch: afterCh });
  }
  cycleListCheckList() {
    return this.unsafeApp.commands.executeCommandById("editor:cycle-list-checklist");
  }
  isCheckedCurrentLineTask() {
    const line = this.getActiveLine();
    return line ? /[-*] \[x] .+/.test(line) : false;
  }
  executeCoreCommand(command) {
    return this.unsafeApp.commands.executeCommandById(command);
  }
  removeCommand(commandId) {
    this.unsafeApp.commands.removeCommand(commandId);
  }
};

// src/repository/TaskRepositoryImpl.ts
var import_owlelia3 = __toESM(require_dist());

// src/domain/entity/RepetitionTask.ts
var import_owlelia = __toESM(require_dist());

// src/utils/dates.ts
function isHoliday(date, holidays) {
  return holidays.some((x) => x.equals(date));
}
function isWeekday(date) {
  return date.isMonday || date.isTuesday || date.isWednesday || date.isThursday || date.isFriday;
}
function isWorkday(date, holidays) {
  return isWeekday(date) && !isHoliday(date, holidays);
}
function reverseOffsetWorkdays(dst, days2, holidays) {
  if (!isWorkday(dst, holidays)) {
    return [];
  }
  let dates = [];
  let d = dst.clone();
  if (days2 > 0) {
    while (days2 > 0 || !isWorkday(d, holidays)) {
      if (isWorkday(d, holidays)) {
        days2 -= 1;
      }
      d = d.minusDays(1);
      if (days2 === 0) {
        dates.push(d);
      }
    }
  } else {
    while (days2 < 0 || !isWorkday(d, holidays)) {
      if (isWorkday(d, holidays)) {
        days2 += 1;
      }
      d = d.plusDays(1);
      if (days2 === 0) {
        dates.push(d);
      }
    }
  }
  return dates;
}

// src/domain/entity/RepetitionTask.ts
var _brand = Symbol();
var _RepetitionTask = class extends import_owlelia.Entity {
  static of(props) {
    return new _RepetitionTask(props.name, props);
  }
  cloneWith(partial) {
    return _RepetitionTask.of({ ...this._props, ...partial });
  }
  get name() {
    return this._props.name;
  }
  get indent() {
    var _a;
    return (_a = this._props.indent) != null ? _a : "";
  }
  shouldTry(date, holidays) {
    var _a;
    const p = this._props;
    let targetDate = date.clone();
    if (p.repetition.dayOffset !== 0) {
      targetDate = date.minusDays(p.repetition.dayOffset);
    }
    if (p.repetition.workdayOffset !== 0) {
      const targetDates = reverseOffsetWorkdays(date, p.repetition.workdayOffset, holidays);
      if (targetDates.length === 0) {
        return false;
      }
      const cloned = this.cloneWith({
        repetition: p.repetition.withOffset({
          dayOffset: 0,
          workdayOffset: 0
        })
      });
      return targetDates.some((d) => cloned.shouldTry(d, holidays));
    }
    if ((_a = p.baseDate) == null ? void 0 : _a.isAfter(targetDate)) {
      return false;
    }
    if (!this.includesDay(targetDate, holidays)) {
      return false;
    }
    switch (p.repetition.special) {
      case void 0:
        break;
      case "beginning of month":
        let beginD = targetDate.minusDays(1);
        while (beginD.isAfterOrEquals(targetDate.replaceDay(1), true)) {
          if (this.includesDay(beginD, holidays)) {
            return false;
          }
          beginD = beginD.minusDays(1);
        }
        break;
      case "end of month":
        let endD = targetDate.plusDays(1);
        while (endD.isBeforeOrEquals(targetDate.endOfMonth(), true)) {
          if (this.includesDay(endD, holidays)) {
            return false;
          }
          endD = endD.plusDays(1);
        }
        break;
      default:
        throw new ExhaustiveError(p.repetition.special);
    }
    if (p.repetition.day.type === "period") {
      if (p.repetition.day.period === 1) {
        return true;
      }
      if (!p.baseDate) {
        return false;
      }
      return targetDate.diffDays(p.baseDate) % p.repetition.day.period === 0;
    }
    return p.repetition.day.values.includes(targetDate.day);
  }
  includesDay(date, holidays) {
    if (isHoliday(date, holidays)) {
      if (!this._props.repetition.dayOfWeekHoliday.includes(date.date.getDay()) && !this._props.repetition.dayOfWeekHoliday.includes(date.date.getDay() + 10 * date.nthDayOfWeek)) {
        return false;
      }
    } else {
      if (!this._props.repetition.dayOfWeek.includes(date.date.getDay()) && !this._props.repetition.dayOfWeek.includes(date.date.getDay() + 10 * date.nthDayOfWeek)) {
        return false;
      }
    }
    return true;
  }
};
var RepetitionTask = _RepetitionTask;
_brand;

// src/domain/vo/Repetition.ts
var import_owlelia2 = __toESM(require_dist());
var DAY_OF_WEEK_MAPPINGS = {
  sun: 0,
  mon: 1,
  tue: 2,
  wed: 3,
  thu: 4,
  fri: 5,
  sat: 6,
  "1sun": 10,
  "1mon": 11,
  "1tue": 12,
  "1wed": 13,
  "1thu": 14,
  "1fri": 15,
  "1sat": 16,
  "2sun": 20,
  "2mon": 21,
  "2tue": 22,
  "2wed": 23,
  "2thu": 24,
  "2fri": 25,
  "2sat": 26,
  "3sun": 30,
  "3mon": 31,
  "3tue": 32,
  "3wed": 33,
  "3thu": 34,
  "3fri": 35,
  "3sat": 36,
  "4sun": 40,
  "4mon": 41,
  "4tue": 42,
  "4wed": 43,
  "4thu": 44,
  "4fri": 45,
  "4sat": 46,
  "5sun": 50,
  "5mon": 51,
  "5tue": 52,
  "5wed": 53,
  "5thu": 54,
  "5fri": 55,
  "5sat": 56
};
var DAY_OF_WEEK_WORKDAY_MAPPINGS = {
  "sun!": 0,
  "mon!": 1,
  "tue!": 2,
  "wed!": 3,
  "thu!": 4,
  "fri!": 5,
  "sat!": 6,
  "1sun!": 10,
  "1mon!": 11,
  "1tue!": 12,
  "1wed!": 13,
  "1thu!": 14,
  "1fri!": 15,
  "1sat!": 16,
  "2sun!": 20,
  "2mon!": 21,
  "2tue!": 22,
  "2wed!": 23,
  "2thu!": 24,
  "2fri!": 25,
  "2sat!": 26,
  "3sun!": 30,
  "3mon!": 31,
  "3tue!": 32,
  "3wed!": 33,
  "3thu!": 34,
  "3fri!": 35,
  "3sat!": 36,
  "4sun!": 40,
  "4mon!": 41,
  "4tue!": 42,
  "4wed!": 43,
  "4thu!": 44,
  "4fri!": 45,
  "4sat!": 46,
  "5sun!": 50,
  "5mon!": 51,
  "5tue!": 52,
  "5wed!": 53,
  "5thu!": 54,
  "5fri!": 55,
  "5sat!": 56
};
var DAY_OF_WEEK_HOLIDAY_MAPPINGS = {
  "sun*": 0,
  "mon*": 1,
  "tue*": 2,
  "wed*": 3,
  "thu*": 4,
  "fri*": 5,
  "sat*": 6,
  "1sun*": 10,
  "1mon*": 11,
  "1tue*": 12,
  "1wed*": 13,
  "1thu*": 14,
  "1fri*": 15,
  "1sat*": 16,
  "2sun*": 20,
  "2mon*": 21,
  "2tue*": 22,
  "2wed*": 23,
  "2thu*": 24,
  "2fri*": 25,
  "2sat*": 26,
  "3sun*": 30,
  "3mon*": 31,
  "3tue*": 32,
  "3wed*": 33,
  "3thu*": 34,
  "3fri*": 35,
  "3sat*": 36,
  "4sun*": 40,
  "4mon*": 41,
  "4tue*": 42,
  "4wed*": 43,
  "4thu*": 44,
  "4fri*": 45,
  "4sat*": 46,
  "5sun*": 50,
  "5mon*": 51,
  "5tue*": 52,
  "5wed*": 53,
  "5thu*": 54,
  "5fri*": 55,
  "5sat*": 56
};
var repetitionBase = {
  day: { type: "period", period: 1 },
  dayOfWeek: [0, 1, 2, 3, 4, 5, 6],
  dayOfWeekHoliday: [0, 1, 2, 3, 4, 5, 6],
  week: { type: "period", period: 1 },
  month: { type: "period", period: 1 },
  dayOffset: 0,
  workdayOffset: 0
};
var _brand2 = Symbol();
var _Repetition = class extends import_owlelia2.ValueObject {
  static get everyDay() {
    return new _Repetition(repetitionBase);
  }
  static get weekday() {
    return new _Repetition({
      ...repetitionBase,
      dayOfWeek: [1, 2, 3, 4, 5],
      dayOfWeekHoliday: [1, 2, 3, 4, 5]
    });
  }
  static get weekend() {
    return new _Repetition({
      ...repetitionBase,
      dayOfWeek: [0, 6],
      dayOfWeekHoliday: [0, 6]
    });
  }
  static get workday() {
    return new _Repetition({
      ...repetitionBase,
      dayOfWeek: [1, 2, 3, 4, 5],
      dayOfWeekHoliday: []
    });
  }
  static get nonWorkday() {
    return new _Repetition({
      ...repetitionBase,
      dayOfWeek: [0, 6],
      dayOfWeekHoliday: [0, 1, 2, 3, 4, 5, 6]
    });
  }
  static everyNDay(nDay) {
    return new _Repetition({
      ...repetitionBase,
      day: { type: "period", period: nDay }
    });
  }
  static get endOfMonth() {
    return new _Repetition({
      ...repetitionBase,
      special: "end of month"
    });
  }
  static get workdayEndOfMonth() {
    return new _Repetition({
      ...repetitionBase,
      dayOfWeek: [1, 2, 3, 4, 5],
      dayOfWeekHoliday: [],
      special: "end of month"
    });
  }
  static get beginningOfMonth() {
    return new _Repetition({
      ...repetitionBase,
      special: "beginning of month"
    });
  }
  static get workdayBeginningOfMonth() {
    return new _Repetition({
      ...repetitionBase,
      dayOfWeek: [1, 2, 3, 4, 5],
      dayOfWeekHoliday: [],
      special: "beginning of month"
    });
  }
  get day() {
    return this._value.day;
  }
  get dayOfWeek() {
    return this._value.dayOfWeek;
  }
  get dayOfWeekHoliday() {
    return this._value.dayOfWeekHoliday;
  }
  get week() {
    return this._value.week;
  }
  get month() {
    return this._value.month;
  }
  get dayOffset() {
    return this._value.dayOffset;
  }
  get workdayOffset() {
    return this._value.workdayOffset;
  }
  get special() {
    return this._value.special;
  }
  withOffset(args) {
    const { dayOffset, workdayOffset } = args;
    return new _Repetition({ ...this._value, dayOffset, workdayOffset });
  }
  static divideTokenWithOffset(token) {
    const [tp, pastOffset] = token.split("<");
    if (pastOffset) {
      return pastOffset.endsWith("!") ? [tp, 0, -1 * Number(pastOffset.slice(0, -1))] : [tp, -1 * Number(pastOffset), 0];
    }
    const [tf, futureOffset] = token.split(">");
    if (futureOffset) {
      return futureOffset.endsWith("!") ? [tf, 0, Number(futureOffset.slice(0, -1))] : [tf, Number(futureOffset), 0];
    }
    return [token, 0, 0];
  }
  static from(str) {
    var _a, _b;
    const [token, dayOffset, workdayOffset] = this.divideTokenWithOffset(str);
    switch (token) {
      case "every day":
        return _Repetition.everyDay.withOffset({ dayOffset, workdayOffset });
      case "weekday":
        return _Repetition.weekday.withOffset({ dayOffset, workdayOffset });
      case "weekend":
        return _Repetition.weekend.withOffset({ dayOffset, workdayOffset });
      case "workday":
        return _Repetition.workday.withOffset({ dayOffset, workdayOffset });
      case "non workday":
        return _Repetition.nonWorkday.withOffset({ dayOffset, workdayOffset });
      case "end of month":
        return _Repetition.endOfMonth.withOffset({ dayOffset, workdayOffset });
      case "workday end of month":
        return _Repetition.workdayEndOfMonth.withOffset({
          dayOffset,
          workdayOffset
        });
      case "beginning of month":
        return _Repetition.beginningOfMonth.withOffset({
          dayOffset,
          workdayOffset
        });
      case "workday beginning of month":
        return _Repetition.workdayBeginningOfMonth.withOffset({
          dayOffset,
          workdayOffset
        });
      default:
        const dayPeriod = (_b = (_a = token.match(/every (?<period>\d+) day/)) == null ? void 0 : _a.groups) == null ? void 0 : _b.period;
        if (dayPeriod) {
          return _Repetition.everyNDay(Number(dayPeriod)).withOffset({
            dayOffset,
            workdayOffset
          });
        }
    }
    const tokens = token.split("/");
    const dayOfWeek = tokens.map((x) => {
      var _a2;
      return (_a2 = DAY_OF_WEEK_MAPPINGS[x]) != null ? _a2 : DAY_OF_WEEK_WORKDAY_MAPPINGS[x];
    }).filter((x) => x !== void 0);
    const dayOfWeekHoliday = tokens.map((x) => {
      var _a2;
      return (_a2 = DAY_OF_WEEK_MAPPINGS[x]) != null ? _a2 : DAY_OF_WEEK_HOLIDAY_MAPPINGS[x];
    }).filter((x) => x !== void 0);
    if (dayOfWeek.length > 0 || dayOfWeekHoliday.length > 0) {
      return new _Repetition({
        day: { type: "period", period: 1 },
        dayOfWeek,
        dayOfWeekHoliday,
        week: { type: "period", period: 1 },
        month: { type: "period", period: 1 },
        dayOffset,
        workdayOffset
      });
    }
    const days2 = tokens.filter((x) => Boolean(x.match(/^[0-9]+d$/))).map((x) => Number(x.slice(0, -1)));
    const to = (x) => x.length > 0 ? { type: "specific", values: x } : { type: "period", period: 1 };
    return new _Repetition({
      day: to(days2),
      dayOfWeek: [0, 1, 2, 3, 4, 5, 6],
      dayOfWeekHoliday: [0, 1, 2, 3, 4, 5, 6],
      week: { type: "period", period: 1 },
      month: { type: "period", period: 1 },
      dayOffset,
      workdayOffset
    });
  }
};
var Repetition = _Repetition;
_brand2;

// src/repository/TaskRepositoryImpl.ts
var TaskRepositoryImpl = class {
  constructor(appHelper, repetitionTasksFilePath, holidaysFilePath) {
    this.appHelper = appHelper;
    this.repetitionTasksFilePath = repetitionTasksFilePath;
    this.holidaysFilePath = holidaysFilePath;
  }
  loadRepetitionTasks() {
    return (0, import_owlelia3.fromPromise)(this.appHelper.loadFile(this.repetitionTasksFilePath).then((tasksStr) => tasksStr.split("\n").filter((line) => !line.startsWith("//") && line.trim() !== "").map((line) => line.split(",")).filter((cols) => cols.length > 1).map(([name, repetition, baseDate]) => {
      var _a, _b;
      return RepetitionTask.of({
        name: name.replace(/^[ \t]+/, ""),
        repetition: Repetition.from(repetition),
        baseDate: baseDate ? import_owlelia3.DateTime.of(baseDate) : void 0,
        indent: (_b = (_a = name.match("^[ 	]+")) == null ? void 0 : _a.at(0)) != null ? _b : ""
      });
    })));
  }
  loadHolidays() {
    return (0, import_owlelia3.fromPromise)(this.holidaysFilePath ? this.appHelper.loadFile(this.holidaysFilePath).then((holidaysStr) => holidaysStr.split("\n").filter((line) => !line.startsWith("//") && line.trim() !== "").map(import_owlelia3.DateTime.of).filter((x) => !Number.isNaN(x.date.getTime()))) : Promise.resolve([]));
  }
};

// src/repository/TimerRepositoryImpl.ts
var import_owlelia5 = __toESM(require_dist());

// src/domain/vo/Timer.ts
var import_owlelia4 = __toESM(require_dist());
var _brand3 = Symbol();
var _Timer = class extends import_owlelia4.ValueObject {
  static of(props) {
    return new _Timer(props);
  }
  stop(now2) {
    return new _Timer({
      ...this._value,
      startTime: void 0,
      accumulatedSeconds: now2.diffSeconds(this._value.startTime) + this._value.accumulatedSeconds
    });
  }
  get name() {
    return this._value.name;
  }
  get accumulatedSeconds() {
    return this._value.accumulatedSeconds;
  }
  get startTime() {
    return this._value.startTime;
  }
};
var Timer = _Timer;
_brand3;

// src/repository/TimerRepositoryImpl.ts
var import_obsidian3 = require("obsidian");
var TimerRepositoryImpl = class {
  constructor(appHelper, timerStorageFilePath) {
    this.appHelper = appHelper;
    this.timerStorageFilePath = timerStorageFilePath;
  }
  get normalizedTimerStoragePath() {
    return (0, import_obsidian3.normalizePath)(this.timerStorageFilePath);
  }
  hasTimer() {
    return this.appHelper.exists(this.normalizedTimerStoragePath);
  }
  loadTimer() {
    return (0, import_owlelia5.fromPromise)(this.appHelper.loadJson(this.normalizedTimerStoragePath).then((x) => Timer.of({
      name: x.name,
      accumulatedSeconds: x.accumulatedSeconds,
      startTime: x.startTime ? import_owlelia5.DateTime.of(x.startTime) : void 0
    })));
  }
  saveTimer(timer) {
    var _a;
    return (0, import_owlelia5.fromPromise)(this.appHelper.saveJson(this.normalizedTimerStoragePath, {
      name: timer.name,
      accumulatedSeconds: timer.accumulatedSeconds,
      startTime: (_a = timer.startTime) == null ? void 0 : _a.unix
    }));
  }
  clearTimer() {
    return (0, import_owlelia5.fromPromise)(this.appHelper.deleteFileIfExists(this.normalizedTimerStoragePath));
  }
};

// src/app/TaskServiceImpl.ts
var import_owlelia6 = __toESM(require_dist());
var TaskServiceImpl = class {
  constructor(appHelper, repository) {
    this.appHelper = appHelper;
    this.repository = repository;
  }
  serRepository(repository) {
    this.repository = repository;
  }
  async insertTasksToDailyNote(date) {
    const [tasks, err] = (await this.loadRepetitionTasks()).unwrap();
    if (err) {
      return err;
    }
    const [holidays, err2] = (await this.loadHolidays()).unwrap();
    if (err2) {
      return err2;
    }
    this.appHelper.insertStringToActiveFile(tasks.filter((x) => x.shouldTry(date, holidays)).map((x) => `${x.indent}- [ ] ${x.name}`).join("\n"));
  }
  async loadRepetitionTasks() {
    return this.repository.loadRepetitionTasks();
  }
  async loadHolidays() {
    return this.repository.loadHolidays();
  }
  calcDatesInFuture(task, holidays) {
    const today2 = import_owlelia6.DateTime.today();
    return today2.toDate(today2.plusMonths(3)).filter((x) => task.shouldTry(x, holidays));
  }
};

// src/ui/RepetitionTaskItemView.ts
var import_obsidian4 = require("obsidian");

// node_modules/svelte/internal/index.mjs
function noop() {
}
var identity = (x) => x;
function assign(tar, src) {
  for (const k in src)
    tar[k] = src[k];
  return tar;
}
function run(fn) {
  return fn();
}
function blank_object() {
  return /* @__PURE__ */ Object.create(null);
}
function run_all(fns) {
  fns.forEach(run);
}
function is_function(thing) {
  return typeof thing === "function";
}
function safe_not_equal(a, b) {
  return a != a ? b == b : a !== b || (a && typeof a === "object" || typeof a === "function");
}
function is_empty(obj) {
  return Object.keys(obj).length === 0;
}
function subscribe(store, ...callbacks) {
  if (store == null) {
    return noop;
  }
  const unsub = store.subscribe(...callbacks);
  return unsub.unsubscribe ? () => unsub.unsubscribe() : unsub;
}
function get_store_value(store) {
  let value;
  subscribe(store, (_) => value = _)();
  return value;
}
function component_subscribe(component, store, callback) {
  component.$$.on_destroy.push(subscribe(store, callback));
}
function create_slot(definition, ctx, $$scope, fn) {
  if (definition) {
    const slot_ctx = get_slot_context(definition, ctx, $$scope, fn);
    return definition[0](slot_ctx);
  }
}
function get_slot_context(definition, ctx, $$scope, fn) {
  return definition[1] && fn ? assign($$scope.ctx.slice(), definition[1](fn(ctx))) : $$scope.ctx;
}
function get_slot_changes(definition, $$scope, dirty, fn) {
  if (definition[2] && fn) {
    const lets = definition[2](fn(dirty));
    if ($$scope.dirty === void 0) {
      return lets;
    }
    if (typeof lets === "object") {
      const merged = [];
      const len = Math.max($$scope.dirty.length, lets.length);
      for (let i = 0; i < len; i += 1) {
        merged[i] = $$scope.dirty[i] | lets[i];
      }
      return merged;
    }
    return $$scope.dirty | lets;
  }
  return $$scope.dirty;
}
function update_slot_base(slot, slot_definition, ctx, $$scope, slot_changes, get_slot_context_fn) {
  if (slot_changes) {
    const slot_context = get_slot_context(slot_definition, ctx, $$scope, get_slot_context_fn);
    slot.p(slot_context, slot_changes);
  }
}
function get_all_dirty_from_scope($$scope) {
  if ($$scope.ctx.length > 32) {
    const dirty = [];
    const length = $$scope.ctx.length / 32;
    for (let i = 0; i < length; i++) {
      dirty[i] = -1;
    }
    return dirty;
  }
  return -1;
}
function set_store_value(store, ret, value) {
  store.set(value);
  return ret;
}
function action_destroyer(action_result) {
  return action_result && is_function(action_result.destroy) ? action_result.destroy : noop;
}
var is_hydrating = false;
function start_hydrating() {
  is_hydrating = true;
}
function end_hydrating() {
  is_hydrating = false;
}
function append(target, node) {
  target.appendChild(node);
}
function insert(target, node, anchor) {
  target.insertBefore(node, anchor || null);
}
function detach(node) {
  if (node.parentNode) {
    node.parentNode.removeChild(node);
  }
}
function destroy_each(iterations, detaching) {
  for (let i = 0; i < iterations.length; i += 1) {
    if (iterations[i])
      iterations[i].d(detaching);
  }
}
function element(name) {
  return document.createElement(name);
}
function text(data) {
  return document.createTextNode(data);
}
function space() {
  return text(" ");
}
function empty() {
  return text("");
}
function listen(node, event, handler, options) {
  node.addEventListener(event, handler, options);
  return () => node.removeEventListener(event, handler, options);
}
function stop_propagation(fn) {
  return function(event) {
    event.stopPropagation();
    return fn.call(this, event);
  };
}
function attr(node, attribute, value) {
  if (value == null)
    node.removeAttribute(attribute);
  else if (node.getAttribute(attribute) !== value)
    node.setAttribute(attribute, value);
}
function children(element2) {
  return Array.from(element2.childNodes);
}
function set_data(text2, data) {
  data = "" + data;
  if (text2.wholeText !== data)
    text2.data = data;
}
function set_style(node, key, value, important) {
  if (value === null) {
    node.style.removeProperty(key);
  } else {
    node.style.setProperty(key, value, important ? "important" : "");
  }
}
function toggle_class(element2, name, toggle) {
  element2.classList[toggle ? "add" : "remove"](name);
}
function construct_svelte_component(component, props) {
  return new component(props);
}
var current_component;
function set_current_component(component) {
  current_component = component;
}
function get_current_component() {
  if (!current_component)
    throw new Error("Function called outside component initialization");
  return current_component;
}
function onMount(fn) {
  get_current_component().$$.on_mount.push(fn);
}
function afterUpdate(fn) {
  get_current_component().$$.after_update.push(fn);
}
function setContext(key, context) {
  get_current_component().$$.context.set(key, context);
  return context;
}
function getContext(key) {
  return get_current_component().$$.context.get(key);
}
function bubble(component, event) {
  const callbacks = component.$$.callbacks[event.type];
  if (callbacks) {
    callbacks.slice().forEach((fn) => fn.call(this, event));
  }
}
var dirty_components = [];
var binding_callbacks = [];
var render_callbacks = [];
var flush_callbacks = [];
var resolved_promise = Promise.resolve();
var update_scheduled = false;
function schedule_update() {
  if (!update_scheduled) {
    update_scheduled = true;
    resolved_promise.then(flush);
  }
}
function tick() {
  schedule_update();
  return resolved_promise;
}
function add_render_callback(fn) {
  render_callbacks.push(fn);
}
var seen_callbacks = /* @__PURE__ */ new Set();
var flushidx = 0;
function flush() {
  if (flushidx !== 0) {
    return;
  }
  const saved_component = current_component;
  do {
    try {
      while (flushidx < dirty_components.length) {
        const component = dirty_components[flushidx];
        flushidx++;
        set_current_component(component);
        update(component.$$);
      }
    } catch (e) {
      dirty_components.length = 0;
      flushidx = 0;
      throw e;
    }
    set_current_component(null);
    dirty_components.length = 0;
    flushidx = 0;
    while (binding_callbacks.length)
      binding_callbacks.pop()();
    for (let i = 0; i < render_callbacks.length; i += 1) {
      const callback = render_callbacks[i];
      if (!seen_callbacks.has(callback)) {
        seen_callbacks.add(callback);
        callback();
      }
    }
    render_callbacks.length = 0;
  } while (dirty_components.length);
  while (flush_callbacks.length) {
    flush_callbacks.pop()();
  }
  update_scheduled = false;
  seen_callbacks.clear();
  set_current_component(saved_component);
}
function update($$) {
  if ($$.fragment !== null) {
    $$.update();
    run_all($$.before_update);
    const dirty = $$.dirty;
    $$.dirty = [-1];
    $$.fragment && $$.fragment.p($$.ctx, dirty);
    $$.after_update.forEach(add_render_callback);
  }
}
var outroing = /* @__PURE__ */ new Set();
var outros;
function group_outros() {
  outros = {
    r: 0,
    c: [],
    p: outros
  };
}
function check_outros() {
  if (!outros.r) {
    run_all(outros.c);
  }
  outros = outros.p;
}
function transition_in(block, local) {
  if (block && block.i) {
    outroing.delete(block);
    block.i(local);
  }
}
function transition_out(block, local, detach2, callback) {
  if (block && block.o) {
    if (outroing.has(block))
      return;
    outroing.add(block);
    outros.c.push(() => {
      outroing.delete(block);
      if (callback) {
        if (detach2)
          block.d(1);
        callback();
      }
    });
    block.o(local);
  } else if (callback) {
    callback();
  }
}
var globals = typeof window !== "undefined" ? window : typeof globalThis !== "undefined" ? globalThis : global;
function outro_and_destroy_block(block, lookup) {
  transition_out(block, 1, 1, () => {
    lookup.delete(block.key);
  });
}
function update_keyed_each(old_blocks, dirty, get_key, dynamic, ctx, list, lookup, node, destroy, create_each_block4, next, get_context) {
  let o = old_blocks.length;
  let n = list.length;
  let i = o;
  const old_indexes = {};
  while (i--)
    old_indexes[old_blocks[i].key] = i;
  const new_blocks = [];
  const new_lookup = /* @__PURE__ */ new Map();
  const deltas = /* @__PURE__ */ new Map();
  i = n;
  while (i--) {
    const child_ctx = get_context(ctx, list, i);
    const key = get_key(child_ctx);
    let block = lookup.get(key);
    if (!block) {
      block = create_each_block4(key, child_ctx);
      block.c();
    } else if (dynamic) {
      block.p(child_ctx, dirty);
    }
    new_lookup.set(key, new_blocks[i] = block);
    if (key in old_indexes)
      deltas.set(key, Math.abs(i - old_indexes[key]));
  }
  const will_move = /* @__PURE__ */ new Set();
  const did_move = /* @__PURE__ */ new Set();
  function insert2(block) {
    transition_in(block, 1);
    block.m(node, next);
    lookup.set(block.key, block);
    next = block.first;
    n--;
  }
  while (o && n) {
    const new_block = new_blocks[n - 1];
    const old_block = old_blocks[o - 1];
    const new_key = new_block.key;
    const old_key = old_block.key;
    if (new_block === old_block) {
      next = new_block.first;
      o--;
      n--;
    } else if (!new_lookup.has(old_key)) {
      destroy(old_block, lookup);
      o--;
    } else if (!lookup.has(new_key) || will_move.has(new_key)) {
      insert2(new_block);
    } else if (did_move.has(old_key)) {
      o--;
    } else if (deltas.get(new_key) > deltas.get(old_key)) {
      did_move.add(new_key);
      insert2(new_block);
    } else {
      will_move.add(old_key);
      o--;
    }
  }
  while (o--) {
    const old_block = old_blocks[o];
    if (!new_lookup.has(old_block.key))
      destroy(old_block, lookup);
  }
  while (n)
    insert2(new_blocks[n - 1]);
  return new_blocks;
}
function create_component(block) {
  block && block.c();
}
function mount_component(component, target, anchor, customElement) {
  const { fragment, after_update } = component.$$;
  fragment && fragment.m(target, anchor);
  if (!customElement) {
    add_render_callback(() => {
      const new_on_destroy = component.$$.on_mount.map(run).filter(is_function);
      if (component.$$.on_destroy) {
        component.$$.on_destroy.push(...new_on_destroy);
      } else {
        run_all(new_on_destroy);
      }
      component.$$.on_mount = [];
    });
  }
  after_update.forEach(add_render_callback);
}
function destroy_component(component, detaching) {
  const $$ = component.$$;
  if ($$.fragment !== null) {
    run_all($$.on_destroy);
    $$.fragment && $$.fragment.d(detaching);
    $$.on_destroy = $$.fragment = null;
    $$.ctx = [];
  }
}
function make_dirty(component, i) {
  if (component.$$.dirty[0] === -1) {
    dirty_components.push(component);
    schedule_update();
    component.$$.dirty.fill(0);
  }
  component.$$.dirty[i / 31 | 0] |= 1 << i % 31;
}
function init(component, options, instance4, create_fragment4, not_equal, props, append_styles, dirty = [-1]) {
  const parent_component = current_component;
  set_current_component(component);
  const $$ = component.$$ = {
    fragment: null,
    ctx: [],
    props,
    update: noop,
    not_equal,
    bound: blank_object(),
    on_mount: [],
    on_destroy: [],
    on_disconnect: [],
    before_update: [],
    after_update: [],
    context: new Map(options.context || (parent_component ? parent_component.$$.context : [])),
    callbacks: blank_object(),
    dirty,
    skip_bound: false,
    root: options.target || parent_component.$$.root
  };
  append_styles && append_styles($$.root);
  let ready = false;
  $$.ctx = instance4 ? instance4(component, options.props || {}, (i, ret, ...rest) => {
    const value = rest.length ? rest[0] : ret;
    if ($$.ctx && not_equal($$.ctx[i], $$.ctx[i] = value)) {
      if (!$$.skip_bound && $$.bound[i])
        $$.bound[i](value);
      if (ready)
        make_dirty(component, i);
    }
    return ret;
  }) : [];
  $$.update();
  ready = true;
  run_all($$.before_update);
  $$.fragment = create_fragment4 ? create_fragment4($$.ctx) : false;
  if (options.target) {
    if (options.hydrate) {
      start_hydrating();
      const nodes = children(options.target);
      $$.fragment && $$.fragment.l(nodes);
      nodes.forEach(detach);
    } else {
      $$.fragment && $$.fragment.c();
    }
    if (options.intro)
      transition_in(component.$$.fragment);
    mount_component(component, options.target, options.anchor, options.customElement);
    end_hydrating();
    flush();
  }
  set_current_component(parent_component);
}
var SvelteElement;
if (typeof HTMLElement === "function") {
  SvelteElement = class extends HTMLElement {
    constructor() {
      super();
      this.attachShadow({ mode: "open" });
    }
    connectedCallback() {
      const { on_mount } = this.$$;
      this.$$.on_disconnect = on_mount.map(run).filter(is_function);
      for (const key in this.$$.slotted) {
        this.appendChild(this.$$.slotted[key]);
      }
    }
    attributeChangedCallback(attr2, _oldValue, newValue) {
      this[attr2] = newValue;
    }
    disconnectedCallback() {
      run_all(this.$$.on_disconnect);
    }
    $destroy() {
      destroy_component(this, 1);
      this.$destroy = noop;
    }
    $on(type, callback) {
      if (!is_function(callback)) {
        return noop;
      }
      const callbacks = this.$$.callbacks[type] || (this.$$.callbacks[type] = []);
      callbacks.push(callback);
      return () => {
        const index2 = callbacks.indexOf(callback);
        if (index2 !== -1)
          callbacks.splice(index2, 1);
      };
    }
    $set($$props) {
      if (this.$$set && !is_empty($$props)) {
        this.$$.skip_bound = true;
        this.$$set($$props);
        this.$$.skip_bound = false;
      }
    }
  };
}
var SvelteComponent = class {
  $destroy() {
    destroy_component(this, 1);
    this.$destroy = noop;
  }
  $on(type, callback) {
    if (!is_function(callback)) {
      return noop;
    }
    const callbacks = this.$$.callbacks[type] || (this.$$.callbacks[type] = []);
    callbacks.push(callback);
    return () => {
      const index2 = callbacks.indexOf(callback);
      if (index2 !== -1)
        callbacks.splice(index2, 1);
    };
  }
  $set($$props) {
    if (this.$$set && !is_empty($$props)) {
      this.$$.skip_bound = true;
      this.$$set($$props);
      this.$$.skip_bound = false;
    }
  }
};

// node_modules/tslib/modules/index.js
var import_tslib = __toESM(require_tslib(), 1);
var {
  __extends,
  __assign,
  __rest,
  __decorate,
  __param,
  __metadata,
  __awaiter,
  __generator,
  __exportStar,
  __createBinding,
  __values,
  __read,
  __spread,
  __spreadArrays,
  __spreadArray,
  __await,
  __asyncGenerator,
  __asyncDelegator,
  __asyncValues,
  __makeTemplateObject,
  __importStar,
  __importDefault,
  __classPrivateFieldGet,
  __classPrivateFieldSet,
  __classPrivateFieldIn
} = import_tslib.default;

// node_modules/svelte/store/index.mjs
var subscriber_queue = [];
function readable(value, start) {
  return {
    subscribe: writable(value, start).subscribe
  };
}
function writable(value, start = noop) {
  let stop;
  const subscribers = /* @__PURE__ */ new Set();
  function set(new_value) {
    if (safe_not_equal(value, new_value)) {
      value = new_value;
      if (stop) {
        const run_queue = !subscriber_queue.length;
        for (const subscriber of subscribers) {
          subscriber[1]();
          subscriber_queue.push(subscriber, value);
        }
        if (run_queue) {
          for (let i = 0; i < subscriber_queue.length; i += 2) {
            subscriber_queue[i][0](subscriber_queue[i + 1]);
          }
          subscriber_queue.length = 0;
        }
      }
    }
  }
  function update2(fn) {
    set(fn(value));
  }
  function subscribe2(run2, invalidate = noop) {
    const subscriber = [run2, invalidate];
    subscribers.add(subscriber);
    if (subscribers.size === 1) {
      stop = start(set) || noop;
    }
    run2(value);
    return () => {
      subscribers.delete(subscriber);
      if (subscribers.size === 0) {
        stop();
        stop = null;
      }
    };
  }
  return { set, update: update2, subscribe: subscribe2 };
}
function derived(stores, fn, initial_value) {
  const single = !Array.isArray(stores);
  const stores_array = single ? [stores] : stores;
  const auto = fn.length < 2;
  return readable(initial_value, (set) => {
    let inited = false;
    const values = [];
    let pending = 0;
    let cleanup = noop;
    const sync = () => {
      if (pending) {
        return;
      }
      cleanup();
      const result = fn(single ? values[0] : values, set);
      if (auto) {
        set(result);
      } else {
        cleanup = is_function(result) ? result : noop;
      }
    };
    const unsubscribers = stores_array.map((store, i) => subscribe(store, (value) => {
      values[i] = value;
      pending &= ~(1 << i);
      if (inited) {
        sync();
      }
    }, () => {
      pending |= 1 << i;
    }));
    inited = true;
    sync();
    return function stop() {
      run_all(unsubscribers);
      cleanup();
    };
  });
}

// node_modules/@event-calendar/common/index.js
function assign2(...args) {
  return Object.assign(...args);
}
function hasYScroll(el) {
  return el.scrollHeight > el.clientHeight;
}
function rect(el) {
  return el.getBoundingClientRect();
}
function ancestor(el, up) {
  while (up--) {
    el = el.parentElement;
  }
  return el;
}
function height(el) {
  return el.offsetHeight;
}
var ignoreProp = Symbol("ec");
function ignore(jsEvent) {
  jsEvent[ignoreProp] = true;
}
function maybeIgnore(fn) {
  return (jsEvent) => {
    if (!jsEvent[ignoreProp]) {
      fn && fn(jsEvent);
      ignore(jsEvent);
    }
  };
}
function isObject(test) {
  return typeof test === "object" && test !== null;
}
function createElement(tag, className, html, text2) {
  let el = document.createElement(tag);
  el.className = className;
  if (html) {
    el.innerHTML = html;
  } else if (text2) {
    el.innerText = text2;
  }
  return el;
}
function setContent(node, content) {
  let actions = {
    update(content2) {
      while (node.firstChild) {
        node.removeChild(node.lastChild);
      }
      if (!isObject(content2)) {
        node.innerText = content2;
      } else if (content2.domNodes) {
        for (let child of content2.domNodes) {
          node.appendChild(child);
        }
      } else if (content2.html) {
        node.innerHTML = content2.html;
      }
    }
  };
  actions.update(content);
  return actions;
}
function outsideEvent(node, type) {
  const handlePointerDown = (jsEvent) => {
    if (node && !node.contains(jsEvent.target)) {
      node.dispatchEvent(new CustomEvent(type + "outside", { detail: { jsEvent } }));
    }
  };
  document.addEventListener(type, handlePointerDown, true);
  return {
    destroy() {
      document.removeEventListener(type, handlePointerDown, true);
    }
  };
}
var DAY_IN_SECONDS = 86400;
function createDate(input) {
  if (input !== void 0) {
    return input instanceof Date ? _fromLocalDate(input) : _fromISOString(input);
  }
  return _fromLocalDate(new Date());
}
function createDuration(input) {
  if (typeof input === "number") {
    input = { seconds: input };
  } else if (typeof input === "string") {
    let seconds = 0, exp = 2;
    for (let part of input.split(":", 3)) {
      seconds += parseInt(part, 10) * Math.pow(60, exp--);
    }
    input = { seconds };
  } else if (input instanceof Date) {
    input = { hours: input.getUTCHours(), minutes: input.getUTCMinutes(), seconds: input.getUTCSeconds() };
  }
  let weeks = input.weeks || input.week || 0;
  return {
    years: input.years || input.year || 0,
    months: input.months || input.month || 0,
    days: weeks * 7 + (input.days || input.day || 0),
    seconds: (input.hours || input.hour || 0) * 60 * 60 + (input.minutes || input.minute || 0) * 60 + (input.seconds || input.second || 0),
    inWeeks: !!weeks
  };
}
function cloneDate(date) {
  return new Date(date.getTime());
}
function addDuration(date, duration, x) {
  if (x === void 0) {
    x = 1;
  }
  date.setUTCFullYear(date.getUTCFullYear() + x * duration.years);
  let month = date.getUTCMonth() + x * duration.months;
  date.setUTCMonth(month);
  month %= 12;
  if (month < 0) {
    month += 12;
  }
  while (date.getUTCMonth() !== month) {
    subtractDay(date);
  }
  date.setUTCDate(date.getUTCDate() + x * duration.days);
  date.setUTCSeconds(date.getUTCSeconds() + x * duration.seconds);
  return date;
}
function subtractDuration(date, duration, x) {
  return addDuration(date, duration, x === void 0 ? -1 : -x);
}
function addDay(date, x) {
  date.setUTCDate(date.getUTCDate() + (x === void 0 ? 1 : x));
  return date;
}
function subtractDay(date, x) {
  return addDay(date, x === void 0 ? -1 : -x);
}
function setMidnight(date) {
  date.setUTCHours(0, 0, 0, 0);
  return date;
}
function toLocalDate(date) {
  return new Date(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate(), date.getUTCHours(), date.getUTCMinutes(), date.getUTCSeconds());
}
function toISOString(date) {
  return date.toISOString().substring(0, 19);
}
function formatRange(start, end, intl2) {
  if (start.getFullYear() !== end.getFullYear()) {
    return intl2.format(start) + " - " + intl2.format(end);
  }
  let diff2 = [];
  if (start.getMonth() !== end.getMonth()) {
    diff2.push("month");
  }
  if (start.getDate() !== end.getDate()) {
    diff2.push("day");
  }
  if (!diff2.length) {
    return intl2.format(start);
  }
  let opts1 = intl2.resolvedOptions();
  let opts2 = {};
  for (let key of diff2) {
    opts2[key] = opts1[key];
  }
  let intl22 = new Intl.DateTimeFormat(opts1.locale, opts2);
  let full1 = intl2.format(start);
  let full2 = intl2.format(end);
  let part1 = intl22.format(start);
  let part2 = intl22.format(end);
  let common = _commonChunks(full1, part1, full2, part2);
  if (common) {
    return common.head + part1 + " - " + part2 + common.tail;
  }
  return full1 + " - " + full2;
}
function datesEqual(date1, ...dates2) {
  return dates2.every((date2) => date1.getTime() === date2.getTime());
}
function nextClosestDay(date, day) {
  let diff2 = day - date.getUTCDay();
  date.setUTCDate(date.getUTCDate() + (diff2 >= 0 ? diff2 : diff2 + 7));
  return date;
}
function prevClosestDay(date, day) {
  let diff2 = day - date.getUTCDay();
  date.setUTCDate(date.getUTCDate() + (diff2 <= 0 ? diff2 : diff2 - 7));
  return date;
}
function _fromLocalDate(date) {
  return new Date(Date.UTC(date.getFullYear(), date.getMonth(), date.getDate(), date.getHours(), date.getMinutes(), date.getSeconds()));
}
function _fromISOString(str) {
  const parts = str.match(/\d+/g);
  return new Date(Date.UTC(Number(parts[0]), Number(parts[1]) - 1, Number(parts[2]), Number(parts[3] || 0), Number(parts[4] || 0), Number(parts[5] || 0)));
}
function _commonChunks(str1, substr1, str2, substr2) {
  let i = 0;
  while (i < str1.length) {
    let res1;
    [i, res1] = _cut(str1, substr1, i);
    if (!res1) {
      break;
    }
    let j = 0;
    while (j < str2.length) {
      let res2;
      [j, res2] = _cut(str2, substr2, j);
      if (!res2) {
        break;
      }
      if (res1.head === res2.head && res1.tail === res2.tail) {
        return res1;
      }
    }
  }
  return null;
}
function _cut(str, substr, from) {
  let start = str.indexOf(substr, from);
  if (start >= 0) {
    let end = start + substr.length;
    return [end, {
      head: str.substr(0, start),
      tail: str.substr(end)
    }];
  }
  return [-1, null];
}
function createView(view2, _viewTitle, _currentRange, _activeRange) {
  return {
    type: view2,
    title: _viewTitle,
    currentStart: _currentRange.start,
    currentEnd: _currentRange.end,
    activeStart: _activeRange.start,
    activeEnd: _activeRange.end,
    calendar: void 0
  };
}
function toViewWithLocalDates(view2) {
  view2 = assign2({}, view2);
  view2.currentStart = toLocalDate(view2.currentStart);
  view2.currentEnd = toLocalDate(view2.currentEnd);
  view2.activeStart = toLocalDate(view2.activeStart);
  view2.activeEnd = toLocalDate(view2.activeEnd);
  return view2;
}
var display = ["background"];
var eventId = 1;
function createEvents(input) {
  return input.map((event) => ({
    id: "id" in event ? String(event.id) : `{generated-${eventId++}}`,
    resourceIds: Array.isArray(event.resourceIds) ? event.resourceIds.map(String) : "resourceId" in event ? [String(event.resourceId)] : [],
    allDay: event.allDay || false,
    start: createDate(event.start),
    end: createDate(event.end),
    title: event.title || "",
    titleHTML: event.titleHTML || "",
    editable: event.editable,
    startEditable: event.startEditable,
    durationEditable: event.durationEditable,
    display: display.includes(event.display) ? event.display : "auto",
    extendedProps: event.extendedProps || {},
    backgroundColor: event.backgroundColor || event.color
  }));
}
function createEventSources(input) {
  return input.map((source) => ({
    events: source.events,
    url: source.url && source.url.trimEnd("&") || "",
    method: source.method && source.method.toUpperCase() || "GET",
    extraParams: source.extraParams || {}
  }));
}
function createEventChunk(event, start, end) {
  return {
    start: event.start > start ? event.start : start,
    end: event.end < end ? event.end : end,
    event
  };
}
function sortEventChunks(chunks) {
  chunks.sort((a, b) => {
    if (a.start < b.start) {
      return -1;
    }
    if (a.start > b.start) {
      return 1;
    }
    return 0;
  });
}
function prepareEventChunks(chunks, hiddenDays) {
  let longChunks = {};
  if (chunks.length) {
    sortEventChunks(chunks);
    let prevChunk;
    for (let chunk of chunks) {
      let dates = [];
      let date = setMidnight(cloneDate(chunk.start));
      while (chunk.end > date) {
        if (!hiddenDays.includes(date.getUTCDay())) {
          dates.push(cloneDate(date));
          if (dates.length > 1) {
            let key = date.getTime();
            if (longChunks[key]) {
              longChunks[key].push(chunk);
            } else {
              longChunks[key] = [chunk];
            }
          }
        }
        addDay(date);
      }
      if (dates.length) {
        chunk.date = dates[0];
        chunk.days = dates.length;
        chunk.dates = dates;
        if (chunk.start < dates[0]) {
          chunk.start = dates[0];
        }
        if (setMidnight(cloneDate(chunk.end)) > dates[dates.length - 1]) {
          chunk.end = dates[dates.length - 1];
        }
      } else {
        chunk.date = setMidnight(cloneDate(chunk.start));
        chunk.days = 1;
        chunk.dates = [chunk.date];
      }
      if (prevChunk && datesEqual(prevChunk.date, chunk.date)) {
        chunk.prev = prevChunk;
      }
      prevChunk = chunk;
    }
  }
  return longChunks;
}
function createEventContent(chunk, displayEventEnd, eventContent, theme, _intlEventTime, _view) {
  let timeText = _intlEventTime.format(chunk.start), content;
  if (displayEventEnd && chunk.event.display !== "pointer") {
    timeText += ` - ${_intlEventTime.format(chunk.end)}`;
  }
  if (eventContent) {
    content = is_function(eventContent) ? eventContent({
      event: toEventWithLocalDates(chunk.event),
      timeText,
      view: toViewWithLocalDates(_view)
    }) : eventContent;
  } else {
    switch (chunk.event.display) {
      case "background":
        content = "";
        break;
      case "pointer":
        content = {
          domNodes: [createElement("div", theme.eventTime, null, timeText)]
        };
        break;
      default:
        content = {
          domNodes: [
            createElement("div", theme.eventTime, null, timeText),
            createElement("div", theme.eventTitle, chunk.event.titleHTML, chunk.event.title)
          ]
        };
    }
  }
  return [timeText, content];
}
function toEventWithLocalDates(event) {
  return _cloneEvent(event, toLocalDate);
}
function _cloneEvent(event, dateFn) {
  event = assign2({}, event);
  event.start = dateFn(event.start);
  event.end = dateFn(event.end);
  return event;
}
function writable2(value, parser, start) {
  return {
    ...writable(parser ? parser(value) : value, start),
    parse: parser
  };
}
function derived2(stores, fn, initValue) {
  let storeValue = initValue;
  let hasSubscribers = false;
  let auto = fn.length < 2;
  let fn2 = (_, set) => {
    hasSubscribers = true;
    if (auto) {
      storeValue = fn(_, set);
      set(storeValue);
    } else {
      fn(_, (value) => {
        storeValue = value;
        set(value);
      });
    }
    return () => {
      hasSubscribers = false;
    };
  };
  let store = derived(stores, fn2, storeValue);
  return {
    ...store,
    get: () => hasSubscribers ? storeValue : get_store_value(store)
  };
}
function intl(locale, format) {
  return derived([locale, format], ([$locale, $format]) => {
    let intl2 = is_function($format) ? { format: $format } : new Intl.DateTimeFormat($locale, $format);
    return {
      format: (date) => intl2.format(toLocalDate(date))
    };
  });
}
function intlRange(locale, format) {
  return derived([locale, format], ([$locale, $format]) => {
    if (is_function($format)) {
      return { format: (start, end) => $format(toLocalDate(start), toLocalDate(end)) };
    }
    let intl2 = new Intl.DateTimeFormat($locale, $format);
    return {
      format: (start, end) => formatRange(toLocalDate(start), toLocalDate(end), intl2)
    };
  });
}

// node_modules/@event-calendar/core/index.js
function createOptions(plugins) {
  let options = {
    allDayContent: void 0,
    allDaySlot: true,
    buttonText: {
      today: "today"
    },
    date: new Date(),
    dateClick: void 0,
    datesSet: void 0,
    dayHeaderFormat: {
      weekday: "short",
      month: "numeric",
      day: "numeric"
    },
    displayEventEnd: true,
    duration: { weeks: 1 },
    events: [],
    eventBackgroundColor: void 0,
    eventClick: void 0,
    eventColor: void 0,
    eventContent: void 0,
    eventDidMount: void 0,
    eventMouseEnter: void 0,
    eventMouseLeave: void 0,
    eventSources: [],
    eventTimeFormat: {
      hour: "numeric",
      minute: "2-digit"
    },
    firstDay: 0,
    flexibleSlotTimeLimits: false,
    headerToolbar: {
      start: "title",
      center: "",
      end: "today prev,next"
    },
    height: "auto",
    hiddenDays: [],
    highlightedDates: [],
    lazyFetching: true,
    loading: void 0,
    locale: void 0,
    monthMode: false,
    nowIndicator: false,
    selectable: false,
    scrollTime: "06:00:00",
    slotDuration: "00:30:00",
    slotHeight: 24,
    slotLabelFormat: {
      hour: "numeric",
      minute: "2-digit"
    },
    slotMaxTime: "24:00:00",
    slotMinTime: "00:00:00",
    theme: {
      allDay: "ec-all-day",
      active: "ec-active",
      bgEvent: "ec-bg-event",
      bgEvents: "ec-bg-events",
      body: "ec-body",
      button: "ec-button",
      buttonGroup: "ec-button-group",
      calendar: "ec",
      compact: "ec-compact",
      content: "ec-content",
      day: "ec-day",
      dayHead: "ec-day-head",
      days: "ec-days",
      event: "ec-event",
      eventBody: "ec-event-body",
      eventTime: "ec-event-time",
      eventTitle: "ec-event-title",
      events: "ec-events",
      extra: "ec-extra",
      handle: "ec-handle",
      header: "ec-header",
      hiddenScroll: "ec-hidden-scroll",
      hiddenTimes: "ec-hidden-times",
      highlight: "ec-highlight",
      icon: "ec-icon",
      line: "ec-line",
      lines: "ec-lines",
      nowIndicator: "ec-now-indicator",
      otherMonth: "ec-other-month",
      sidebar: "ec-sidebar",
      sidebarTitle: "ec-sidebar-title",
      today: "ec-today",
      time: "ec-time",
      title: "ec-title",
      toolbar: "ec-toolbar",
      week: "ec-week",
      withScroll: "ec-with-scroll"
    },
    titleFormat: {
      year: "numeric",
      month: "short",
      day: "numeric"
    },
    view: void 0,
    viewDidMount: void 0,
    views: {}
  };
  for (let plugin of plugins) {
    if ("createOptions" in plugin) {
      plugin.createOptions(options);
    }
  }
  return options;
}
function createParsers(options, plugins) {
  let parsers = {
    buttonText: (input) => is_function(input) ? input(options.buttonText) : input,
    date: (date) => setMidnight(createDate(date)),
    duration: createDuration,
    events: createEvents,
    eventSources: createEventSources,
    hiddenDays: (days2) => [...new Set(days2)],
    highlightedDates: (dates) => dates.map(createDate),
    scrollTime: createDuration,
    slotDuration: createDuration,
    slotMaxTime: createDuration,
    slotMinTime: createDuration,
    theme: (input) => is_function(input) ? input(options.theme) : input
  };
  for (let plugin of plugins) {
    if ("createParsers" in plugin) {
      plugin.createParsers(parsers, options);
    }
  }
  return parsers;
}
var prev;
function diff(options) {
  let diff2 = [];
  if (prev) {
    for (let name of Object.keys(options)) {
      if (options[name] !== prev[name]) {
        diff2.push([name, options[name]]);
      }
    }
  }
  prev = assign2({}, options);
  return diff2;
}
function activeRange(state) {
  let _activeRange = derived([state._currentRange, state.firstDay, state.monthMode, state.slotMinTime, state.slotMaxTime], ([$_currentRange, $firstDay, $monthMode, $slotMinTime, $slotMaxTime]) => {
    let start = cloneDate($_currentRange.start);
    let end = cloneDate($_currentRange.end);
    if ($monthMode) {
      prevClosestDay(start, $firstDay);
      nextClosestDay(end, $firstDay);
    } else if ($slotMaxTime.days || $slotMaxTime.seconds > DAY_IN_SECONDS) {
      addDuration(subtractDay(end), $slotMaxTime);
      let start2 = subtractDay(cloneDate(end));
      if (start2 < start) {
        start = start2;
      }
    }
    return { start, end };
  });
  let debounce = 0;
  derived([_activeRange, state.datesSet], ([$_activeRange, $datesSet]) => {
    if ($datesSet && !debounce) {
      ++debounce;
      tick().then(() => {
        --debounce;
        $datesSet({
          start: toLocalDate($_activeRange.start),
          end: toLocalDate($_activeRange.end),
          startStr: toISOString($_activeRange.start),
          endStr: toISOString($_activeRange.end)
        });
      });
    }
  }).subscribe(noop);
  return _activeRange;
}
function currentRange(state) {
  return derived([state.date, state.duration, state.monthMode, state.firstDay], ([$date, $duration, $monthMode, $firstDay]) => {
    let start = cloneDate($date), end;
    if ($monthMode) {
      start.setUTCDate(1);
    } else if ($duration.inWeeks) {
      prevClosestDay(start, $firstDay);
    }
    end = addDuration(cloneDate(start), $duration);
    return { start, end };
  });
}
function viewDates(state) {
  return derived2([state._activeRange, state.hiddenDays], ([$_activeRange, $hiddenDays]) => {
    let dates = [];
    let date = setMidnight(cloneDate($_activeRange.start));
    let end = setMidnight(cloneDate($_activeRange.end));
    while (date < end) {
      if (!$hiddenDays.includes(date.getUTCDay())) {
        dates.push(cloneDate(date));
      }
      addDay(date);
    }
    if (!dates.length && $hiddenDays.length && $hiddenDays.length < 7) {
      state.date.update((date2) => {
        while ($hiddenDays.includes(date2.getUTCDay())) {
          addDay(date2);
        }
        return date2;
      });
      dates = state._viewDates.get();
    }
    return dates;
  });
}
function viewTitle(state) {
  return derived([state.date, state._activeRange, state._titleIntlRange, state.monthMode], ([$date, $_activeRange, $_titleIntlRange, $monthMode]) => {
    return $monthMode ? $_titleIntlRange.format($date, $date) : $_titleIntlRange.format($_activeRange.start, subtractDay(cloneDate($_activeRange.end)));
  });
}
function view(state) {
  return derived2([state.view, state._viewTitle, state._currentRange, state._activeRange], (args) => createView(...args));
}
function events(state) {
  let _events = writable([]);
  let abortController;
  let fetching = 0;
  derived([state.events, state.eventSources, state._activeRange, state._fetchedRange, state.lazyFetching, state.loading], (values, set) => tick().then(() => {
    let [$events, $eventSources, $_activeRange, $_fetchedRange, $lazyFetching, $loading] = values;
    if (!$eventSources.length) {
      set($events);
      return;
    }
    if (!$_fetchedRange.start || $_fetchedRange.start > $_activeRange.start || $_fetchedRange.end < $_activeRange.end || !$lazyFetching) {
      if (abortController) {
        abortController.abort();
      }
      abortController = new AbortController();
      if (is_function($loading) && !fetching) {
        $loading(true);
      }
      let stopLoading = () => {
        if (--fetching === 0 && is_function($loading)) {
          $loading(false);
        }
      };
      let events2 = [];
      let failure = (e) => stopLoading();
      let success = (data) => {
        events2 = events2.concat(createEvents(data));
        set(events2);
        stopLoading();
      };
      let startStr = toISOString($_activeRange.start);
      let endStr = toISOString($_activeRange.end);
      for (let source of $eventSources) {
        if (is_function(source.events)) {
          let result = source.events({
            start: toLocalDate($_activeRange.start),
            end: toLocalDate($_activeRange.end),
            startStr,
            endStr
          }, success, failure);
          if (result !== void 0) {
            Promise.resolve(result).then(success, failure);
          }
        } else {
          let params = is_function(source.extraParams) ? source.extraParams() : assign2({}, source.extraParams);
          params.start = startStr;
          params.end = endStr;
          params = new URLSearchParams(params);
          let url = source.url, headers = {}, body;
          if (["GET", "HEAD"].includes(source.method)) {
            url += (url.includes("?") ? "&" : "?") + params;
          } else {
            headers["content-type"] = "application/x-www-form-urlencoded;charset=UTF-8";
            body = String(params);
          }
          fetch(url, { method: source.method, headers, body, signal: abortController.signal, credentials: "same-origin" }).then((response) => response.json()).then(success).catch(failure);
        }
        ++fetching;
      }
      $_fetchedRange.start = $_activeRange.start;
      $_fetchedRange.end = $_activeRange.end;
    }
  }), []).subscribe(_events.set);
  return _events;
}
function now() {
  return readable(createDate(), (set) => {
    let interval = setInterval(() => {
      set(createDate());
    }, 1e3);
    return () => clearInterval(interval);
  });
}
function today(state) {
  return derived(state._now, ($_now) => setMidnight(cloneDate($_now)));
}
var State = class {
  constructor(plugins, input) {
    plugins = plugins || [];
    let options = createOptions(plugins);
    let parsers = createParsers(options, plugins);
    for (let [option, value] of Object.entries(options)) {
      this[option] = writable2(value, parsers[option]);
    }
    this._currentRange = currentRange(this);
    this._activeRange = activeRange(this);
    this._fetchedRange = writable({ start: void 0, end: void 0 });
    this._events = events(this);
    this._now = now();
    this._today = today(this);
    this._ignoreClick = writable(false);
    this._intlEventTime = intl(this.locale, this.eventTimeFormat);
    this._intlSlotLabel = intl(this.locale, this.slotLabelFormat);
    this._intlDayHeader = intl(this.locale, this.dayHeaderFormat);
    this._titleIntlRange = intlRange(this.locale, this.titleFormat);
    this._scrollable = writable(false);
    this._viewTitle = viewTitle(this);
    this._viewDates = viewDates(this);
    this._view = view(this);
    this._viewComponent = writable(void 0);
    this._viewClass = writable(void 0);
    this._interaction = writable({});
    this._iEvents = writable([null, null]);
    this._draggable = writable(noop);
    this._resizable = writable(noop);
    this._classes = writable(identity);
    this._iClass = writable(void 0);
    this._scroll = writable(void 0);
    for (let plugin of plugins) {
      if ("createStores" in plugin) {
        plugin.createStores(this);
      }
    }
    if (input.view) {
      this.view.set(input.view);
    }
    let commonOpts = assign2({}, options, input);
    parseOpts(commonOpts, this);
    let views = /* @__PURE__ */ new Set([...Object.keys(options.views), ...Object.keys(input.views || {})]);
    for (let view2 of views) {
      let viewOpts = assign2({}, options.views[view2] || {}, input.views && input.views[view2] || {});
      parseOpts(viewOpts, this);
      let opts = assign2({}, commonOpts, viewOpts);
      this.view.subscribe((newView) => {
        if (newView === view2) {
          this._viewComponent.set(opts.component);
          if (is_function(opts.viewDidMount)) {
            tick().then(() => opts.viewDidMount(this._view.get()));
          }
        }
      });
      for (let key of Object.keys(opts)) {
        if (this.hasOwnProperty(key) && key[0] !== "_") {
          let { set, _set, ...rest } = this[key];
          if (!_set) {
            _set = set;
          }
          this[key] = {
            set: (value) => {
              opts[key] = value;
              set(value);
            },
            _set,
            ...rest
          };
          this.view.subscribe((newView) => {
            if (newView === view2) {
              _set(opts[key]);
            }
          });
        }
      }
    }
  }
};
function parseOpts(opts, state) {
  for (let key of Object.keys(opts)) {
    if (state.hasOwnProperty(key) && key[0] !== "_") {
      if (state[key].parse) {
        opts[key] = state[key].parse(opts[key]);
      }
    }
  }
}
function get_each_context$1(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[23] = list[i];
  return child_ctx;
}
function create_else_block$1(ctx) {
  let button;
  let t_value = ctx[5][ctx[23]] + "";
  let t;
  let button_class_value;
  let mounted;
  let dispose;
  function click_handler_1() {
    return ctx[20](ctx[23]);
  }
  return {
    c() {
      button = element("button");
      t = text(t_value);
      attr(button, "class", button_class_value = ctx[3].button + (ctx[6] === ctx[23] ? " " + ctx[3].active : "") + " ec-" + ctx[23]);
    },
    m(target, anchor) {
      insert(target, button, anchor);
      append(button, t);
      if (!mounted) {
        dispose = listen(button, "click", click_handler_1);
        mounted = true;
      }
    },
    p(new_ctx, dirty) {
      ctx = new_ctx;
      if (dirty & 33 && t_value !== (t_value = ctx[5][ctx[23]] + ""))
        set_data(t, t_value);
      if (dirty & 73 && button_class_value !== (button_class_value = ctx[3].button + (ctx[6] === ctx[23] ? " " + ctx[3].active : "") + " ec-" + ctx[23])) {
        attr(button, "class", button_class_value);
      }
    },
    d(detaching) {
      if (detaching)
        detach(button);
      mounted = false;
      dispose();
    }
  };
}
function create_if_block_4(ctx) {
  let button;
  let t_value = ctx[5][ctx[23]] + "";
  let t;
  let button_class_value;
  let mounted;
  let dispose;
  return {
    c() {
      button = element("button");
      t = text(t_value);
      attr(button, "class", button_class_value = ctx[3].button + " ec-" + ctx[23]);
      button.disabled = ctx[1];
    },
    m(target, anchor) {
      insert(target, button, anchor);
      append(button, t);
      if (!mounted) {
        dispose = listen(button, "click", ctx[19]);
        mounted = true;
      }
    },
    p(ctx2, dirty) {
      if (dirty & 33 && t_value !== (t_value = ctx2[5][ctx2[23]] + ""))
        set_data(t, t_value);
      if (dirty & 9 && button_class_value !== (button_class_value = ctx2[3].button + " ec-" + ctx2[23])) {
        attr(button, "class", button_class_value);
      }
      if (dirty & 2) {
        button.disabled = ctx2[1];
      }
    },
    d(detaching) {
      if (detaching)
        detach(button);
      mounted = false;
      dispose();
    }
  };
}
function create_if_block_3(ctx) {
  let button;
  let i;
  let i_class_value;
  let button_class_value;
  let mounted;
  let dispose;
  return {
    c() {
      button = element("button");
      i = element("i");
      attr(i, "class", i_class_value = ctx[3].icon + " ec-" + ctx[23]);
      attr(button, "class", button_class_value = ctx[3].button + " ec-" + ctx[23]);
    },
    m(target, anchor) {
      insert(target, button, anchor);
      append(button, i);
      if (!mounted) {
        dispose = listen(button, "click", ctx[17]);
        mounted = true;
      }
    },
    p(ctx2, dirty) {
      if (dirty & 9 && i_class_value !== (i_class_value = ctx2[3].icon + " ec-" + ctx2[23])) {
        attr(i, "class", i_class_value);
      }
      if (dirty & 9 && button_class_value !== (button_class_value = ctx2[3].button + " ec-" + ctx2[23])) {
        attr(button, "class", button_class_value);
      }
    },
    d(detaching) {
      if (detaching)
        detach(button);
      mounted = false;
      dispose();
    }
  };
}
function create_if_block_2(ctx) {
  let button;
  let i;
  let i_class_value;
  let button_class_value;
  let mounted;
  let dispose;
  return {
    c() {
      button = element("button");
      i = element("i");
      attr(i, "class", i_class_value = ctx[3].icon + " ec-" + ctx[23]);
      attr(button, "class", button_class_value = ctx[3].button + " ec-" + ctx[23]);
    },
    m(target, anchor) {
      insert(target, button, anchor);
      append(button, i);
      if (!mounted) {
        dispose = listen(button, "click", ctx[16]);
        mounted = true;
      }
    },
    p(ctx2, dirty) {
      if (dirty & 9 && i_class_value !== (i_class_value = ctx2[3].icon + " ec-" + ctx2[23])) {
        attr(i, "class", i_class_value);
      }
      if (dirty & 9 && button_class_value !== (button_class_value = ctx2[3].button + " ec-" + ctx2[23])) {
        attr(button, "class", button_class_value);
      }
    },
    d(detaching) {
      if (detaching)
        detach(button);
      mounted = false;
      dispose();
    }
  };
}
function create_if_block_1(ctx) {
  let h2;
  let t;
  let h2_class_value;
  return {
    c() {
      h2 = element("h2");
      t = text(ctx[4]);
      attr(h2, "class", h2_class_value = ctx[3].title);
    },
    m(target, anchor) {
      insert(target, h2, anchor);
      append(h2, t);
    },
    p(ctx2, dirty) {
      if (dirty & 16)
        set_data(t, ctx2[4]);
      if (dirty & 8 && h2_class_value !== (h2_class_value = ctx2[3].title)) {
        attr(h2, "class", h2_class_value);
      }
    },
    d(detaching) {
      if (detaching)
        detach(h2);
    }
  };
}
function create_if_block$1(ctx) {
  return { c: noop, m: noop, p: noop, d: noop };
}
function create_each_block$1(ctx) {
  let if_block_anchor;
  function select_block_type(ctx2, dirty) {
    if (ctx2[23] == "")
      return create_if_block$1;
    if (ctx2[23] == "title")
      return create_if_block_1;
    if (ctx2[23] == "prev")
      return create_if_block_2;
    if (ctx2[23] === "next")
      return create_if_block_3;
    if (ctx2[23] === "today")
      return create_if_block_4;
    return create_else_block$1;
  }
  let current_block_type = select_block_type(ctx);
  let if_block = current_block_type(ctx);
  return {
    c() {
      if_block.c();
      if_block_anchor = empty();
    },
    m(target, anchor) {
      if_block.m(target, anchor);
      insert(target, if_block_anchor, anchor);
    },
    p(ctx2, dirty) {
      if (current_block_type === (current_block_type = select_block_type(ctx2)) && if_block) {
        if_block.p(ctx2, dirty);
      } else {
        if_block.d(1);
        if_block = current_block_type(ctx2);
        if (if_block) {
          if_block.c();
          if_block.m(if_block_anchor.parentNode, if_block_anchor);
        }
      }
    },
    d(detaching) {
      if_block.d(detaching);
      if (detaching)
        detach(if_block_anchor);
    }
  };
}
function create_fragment$2(ctx) {
  let each_1_anchor;
  let each_value = ctx[0];
  let each_blocks = [];
  for (let i = 0; i < each_value.length; i += 1) {
    each_blocks[i] = create_each_block$1(get_each_context$1(ctx, each_value, i));
  }
  return {
    c() {
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      each_1_anchor = empty();
    },
    m(target, anchor) {
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(target, anchor);
      }
      insert(target, each_1_anchor, anchor);
    },
    p(ctx2, [dirty]) {
      if (dirty & 229503) {
        each_value = ctx2[0];
        let i;
        for (i = 0; i < each_value.length; i += 1) {
          const child_ctx = get_each_context$1(ctx2, each_value, i);
          if (each_blocks[i]) {
            each_blocks[i].p(child_ctx, dirty);
          } else {
            each_blocks[i] = create_each_block$1(child_ctx);
            each_blocks[i].c();
            each_blocks[i].m(each_1_anchor.parentNode, each_1_anchor);
          }
        }
        for (; i < each_blocks.length; i += 1) {
          each_blocks[i].d(1);
        }
        each_blocks.length = each_value.length;
      }
    },
    i: noop,
    o: noop,
    d(detaching) {
      destroy_each(each_blocks, detaching);
      if (detaching)
        detach(each_1_anchor);
    }
  };
}
function instance$2($$self, $$props, $$invalidate) {
  let $duration;
  let $date;
  let $hiddenDays;
  let $_currentRange;
  let $theme;
  let $_viewTitle;
  let $buttonText;
  let $view;
  let { buttons } = $$props;
  let { _currentRange, _viewTitle, buttonText, date, duration, hiddenDays, theme, view: view2 } = getContext("state");
  component_subscribe($$self, _currentRange, (value) => $$invalidate(18, $_currentRange = value));
  component_subscribe($$self, _viewTitle, (value) => $$invalidate(4, $_viewTitle = value));
  component_subscribe($$self, buttonText, (value) => $$invalidate(5, $buttonText = value));
  component_subscribe($$self, date, (value) => $$invalidate(2, $date = value));
  component_subscribe($$self, duration, (value) => $$invalidate(21, $duration = value));
  component_subscribe($$self, hiddenDays, (value) => $$invalidate(22, $hiddenDays = value));
  component_subscribe($$self, theme, (value) => $$invalidate(3, $theme = value));
  component_subscribe($$self, view2, (value) => $$invalidate(6, $view = value));
  let today2 = setMidnight(createDate()), isToday;
  function prev2() {
    let d = subtractDuration($date, $duration);
    if ($hiddenDays.length && $hiddenDays.length < 7) {
      while ($hiddenDays.includes(d.getUTCDay())) {
        subtractDay(d);
      }
    }
    set_store_value(date, $date = d, $date);
  }
  function next() {
    set_store_value(date, $date = addDuration($date, $duration), $date);
  }
  const click_handler = () => set_store_value(date, $date = cloneDate(today2), $date);
  const click_handler_1 = (button) => set_store_value(view2, $view = button, $view);
  $$self.$$set = ($$props2) => {
    if ("buttons" in $$props2)
      $$invalidate(0, buttons = $$props2.buttons);
  };
  $$self.$$.update = () => {
    if ($$self.$$.dirty & 262144) {
      $$invalidate(1, isToday = today2 >= $_currentRange.start && today2 < $_currentRange.end || null);
    }
  };
  return [
    buttons,
    isToday,
    $date,
    $theme,
    $_viewTitle,
    $buttonText,
    $view,
    _currentRange,
    _viewTitle,
    buttonText,
    date,
    duration,
    hiddenDays,
    theme,
    view2,
    today2,
    prev2,
    next,
    $_currentRange,
    click_handler,
    click_handler_1
  ];
}
var Buttons = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance$2, create_fragment$2, safe_not_equal, { buttons: 0 });
  }
};
function get_each_context(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[5] = list[i];
  return child_ctx;
}
function get_each_context_1(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[8] = list[i];
  return child_ctx;
}
function create_else_block(ctx) {
  let buttons;
  let current;
  buttons = new Buttons({ props: { buttons: ctx[8] } });
  return {
    c() {
      create_component(buttons.$$.fragment);
    },
    m(target, anchor) {
      mount_component(buttons, target, anchor);
      current = true;
    },
    p(ctx2, dirty) {
      const buttons_changes = {};
      if (dirty & 1)
        buttons_changes.buttons = ctx2[8];
      buttons.$set(buttons_changes);
    },
    i(local) {
      if (current)
        return;
      transition_in(buttons.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(buttons.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      destroy_component(buttons, detaching);
    }
  };
}
function create_if_block(ctx) {
  let div;
  let buttons;
  let div_class_value;
  let current;
  buttons = new Buttons({ props: { buttons: ctx[8] } });
  return {
    c() {
      div = element("div");
      create_component(buttons.$$.fragment);
      attr(div, "class", div_class_value = ctx[1].buttonGroup);
    },
    m(target, anchor) {
      insert(target, div, anchor);
      mount_component(buttons, div, null);
      current = true;
    },
    p(ctx2, dirty) {
      const buttons_changes = {};
      if (dirty & 1)
        buttons_changes.buttons = ctx2[8];
      buttons.$set(buttons_changes);
      if (!current || dirty & 2 && div_class_value !== (div_class_value = ctx2[1].buttonGroup)) {
        attr(div, "class", div_class_value);
      }
    },
    i(local) {
      if (current)
        return;
      transition_in(buttons.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(buttons.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div);
      destroy_component(buttons);
    }
  };
}
function create_each_block_1(ctx) {
  let current_block_type_index;
  let if_block;
  let if_block_anchor;
  let current;
  const if_block_creators = [create_if_block, create_else_block];
  const if_blocks = [];
  function select_block_type(ctx2, dirty) {
    if (ctx2[8].length > 1)
      return 0;
    return 1;
  }
  current_block_type_index = select_block_type(ctx);
  if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx);
  return {
    c() {
      if_block.c();
      if_block_anchor = empty();
    },
    m(target, anchor) {
      if_blocks[current_block_type_index].m(target, anchor);
      insert(target, if_block_anchor, anchor);
      current = true;
    },
    p(ctx2, dirty) {
      let previous_block_index = current_block_type_index;
      current_block_type_index = select_block_type(ctx2);
      if (current_block_type_index === previous_block_index) {
        if_blocks[current_block_type_index].p(ctx2, dirty);
      } else {
        group_outros();
        transition_out(if_blocks[previous_block_index], 1, 1, () => {
          if_blocks[previous_block_index] = null;
        });
        check_outros();
        if_block = if_blocks[current_block_type_index];
        if (!if_block) {
          if_block = if_blocks[current_block_type_index] = if_block_creators[current_block_type_index](ctx2);
          if_block.c();
        } else {
          if_block.p(ctx2, dirty);
        }
        transition_in(if_block, 1);
        if_block.m(if_block_anchor.parentNode, if_block_anchor);
      }
    },
    i(local) {
      if (current)
        return;
      transition_in(if_block);
      current = true;
    },
    o(local) {
      transition_out(if_block);
      current = false;
    },
    d(detaching) {
      if_blocks[current_block_type_index].d(detaching);
      if (detaching)
        detach(if_block_anchor);
    }
  };
}
function create_each_block(ctx) {
  let div;
  let t;
  let current;
  let each_value_1 = ctx[0][ctx[5]];
  let each_blocks = [];
  for (let i = 0; i < each_value_1.length; i += 1) {
    each_blocks[i] = create_each_block_1(get_each_context_1(ctx, each_value_1, i));
  }
  const out = (i) => transition_out(each_blocks[i], 1, 1, () => {
    each_blocks[i] = null;
  });
  return {
    c() {
      div = element("div");
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      t = space();
    },
    m(target, anchor) {
      insert(target, div, anchor);
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(div, null);
      }
      append(div, t);
      current = true;
    },
    p(ctx2, dirty) {
      if (dirty & 3) {
        each_value_1 = ctx2[0][ctx2[5]];
        let i;
        for (i = 0; i < each_value_1.length; i += 1) {
          const child_ctx = get_each_context_1(ctx2, each_value_1, i);
          if (each_blocks[i]) {
            each_blocks[i].p(child_ctx, dirty);
            transition_in(each_blocks[i], 1);
          } else {
            each_blocks[i] = create_each_block_1(child_ctx);
            each_blocks[i].c();
            transition_in(each_blocks[i], 1);
            each_blocks[i].m(div, t);
          }
        }
        group_outros();
        for (i = each_value_1.length; i < each_blocks.length; i += 1) {
          out(i);
        }
        check_outros();
      }
    },
    i(local) {
      if (current)
        return;
      for (let i = 0; i < each_value_1.length; i += 1) {
        transition_in(each_blocks[i]);
      }
      current = true;
    },
    o(local) {
      each_blocks = each_blocks.filter(Boolean);
      for (let i = 0; i < each_blocks.length; i += 1) {
        transition_out(each_blocks[i]);
      }
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div);
      destroy_each(each_blocks, detaching);
    }
  };
}
function create_fragment$1(ctx) {
  let div;
  let div_class_value;
  let current;
  let each_value = Object.keys(ctx[0]);
  let each_blocks = [];
  for (let i = 0; i < each_value.length; i += 1) {
    each_blocks[i] = create_each_block(get_each_context(ctx, each_value, i));
  }
  const out = (i) => transition_out(each_blocks[i], 1, 1, () => {
    each_blocks[i] = null;
  });
  return {
    c() {
      div = element("div");
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      attr(div, "class", div_class_value = ctx[1].toolbar);
    },
    m(target, anchor) {
      insert(target, div, anchor);
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(div, null);
      }
      current = true;
    },
    p(ctx2, [dirty]) {
      if (dirty & 3) {
        each_value = Object.keys(ctx2[0]);
        let i;
        for (i = 0; i < each_value.length; i += 1) {
          const child_ctx = get_each_context(ctx2, each_value, i);
          if (each_blocks[i]) {
            each_blocks[i].p(child_ctx, dirty);
            transition_in(each_blocks[i], 1);
          } else {
            each_blocks[i] = create_each_block(child_ctx);
            each_blocks[i].c();
            transition_in(each_blocks[i], 1);
            each_blocks[i].m(div, null);
          }
        }
        group_outros();
        for (i = each_value.length; i < each_blocks.length; i += 1) {
          out(i);
        }
        check_outros();
      }
      if (!current || dirty & 2 && div_class_value !== (div_class_value = ctx2[1].toolbar)) {
        attr(div, "class", div_class_value);
      }
    },
    i(local) {
      if (current)
        return;
      for (let i = 0; i < each_value.length; i += 1) {
        transition_in(each_blocks[i]);
      }
      current = true;
    },
    o(local) {
      each_blocks = each_blocks.filter(Boolean);
      for (let i = 0; i < each_blocks.length; i += 1) {
        transition_out(each_blocks[i]);
      }
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div);
      destroy_each(each_blocks, detaching);
    }
  };
}
function instance$1($$self, $$props, $$invalidate) {
  let $headerToolbar;
  let $theme;
  let { headerToolbar, theme } = getContext("state");
  component_subscribe($$self, headerToolbar, (value) => $$invalidate(4, $headerToolbar = value));
  component_subscribe($$self, theme, (value) => $$invalidate(1, $theme = value));
  let sections = { start: [], center: [], end: [] };
  $$self.$$.update = () => {
    if ($$self.$$.dirty & 17) {
      {
        for (let key of Object.keys(sections)) {
          $$invalidate(0, sections[key] = $headerToolbar[key].split(" ").map((group) => group.split(",")), sections);
        }
      }
    }
  };
  return [sections, $theme, headerToolbar, theme, $headerToolbar];
}
var Toolbar = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance$1, create_fragment$1, safe_not_equal, {});
  }
};
function create_fragment(ctx) {
  let div;
  let toolbar;
  let t0;
  let switch_instance0;
  let t1;
  let switch_instance1;
  let div_class_value;
  let current;
  let mounted;
  let dispose;
  toolbar = new Toolbar({});
  var switch_value = ctx[5];
  function switch_props(ctx2) {
    return {};
  }
  if (switch_value) {
    switch_instance0 = construct_svelte_component(switch_value, switch_props());
  }
  var switch_value_1 = ctx[0].component;
  function switch_props_1(ctx2) {
    return {};
  }
  if (switch_value_1) {
    switch_instance1 = construct_svelte_component(switch_value_1, switch_props_1());
  }
  return {
    c() {
      div = element("div");
      create_component(toolbar.$$.fragment);
      t0 = space();
      if (switch_instance0)
        create_component(switch_instance0.$$.fragment);
      t1 = space();
      if (switch_instance1)
        create_component(switch_instance1.$$.fragment);
      attr(div, "class", div_class_value = ctx[1].calendar + (ctx[2] ? " " + ctx[1][ctx[2]] : "") + (ctx[3] ? " " + ctx[1][ctx[3]] : ""));
      set_style(div, "height", ctx[4]);
    },
    m(target, anchor) {
      insert(target, div, anchor);
      mount_component(toolbar, div, null);
      append(div, t0);
      if (switch_instance0)
        mount_component(switch_instance0, div, null);
      append(div, t1);
      if (switch_instance1)
        mount_component(switch_instance1, div, null);
      current = true;
      if (!mounted) {
        dispose = listen(window, "click", ctx[16], true);
        mounted = true;
      }
    },
    p(ctx2, dirty) {
      if (switch_value !== (switch_value = ctx2[5])) {
        if (switch_instance0) {
          group_outros();
          const old_component = switch_instance0;
          transition_out(old_component.$$.fragment, 1, 0, () => {
            destroy_component(old_component, 1);
          });
          check_outros();
        }
        if (switch_value) {
          switch_instance0 = construct_svelte_component(switch_value, switch_props());
          create_component(switch_instance0.$$.fragment);
          transition_in(switch_instance0.$$.fragment, 1);
          mount_component(switch_instance0, div, t1);
        } else {
          switch_instance0 = null;
        }
      }
      if (switch_value_1 !== (switch_value_1 = ctx2[0].component)) {
        if (switch_instance1) {
          group_outros();
          const old_component = switch_instance1;
          transition_out(old_component.$$.fragment, 1, 0, () => {
            destroy_component(old_component, 1);
          });
          check_outros();
        }
        if (switch_value_1) {
          switch_instance1 = construct_svelte_component(switch_value_1, switch_props_1());
          create_component(switch_instance1.$$.fragment);
          transition_in(switch_instance1.$$.fragment, 1);
          mount_component(switch_instance1, div, null);
        } else {
          switch_instance1 = null;
        }
      }
      if (!current || dirty[0] & 14 && div_class_value !== (div_class_value = ctx2[1].calendar + (ctx2[2] ? " " + ctx2[1][ctx2[2]] : "") + (ctx2[3] ? " " + ctx2[1][ctx2[3]] : ""))) {
        attr(div, "class", div_class_value);
      }
      if (!current || dirty[0] & 16) {
        set_style(div, "height", ctx2[4]);
      }
    },
    i(local) {
      if (current)
        return;
      transition_in(toolbar.$$.fragment, local);
      if (switch_instance0)
        transition_in(switch_instance0.$$.fragment, local);
      if (switch_instance1)
        transition_in(switch_instance1.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(toolbar.$$.fragment, local);
      if (switch_instance0)
        transition_out(switch_instance0.$$.fragment, local);
      if (switch_instance1)
        transition_out(switch_instance1.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div);
      destroy_component(toolbar);
      if (switch_instance0)
        destroy_component(switch_instance0);
      if (switch_instance1)
        destroy_component(switch_instance1);
      mounted = false;
      dispose();
    }
  };
}
function instance($$self, $$props, $$invalidate) {
  let $_ignoreClick;
  let $events;
  let $_events;
  let $eventSources;
  let $_interaction;
  let $theme;
  let $_viewClass;
  let $_iClass;
  let $height;
  let $_viewComponent;
  let { plugins = [] } = $$props;
  let { options = {} } = $$props;
  let state = new State(plugins, options);
  setContext("state", state);
  let { _viewComponent, _viewClass, _ignoreClick, _interaction, _iClass, _events, events: events2, eventSources, height: height2, theme } = state;
  component_subscribe($$self, _viewComponent, (value) => $$invalidate(5, $_viewComponent = value));
  component_subscribe($$self, _viewClass, (value) => $$invalidate(2, $_viewClass = value));
  component_subscribe($$self, _ignoreClick, (value) => $$invalidate(29, $_ignoreClick = value));
  component_subscribe($$self, _interaction, (value) => $$invalidate(0, $_interaction = value));
  component_subscribe($$self, _iClass, (value) => $$invalidate(3, $_iClass = value));
  component_subscribe($$self, _events, (value) => $$invalidate(31, $_events = value));
  component_subscribe($$self, events2, (value) => $$invalidate(30, $events = value));
  component_subscribe($$self, eventSources, (value) => $$invalidate(32, $eventSources = value));
  component_subscribe($$self, height2, (value) => $$invalidate(4, $height = value));
  component_subscribe($$self, theme, (value) => $$invalidate(1, $theme = value));
  function setOption(name, value) {
    if (state.hasOwnProperty(name)) {
      if (state[name].parse) {
        value = state[name].parse(value);
      }
      state[name].set(value);
    }
    return this;
  }
  function getOption(name) {
    let value = state.hasOwnProperty(name) ? get_store_value(state[name]) : void 0;
    return value instanceof Date ? toLocalDate(value) : value;
  }
  function refetchEvents() {
    state._fetchedRange.set({ start: void 0, end: void 0 });
    return this;
  }
  function getEvents() {
    return get_store_value(state._events).map(toEventWithLocalDates);
  }
  function getEventById(id) {
    for (let event of get_store_value(state._events)) {
      if (event.id == id) {
        return toEventWithLocalDates(event);
      }
    }
    return null;
  }
  function addEvent(event) {
    updateEvents((events3) => events3.concat(state.events.parse([event])));
    return this;
  }
  function updateEvent(event) {
    updateEvents((events3) => {
      for (let e of events3) {
        if (e.id == event.id) {
          assign2(e, state.events.parse([event])[0]);
          break;
        }
      }
      return events3;
    });
    return this;
  }
  function removeEventById(id) {
    updateEvents((events3) => events3.filter((event) => event.id != id));
    return this;
  }
  function getView() {
    return toViewWithLocalDates(state._view.get());
  }
  function unselect() {
    if ($_interaction.action) {
      $_interaction.action.unselect();
    }
    return this;
  }
  function updateEvents(func) {
    if ($eventSources.length) {
      set_store_value(_events, $_events = func($_events), $_events);
    } else {
      set_store_value(events2, $events = func($events), $events);
    }
  }
  function handleClick(jsEvent) {
    if ($_ignoreClick) {
      ignore(jsEvent);
      set_store_value(_ignoreClick, $_ignoreClick = false, $_ignoreClick);
    }
  }
  $$self.$$set = ($$props2) => {
    if ("plugins" in $$props2)
      $$invalidate(17, plugins = $$props2.plugins);
    if ("options" in $$props2)
      $$invalidate(18, options = $$props2.options);
  };
  $$self.$$.update = () => {
    if ($$self.$$.dirty[0] & 262144) {
      for (let [name, value] of diff(options)) {
        setOption(name, value);
      }
    }
  };
  return [
    $_interaction,
    $theme,
    $_viewClass,
    $_iClass,
    $height,
    $_viewComponent,
    _viewComponent,
    _viewClass,
    _ignoreClick,
    _interaction,
    _iClass,
    _events,
    events2,
    eventSources,
    height2,
    theme,
    handleClick,
    plugins,
    options,
    setOption,
    getOption,
    refetchEvents,
    getEvents,
    getEventById,
    addEvent,
    updateEvent,
    removeEventById,
    getView,
    unselect
  ];
}
var Calendar = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance, create_fragment, safe_not_equal, {
      plugins: 17,
      options: 18,
      setOption: 19,
      getOption: 20,
      refetchEvents: 21,
      getEvents: 22,
      getEventById: 23,
      addEvent: 24,
      updateEvent: 25,
      removeEventById: 26,
      getView: 27,
      unselect: 28
    }, null, [-1, -1]);
  }
  get setOption() {
    return this.$$.ctx[19];
  }
  get getOption() {
    return this.$$.ctx[20];
  }
  get refetchEvents() {
    return this.$$.ctx[21];
  }
  get getEvents() {
    return this.$$.ctx[22];
  }
  get getEventById() {
    return this.$$.ctx[23];
  }
  get addEvent() {
    return this.$$.ctx[24];
  }
  get updateEvent() {
    return this.$$.ctx[25];
  }
  get removeEventById() {
    return this.$$.ctx[26];
  }
  get getView() {
    return this.$$.ctx[27];
  }
  get unselect() {
    return this.$$.ctx[28];
  }
};

// node_modules/@event-calendar/day-grid/index.js
function days(state) {
  return derived([state.date, state.firstDay, state.hiddenDays], ([$date, $firstDay, $hiddenDays]) => {
    let days2 = [];
    let day = cloneDate($date);
    let max = 7;
    while (day.getUTCDay() !== $firstDay && max) {
      subtractDay(day);
      --max;
    }
    for (let i = 0; i < 7; ++i) {
      if (!$hiddenDays.includes(day.getUTCDay())) {
        days2.push(cloneDate(day));
      }
      addDay(day);
    }
    return days2;
  });
}
var State2 = class {
  constructor(state) {
    this._days = days(state);
    this._hiddenEvents = writable({});
    this._popupDate = writable(null);
    this._popupChunks = writable([]);
  }
};
function get_each_context$4(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[8] = list[i];
  return child_ctx;
}
function create_each_block$4(ctx) {
  let div;
  let t_value = ctx[3].format(ctx[8]) + "";
  let t;
  let div_class_value;
  return {
    c() {
      div = element("div");
      t = text(t_value);
      attr(div, "class", div_class_value = ctx[0].day);
    },
    m(target, anchor) {
      insert(target, div, anchor);
      append(div, t);
    },
    p(ctx2, dirty) {
      if (dirty & 12 && t_value !== (t_value = ctx2[3].format(ctx2[8]) + ""))
        set_data(t, t_value);
      if (dirty & 1 && div_class_value !== (div_class_value = ctx2[0].day)) {
        attr(div, "class", div_class_value);
      }
    },
    d(detaching) {
      if (detaching)
        detach(div);
    }
  };
}
function create_fragment$6(ctx) {
  let div2;
  let div0;
  let div0_class_value;
  let t;
  let div1;
  let div1_class_value;
  let div2_class_value;
  let each_value = ctx[2];
  let each_blocks = [];
  for (let i = 0; i < each_value.length; i += 1) {
    each_blocks[i] = create_each_block$4(get_each_context$4(ctx, each_value, i));
  }
  return {
    c() {
      div2 = element("div");
      div0 = element("div");
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      t = space();
      div1 = element("div");
      attr(div0, "class", div0_class_value = ctx[0].days);
      attr(div1, "class", div1_class_value = ctx[0].hiddenScroll);
      attr(div2, "class", div2_class_value = ctx[0].header + (ctx[1] ? " " + ctx[0].withScroll : ""));
    },
    m(target, anchor) {
      insert(target, div2, anchor);
      append(div2, div0);
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(div0, null);
      }
      append(div2, t);
      append(div2, div1);
    },
    p(ctx2, [dirty]) {
      if (dirty & 13) {
        each_value = ctx2[2];
        let i;
        for (i = 0; i < each_value.length; i += 1) {
          const child_ctx = get_each_context$4(ctx2, each_value, i);
          if (each_blocks[i]) {
            each_blocks[i].p(child_ctx, dirty);
          } else {
            each_blocks[i] = create_each_block$4(child_ctx);
            each_blocks[i].c();
            each_blocks[i].m(div0, null);
          }
        }
        for (; i < each_blocks.length; i += 1) {
          each_blocks[i].d(1);
        }
        each_blocks.length = each_value.length;
      }
      if (dirty & 1 && div0_class_value !== (div0_class_value = ctx2[0].days)) {
        attr(div0, "class", div0_class_value);
      }
      if (dirty & 1 && div1_class_value !== (div1_class_value = ctx2[0].hiddenScroll)) {
        attr(div1, "class", div1_class_value);
      }
      if (dirty & 3 && div2_class_value !== (div2_class_value = ctx2[0].header + (ctx2[1] ? " " + ctx2[0].withScroll : ""))) {
        attr(div2, "class", div2_class_value);
      }
    },
    i: noop,
    o: noop,
    d(detaching) {
      if (detaching)
        detach(div2);
      destroy_each(each_blocks, detaching);
    }
  };
}
function instance$6($$self, $$props, $$invalidate) {
  let $theme;
  let $_scrollable;
  let $_days;
  let $_intlDayHeader;
  let { theme, _intlDayHeader, _scrollable } = getContext("state");
  component_subscribe($$self, theme, (value) => $$invalidate(0, $theme = value));
  component_subscribe($$self, _intlDayHeader, (value) => $$invalidate(3, $_intlDayHeader = value));
  component_subscribe($$self, _scrollable, (value) => $$invalidate(1, $_scrollable = value));
  let { _days } = getContext("view-state");
  component_subscribe($$self, _days, (value) => $$invalidate(2, $_days = value));
  return [
    $theme,
    $_scrollable,
    $_days,
    $_intlDayHeader,
    theme,
    _intlDayHeader,
    _scrollable,
    _days
  ];
}
var Header = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance$6, create_fragment$6, safe_not_equal, {});
  }
};
function create_fragment$5(ctx) {
  let div1;
  let div0;
  let div0_class_value;
  let div1_class_value;
  let current;
  let mounted;
  let dispose;
  const default_slot_template = ctx[12].default;
  const default_slot = create_slot(default_slot_template, ctx, ctx[11], null);
  return {
    c() {
      div1 = element("div");
      div0 = element("div");
      if (default_slot)
        default_slot.c();
      attr(div0, "class", div0_class_value = ctx[1].content);
      attr(div1, "class", div1_class_value = ctx[1].body + (ctx[2] === true ? " " + ctx[1].uniform : ""));
    },
    m(target, anchor) {
      insert(target, div1, anchor);
      append(div1, div0);
      if (default_slot) {
        default_slot.m(div0, null);
      }
      ctx[13](div1);
      current = true;
      if (!mounted) {
        dispose = [
          listen(window, "resize", ctx[9]),
          listen(div1, "scroll", function() {
            if (is_function(ctx[3]))
              ctx[3].apply(this, arguments);
          })
        ];
        mounted = true;
      }
    },
    p(new_ctx, [dirty]) {
      ctx = new_ctx;
      if (default_slot) {
        if (default_slot.p && (!current || dirty & 2048)) {
          update_slot_base(default_slot, default_slot_template, ctx, ctx[11], !current ? get_all_dirty_from_scope(ctx[11]) : get_slot_changes(default_slot_template, ctx[11], dirty, null), null);
        }
      }
      if (!current || dirty & 2 && div0_class_value !== (div0_class_value = ctx[1].content)) {
        attr(div0, "class", div0_class_value);
      }
      if (!current || dirty & 6 && div1_class_value !== (div1_class_value = ctx[1].body + (ctx[2] === true ? " " + ctx[1].uniform : ""))) {
        attr(div1, "class", div1_class_value);
      }
    },
    i(local) {
      if (current)
        return;
      transition_in(default_slot, local);
      current = true;
    },
    o(local) {
      transition_out(default_slot, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div1);
      if (default_slot)
        default_slot.d(detaching);
      ctx[13](null);
      mounted = false;
      run_all(dispose);
    }
  };
}
function instance$5($$self, $$props, $$invalidate) {
  let $_scrollable;
  let $_events;
  let $theme;
  let $dayMaxEvents;
  let $_scroll;
  let { $$slots: slots = {}, $$scope } = $$props;
  let { dayMaxEvents, _events, _scrollable, _scroll, theme } = getContext("state");
  component_subscribe($$self, dayMaxEvents, (value) => $$invalidate(2, $dayMaxEvents = value));
  component_subscribe($$self, _events, (value) => $$invalidate(10, $_events = value));
  component_subscribe($$self, _scrollable, (value) => $$invalidate(14, $_scrollable = value));
  component_subscribe($$self, _scroll, (value) => $$invalidate(3, $_scroll = value));
  component_subscribe($$self, theme, (value) => $$invalidate(1, $theme = value));
  let el;
  function recheckScrollable() {
    set_store_value(_scrollable, $_scrollable = hasYScroll(el), $_scrollable);
  }
  function div1_binding($$value) {
    binding_callbacks[$$value ? "unshift" : "push"](() => {
      el = $$value;
      $$invalidate(0, el);
    });
  }
  $$self.$$set = ($$props2) => {
    if ("$$scope" in $$props2)
      $$invalidate(11, $$scope = $$props2.$$scope);
  };
  $$self.$$.update = () => {
    if ($$self.$$.dirty & 1025) {
      if (el && $_events) {
        tick().then(recheckScrollable);
      }
    }
  };
  return [
    el,
    $theme,
    $dayMaxEvents,
    $_scroll,
    dayMaxEvents,
    _events,
    _scrollable,
    _scroll,
    theme,
    recheckScrollable,
    $_events,
    $$scope,
    slots,
    div1_binding
  ];
}
var Body = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance$5, create_fragment$5, safe_not_equal, {});
  }
};
function create_fragment$4(ctx) {
  let div1;
  let div0;
  let div0_class_value;
  let setContent_action;
  let t;
  let switch_instance;
  let current;
  let mounted;
  let dispose;
  var switch_value = ctx[7].resizer;
  function switch_props(ctx2) {
    return { props: { event: ctx2[0] } };
  }
  if (switch_value) {
    switch_instance = construct_svelte_component(switch_value, switch_props(ctx));
    switch_instance.$on("pointerdown", ctx[29](true));
  }
  return {
    c() {
      div1 = element("div");
      div0 = element("div");
      t = space();
      if (switch_instance)
        create_component(switch_instance.$$.fragment);
      attr(div0, "class", div0_class_value = ctx[1].eventBody);
      attr(div1, "class", ctx[3]);
      attr(div1, "style", ctx[4]);
    },
    m(target, anchor) {
      insert(target, div1, anchor);
      append(div1, div0);
      append(div1, t);
      if (switch_instance)
        mount_component(switch_instance, div1, null);
      ctx[44](div1);
      current = true;
      if (!mounted) {
        dispose = [
          listen(window, "resize", ctx[30]),
          action_destroyer(setContent_action = setContent.call(null, div0, ctx[5])),
          listen(div1, "click", function() {
            if (is_function(maybeIgnore(ctx[28](ctx[8], ctx[6]))))
              maybeIgnore(ctx[28](ctx[8], ctx[6])).apply(this, arguments);
          }),
          listen(div1, "mouseenter", function() {
            if (is_function(ctx[28](ctx[9], ctx[6])))
              ctx[28](ctx[9], ctx[6]).apply(this, arguments);
          }),
          listen(div1, "mouseleave", function() {
            if (is_function(ctx[28](ctx[10], ctx[6])))
              ctx[28](ctx[10], ctx[6]).apply(this, arguments);
          }),
          listen(div1, "pointerdown", function() {
            if (is_function(ctx[6] === "auto" && ctx[11](ctx[0]) ? ctx[29]() : void 0))
              (ctx[6] === "auto" && ctx[11](ctx[0]) ? ctx[29]() : void 0).apply(this, arguments);
          })
        ];
        mounted = true;
      }
    },
    p(new_ctx, dirty) {
      ctx = new_ctx;
      if (!current || dirty[0] & 2 && div0_class_value !== (div0_class_value = ctx[1].eventBody)) {
        attr(div0, "class", div0_class_value);
      }
      if (setContent_action && is_function(setContent_action.update) && dirty[0] & 32)
        setContent_action.update.call(null, ctx[5]);
      const switch_instance_changes = {};
      if (dirty[0] & 1)
        switch_instance_changes.event = ctx[0];
      if (switch_value !== (switch_value = ctx[7].resizer)) {
        if (switch_instance) {
          group_outros();
          const old_component = switch_instance;
          transition_out(old_component.$$.fragment, 1, 0, () => {
            destroy_component(old_component, 1);
          });
          check_outros();
        }
        if (switch_value) {
          switch_instance = construct_svelte_component(switch_value, switch_props(ctx));
          switch_instance.$on("pointerdown", ctx[29](true));
          create_component(switch_instance.$$.fragment);
          transition_in(switch_instance.$$.fragment, 1);
          mount_component(switch_instance, div1, null);
        } else {
          switch_instance = null;
        }
      } else if (switch_value) {
        switch_instance.$set(switch_instance_changes);
      }
      if (!current || dirty[0] & 8) {
        attr(div1, "class", ctx[3]);
      }
      if (!current || dirty[0] & 16) {
        attr(div1, "style", ctx[4]);
      }
    },
    i(local) {
      if (current)
        return;
      if (switch_instance)
        transition_in(switch_instance.$$.fragment, local);
      current = true;
    },
    o(local) {
      if (switch_instance)
        transition_out(switch_instance.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div1);
      if (switch_instance)
        destroy_component(switch_instance);
      ctx[44](null);
      mounted = false;
      run_all(dispose);
    }
  };
}
function instance$4($$self, $$props, $$invalidate) {
  let $_hiddenEvents;
  let $dayMaxEvents;
  let $_interaction;
  let $_view;
  let $eventDidMount;
  let $_intlEventTime;
  let $theme;
  let $eventContent;
  let $displayEventEnd;
  let $_classes;
  let $eventColor;
  let $eventBackgroundColor;
  let $eventClick;
  let $eventMouseEnter;
  let $eventMouseLeave;
  let $_draggable;
  let { chunk } = $$props;
  let { longChunks = {} } = $$props;
  let { inPopup = false } = $$props;
  let { dayMaxEvents, displayEventEnd, eventBackgroundColor, eventClick, eventColor, eventContent, eventDidMount, eventMouseEnter, eventMouseLeave, theme, _view, _intlEventTime, _interaction, _classes, _draggable } = getContext("state");
  component_subscribe($$self, dayMaxEvents, (value) => $$invalidate(46, $dayMaxEvents = value));
  component_subscribe($$self, displayEventEnd, (value) => $$invalidate(40, $displayEventEnd = value));
  component_subscribe($$self, eventBackgroundColor, (value) => $$invalidate(43, $eventBackgroundColor = value));
  component_subscribe($$self, eventClick, (value) => $$invalidate(8, $eventClick = value));
  component_subscribe($$self, eventColor, (value) => $$invalidate(42, $eventColor = value));
  component_subscribe($$self, eventContent, (value) => $$invalidate(39, $eventContent = value));
  component_subscribe($$self, eventDidMount, (value) => $$invalidate(47, $eventDidMount = value));
  component_subscribe($$self, eventMouseEnter, (value) => $$invalidate(9, $eventMouseEnter = value));
  component_subscribe($$self, eventMouseLeave, (value) => $$invalidate(10, $eventMouseLeave = value));
  component_subscribe($$self, theme, (value) => $$invalidate(1, $theme = value));
  component_subscribe($$self, _view, (value) => $$invalidate(37, $_view = value));
  component_subscribe($$self, _intlEventTime, (value) => $$invalidate(38, $_intlEventTime = value));
  component_subscribe($$self, _interaction, (value) => $$invalidate(7, $_interaction = value));
  component_subscribe($$self, _classes, (value) => $$invalidate(41, $_classes = value));
  component_subscribe($$self, _draggable, (value) => $$invalidate(11, $_draggable = value));
  let { _hiddenEvents } = getContext("view-state");
  component_subscribe($$self, _hiddenEvents, (value) => $$invalidate(36, $_hiddenEvents = value));
  let el;
  let event;
  let classes;
  let style;
  let content;
  let timeText;
  let margin = 1;
  let hidden = false;
  let display2;
  onMount(() => {
    if (is_function($eventDidMount)) {
      $eventDidMount({
        event: toEventWithLocalDates(event),
        timeText,
        el,
        view: toViewWithLocalDates($_view)
      });
    }
  });
  afterUpdate(reposition);
  function createHandler(fn, display3) {
    return display3 !== "preview" && is_function(fn) ? (jsEvent) => fn({
      event: toEventWithLocalDates(event),
      el,
      jsEvent,
      view: toViewWithLocalDates($_view)
    }) : void 0;
  }
  function createDragHandler(resize) {
    return (jsEvent) => $_interaction.action.dragDayGrid(event, el, jsEvent, inPopup, resize);
  }
  function reposition() {
    if (!el || display2 === "preview" || inPopup) {
      return;
    }
    $$invalidate(31, chunk.ready = false, chunk);
    $$invalidate(31, chunk.top = 0, chunk);
    if (chunk.prev) {
      if (!chunk.prev.ready) {
        tick().then(reposition);
        return;
      }
      $$invalidate(31, chunk.top = chunk.prev.bottom + 1, chunk);
    }
    $$invalidate(31, chunk.bottom = chunk.top + height(el), chunk);
    let m = 1;
    let key = chunk.date.getTime();
    if (longChunks[key]) {
      for (let longChunk of longChunks[key]) {
        if (!longChunk.ready) {
          tick().then(reposition);
          return;
        }
        if (chunk.top < longChunk.bottom && chunk.bottom > longChunk.top) {
          let offset = longChunk.bottom - chunk.top + 1;
          m += offset;
          $$invalidate(31, chunk.top += offset, chunk);
          $$invalidate(31, chunk.bottom += offset, chunk);
        }
      }
    }
    $$invalidate(34, margin = m);
    if ($dayMaxEvents === true) {
      hide();
    } else {
      $$invalidate(35, hidden = false);
    }
    $$invalidate(31, chunk.ready = true, chunk);
  }
  function hide() {
    if (!el) {
      return;
    }
    let dayEl = ancestor(el, 2);
    let h = height(dayEl) - height(dayEl.firstElementChild) - footHeight(dayEl);
    $$invalidate(35, hidden = chunk.bottom > h);
    cloneDate(chunk.date);
    let update2 = false;
    for (let date of chunk.dates) {
      let hiddenEvents = $_hiddenEvents[date.getTime()];
      if (hiddenEvents) {
        let size = hiddenEvents.size;
        if (hidden) {
          hiddenEvents.add(chunk.event);
        } else {
          hiddenEvents.delete(chunk.event);
        }
        if (size !== hiddenEvents.size) {
          update2 = true;
        }
      }
    }
    if (update2) {
      _hiddenEvents.set($_hiddenEvents);
    }
  }
  function footHeight(dayEl) {
    let h = 0;
    for (let i = 0; i < chunk.days; ++i) {
      h = Math.max(h, height(dayEl.lastElementChild));
      dayEl = dayEl.nextElementSibling;
      if (!dayEl) {
        break;
      }
    }
    return h;
  }
  function div1_binding($$value) {
    binding_callbacks[$$value ? "unshift" : "push"](() => {
      el = $$value;
      $$invalidate(2, el);
    });
  }
  $$self.$$set = ($$props2) => {
    if ("chunk" in $$props2)
      $$invalidate(31, chunk = $$props2.chunk);
    if ("longChunks" in $$props2)
      $$invalidate(32, longChunks = $$props2.longChunks);
    if ("inPopup" in $$props2)
      $$invalidate(33, inPopup = $$props2.inPopup);
  };
  $$self.$$.update = () => {
    if ($$self.$$.dirty[1] & 1) {
      $$invalidate(0, event = chunk.event);
    }
    if ($$self.$$.dirty[0] & 19 | $$self.$$.dirty[1] & 7193) {
      {
        $$invalidate(6, display2 = event.display);
        let bgColor = event.backgroundColor || $eventBackgroundColor || $eventColor;
        $$invalidate(4, style = `width:calc(${chunk.days * 100}% + ${(chunk.days - 1) * 7}px);margin-top:${margin}px;`);
        if (bgColor) {
          $$invalidate(4, style += `background-color:${bgColor};`);
        }
        if (hidden) {
          $$invalidate(4, style += "visibility:hidden;");
        }
        $$invalidate(3, classes = $_classes($theme.event, event));
      }
    }
    if ($$self.$$.dirty[0] & 2 | $$self.$$.dirty[1] & 961) {
      $$invalidate(5, [timeText, content] = createEventContent(chunk, $displayEventEnd, $eventContent, $theme, $_intlEventTime, $_view), content);
    }
    if ($$self.$$.dirty[1] & 32) {
      if ($_hiddenEvents) {
        tick().then(reposition);
      }
    }
  };
  return [
    event,
    $theme,
    el,
    classes,
    style,
    content,
    display2,
    $_interaction,
    $eventClick,
    $eventMouseEnter,
    $eventMouseLeave,
    $_draggable,
    dayMaxEvents,
    displayEventEnd,
    eventBackgroundColor,
    eventClick,
    eventColor,
    eventContent,
    eventDidMount,
    eventMouseEnter,
    eventMouseLeave,
    theme,
    _view,
    _intlEventTime,
    _interaction,
    _classes,
    _draggable,
    _hiddenEvents,
    createHandler,
    createDragHandler,
    reposition,
    chunk,
    longChunks,
    inPopup,
    margin,
    hidden,
    $_hiddenEvents,
    $_view,
    $_intlEventTime,
    $eventContent,
    $displayEventEnd,
    $_classes,
    $eventColor,
    $eventBackgroundColor,
    div1_binding
  ];
}
var Event = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance$4, create_fragment$4, safe_not_equal, { chunk: 31, longChunks: 32, inPopup: 33 }, null, [-1, -1]);
  }
};
function get_each_context$3(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[16] = list[i];
  return child_ctx;
}
function create_each_block$3(key_1, ctx) {
  let first;
  let event;
  let current;
  event = new Event({
    props: { chunk: ctx[16], inPopup: true }
  });
  return {
    key: key_1,
    first: null,
    c() {
      first = empty();
      create_component(event.$$.fragment);
      this.first = first;
    },
    m(target, anchor) {
      insert(target, first, anchor);
      mount_component(event, target, anchor);
      current = true;
    },
    p(new_ctx, dirty) {
      ctx = new_ctx;
      const event_changes = {};
      if (dirty & 32)
        event_changes.chunk = ctx[16];
      event.$set(event_changes);
    },
    i(local) {
      if (current)
        return;
      transition_in(event.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(event.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(first);
      destroy_component(event, detaching);
    }
  };
}
function create_fragment$3(ctx) {
  let div2;
  let div0;
  let t0_value = ctx[4].format(ctx[2]) + "";
  let t0;
  let t1;
  let a;
  let div0_class_value;
  let t3;
  let div1;
  let each_blocks = [];
  let each_1_lookup = /* @__PURE__ */ new Map();
  let div1_class_value;
  let div2_class_value;
  let current;
  let mounted;
  let dispose;
  let each_value = ctx[5];
  const get_key = (ctx2) => ctx2[16].event;
  for (let i = 0; i < each_value.length; i += 1) {
    let child_ctx = get_each_context$3(ctx, each_value, i);
    let key = get_key(child_ctx);
    each_1_lookup.set(key, each_blocks[i] = create_each_block$3(key, child_ctx));
  }
  return {
    c() {
      div2 = element("div");
      div0 = element("div");
      t0 = text(t0_value);
      t1 = space();
      a = element("a");
      a.textContent = "\xD7";
      t3 = space();
      div1 = element("div");
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      attr(div0, "class", div0_class_value = ctx[3].dayHead);
      attr(div1, "class", div1_class_value = ctx[3].events);
      attr(div2, "class", div2_class_value = ctx[3].popup);
      attr(div2, "style", ctx[1]);
    },
    m(target, anchor) {
      insert(target, div2, anchor);
      append(div2, div0);
      append(div0, t0);
      append(div0, t1);
      append(div0, a);
      append(div2, t3);
      append(div2, div1);
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(div1, null);
      }
      ctx[14](div2);
      current = true;
      if (!mounted) {
        dispose = [
          listen(a, "click", stop_propagation(ctx[11])),
          action_destroyer(outsideEvent.call(null, div2, "pointerdown")),
          listen(div2, "pointerdown", stop_propagation(ctx[13])),
          listen(div2, "pointerdownoutside", ctx[12])
        ];
        mounted = true;
      }
    },
    p(ctx2, [dirty]) {
      if ((!current || dirty & 20) && t0_value !== (t0_value = ctx2[4].format(ctx2[2]) + ""))
        set_data(t0, t0_value);
      if (!current || dirty & 8 && div0_class_value !== (div0_class_value = ctx2[3].dayHead)) {
        attr(div0, "class", div0_class_value);
      }
      if (dirty & 32) {
        each_value = ctx2[5];
        group_outros();
        each_blocks = update_keyed_each(each_blocks, dirty, get_key, 1, ctx2, each_value, each_1_lookup, div1, outro_and_destroy_block, create_each_block$3, null, get_each_context$3);
        check_outros();
      }
      if (!current || dirty & 8 && div1_class_value !== (div1_class_value = ctx2[3].events)) {
        attr(div1, "class", div1_class_value);
      }
      if (!current || dirty & 8 && div2_class_value !== (div2_class_value = ctx2[3].popup)) {
        attr(div2, "class", div2_class_value);
      }
      if (!current || dirty & 2) {
        attr(div2, "style", ctx2[1]);
      }
    },
    i(local) {
      if (current)
        return;
      for (let i = 0; i < each_value.length; i += 1) {
        transition_in(each_blocks[i]);
      }
      current = true;
    },
    o(local) {
      for (let i = 0; i < each_blocks.length; i += 1) {
        transition_out(each_blocks[i]);
      }
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div2);
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].d();
      }
      ctx[14](null);
      mounted = false;
      run_all(dispose);
    }
  };
}
function instance$3($$self, $$props, $$invalidate) {
  let $_ignoreClick;
  let $_popupDate;
  let $theme;
  let $_intlDayPopover;
  let $_popupChunks;
  let { theme, _ignoreClick, _intlDayPopover } = getContext("state");
  component_subscribe($$self, theme, (value) => $$invalidate(3, $theme = value));
  component_subscribe($$self, _ignoreClick, (value) => $$invalidate(15, $_ignoreClick = value));
  component_subscribe($$self, _intlDayPopover, (value) => $$invalidate(4, $_intlDayPopover = value));
  let { _popupDate, _popupChunks } = getContext("view-state");
  component_subscribe($$self, _popupDate, (value) => $$invalidate(2, $_popupDate = value));
  component_subscribe($$self, _popupChunks, (value) => $$invalidate(5, $_popupChunks = value));
  let el;
  let style = "";
  onMount(() => {
    let dayEl = ancestor(el, 1);
    let bodyEl = ancestor(dayEl, 3);
    let popupRect = rect(el);
    let bodyRect = rect(bodyEl);
    if (!dayEl.previousElementSibling) {
      $$invalidate(1, style = "left:0;");
    } else if (!dayEl.nextElementSibling) {
      $$invalidate(1, style = "right:0;");
    } else {
      let left = (dayEl.offsetWidth - popupRect.width) / 2;
      $$invalidate(1, style = `left:${left}px;`);
    }
    let top = (dayEl.offsetHeight - popupRect.height) / 2;
    if (popupRect.top + top < bodyRect.top) {
      top = bodyRect.top - popupRect.top;
    } else if (popupRect.bottom + top > bodyRect.bottom) {
      top = bodyRect.bottom - popupRect.bottom;
    }
    $$invalidate(1, style += `top:${top}px;`);
    if (popupRect.top + top + popupRect.height > bodyRect.bottom) {
      let bottom = popupRect.top + dayEl.offsetHeight - bodyRect.bottom;
      $$invalidate(1, style += `bottom:${bottom}px;`);
    }
  });
  function close(e) {
    set_store_value(_popupDate, $_popupDate = null, $_popupDate);
  }
  function handlePointerDownOutside(e) {
    close();
    set_store_value(_ignoreClick, $_ignoreClick = true, $_ignoreClick);
  }
  function pointerdown_handler(event) {
    bubble.call(this, $$self, event);
  }
  function div2_binding($$value) {
    binding_callbacks[$$value ? "unshift" : "push"](() => {
      el = $$value;
      $$invalidate(0, el);
    });
  }
  return [
    el,
    style,
    $_popupDate,
    $theme,
    $_intlDayPopover,
    $_popupChunks,
    theme,
    _ignoreClick,
    _intlDayPopover,
    _popupDate,
    _popupChunks,
    close,
    handlePointerDownOutside,
    pointerdown_handler,
    div2_binding
  ];
}
var Popup = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance$3, create_fragment$3, safe_not_equal, {});
  }
};
function get_each_context$2(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[43] = list[i];
  return child_ctx;
}
function create_if_block_32(ctx) {
  let div;
  let event;
  let div_class_value;
  let current;
  event = new Event({ props: { chunk: ctx[2][1] } });
  return {
    c() {
      div = element("div");
      create_component(event.$$.fragment);
      attr(div, "class", div_class_value = ctx[11].events);
    },
    m(target, anchor) {
      insert(target, div, anchor);
      mount_component(event, div, null);
      current = true;
    },
    p(ctx2, dirty) {
      const event_changes = {};
      if (dirty[0] & 4)
        event_changes.chunk = ctx2[2][1];
      event.$set(event_changes);
      if (!current || dirty[0] & 2048 && div_class_value !== (div_class_value = ctx2[11].events)) {
        attr(div, "class", div_class_value);
      }
    },
    i(local) {
      if (current)
        return;
      transition_in(event.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(event.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div);
      destroy_component(event);
    }
  };
}
function create_if_block_22(ctx) {
  let div;
  let event;
  let div_class_value;
  let current;
  event = new Event({ props: { chunk: ctx[2][0] } });
  return {
    c() {
      div = element("div");
      create_component(event.$$.fragment);
      attr(div, "class", div_class_value = ctx[11].events + " " + ctx[11].preview);
    },
    m(target, anchor) {
      insert(target, div, anchor);
      mount_component(event, div, null);
      current = true;
    },
    p(ctx2, dirty) {
      const event_changes = {};
      if (dirty[0] & 4)
        event_changes.chunk = ctx2[2][0];
      event.$set(event_changes);
      if (!current || dirty[0] & 2048 && div_class_value !== (div_class_value = ctx2[11].events + " " + ctx2[11].preview)) {
        attr(div, "class", div_class_value);
      }
    },
    i(local) {
      if (current)
        return;
      transition_in(event.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(event.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div);
      destroy_component(event);
    }
  };
}
function create_each_block$2(key_1, ctx) {
  let first;
  let event;
  let current;
  event = new Event({
    props: {
      chunk: ctx[43],
      longChunks: ctx[1]
    }
  });
  return {
    key: key_1,
    first: null,
    c() {
      first = empty();
      create_component(event.$$.fragment);
      this.first = first;
    },
    m(target, anchor) {
      insert(target, first, anchor);
      mount_component(event, target, anchor);
      current = true;
    },
    p(new_ctx, dirty) {
      ctx = new_ctx;
      const event_changes = {};
      if (dirty[0] & 8)
        event_changes.chunk = ctx[43];
      if (dirty[0] & 2)
        event_changes.longChunks = ctx[1];
      event.$set(event_changes);
    },
    i(local) {
      if (current)
        return;
      transition_in(event.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(event.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(first);
      destroy_component(event, detaching);
    }
  };
}
function create_if_block_12(ctx) {
  let popup;
  let current;
  popup = new Popup({});
  return {
    c() {
      create_component(popup.$$.fragment);
    },
    m(target, anchor) {
      mount_component(popup, target, anchor);
      current = true;
    },
    i(local) {
      if (current)
        return;
      transition_in(popup.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(popup.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      destroy_component(popup, detaching);
    }
  };
}
function create_if_block2(ctx) {
  let a;
  let setContent_action;
  let mounted;
  let dispose;
  return {
    c() {
      a = element("a");
    },
    m(target, anchor) {
      insert(target, a, anchor);
      if (!mounted) {
        dispose = [
          listen(a, "click", stop_propagation(ctx[28])),
          listen(a, "pointerdown", stop_propagation(ctx[36])),
          action_destroyer(setContent_action = setContent.call(null, a, ctx[5]))
        ];
        mounted = true;
      }
    },
    p(ctx2, dirty) {
      if (setContent_action && is_function(setContent_action.update) && dirty[0] & 32)
        setContent_action.update.call(null, ctx2[5]);
    },
    d(detaching) {
      if (detaching)
        detach(a);
      mounted = false;
      run_all(dispose);
    }
  };
}
function create_fragment$22(ctx) {
  let div3;
  let div0;
  let t0_value = ctx[0].getUTCDate() + "";
  let t0;
  let div0_class_value;
  let t1;
  let show_if_1 = ctx[2][1] && datesEqual(ctx[2][1].date, ctx[0]);
  let t2;
  let show_if = ctx[2][0] && datesEqual(ctx[2][0].date, ctx[0]);
  let t3;
  let div1;
  let each_blocks = [];
  let each_1_lookup = /* @__PURE__ */ new Map();
  let div1_class_value;
  let t4;
  let t5;
  let div2;
  let div2_class_value;
  let div3_class_value;
  let current;
  let mounted;
  let dispose;
  let if_block0 = show_if_1 && create_if_block_32(ctx);
  let if_block1 = show_if && create_if_block_22(ctx);
  let each_value = ctx[3];
  const get_key = (ctx2) => ctx2[43].event;
  for (let i = 0; i < each_value.length; i += 1) {
    let child_ctx = get_each_context$2(ctx, each_value, i);
    let key = get_key(child_ctx);
    each_1_lookup.set(key, each_blocks[i] = create_each_block$2(key, child_ctx));
  }
  let if_block2 = ctx[6] && create_if_block_12();
  let if_block3 = ctx[4].size && create_if_block2(ctx);
  return {
    c() {
      div3 = element("div");
      div0 = element("div");
      t0 = text(t0_value);
      t1 = space();
      if (if_block0)
        if_block0.c();
      t2 = space();
      if (if_block1)
        if_block1.c();
      t3 = space();
      div1 = element("div");
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      t4 = space();
      if (if_block2)
        if_block2.c();
      t5 = space();
      div2 = element("div");
      if (if_block3)
        if_block3.c();
      attr(div0, "class", div0_class_value = ctx[11].dayHead);
      attr(div1, "class", div1_class_value = ctx[11].events);
      attr(div2, "class", div2_class_value = ctx[11].dayFoot);
      attr(div3, "class", div3_class_value = ctx[11].day + (ctx[8] ? " " + ctx[11].today : "") + (ctx[9] ? " " + ctx[11].otherMonth : "") + (ctx[10] ? " " + ctx[11].highlight : ""));
    },
    m(target, anchor) {
      insert(target, div3, anchor);
      append(div3, div0);
      append(div0, t0);
      append(div3, t1);
      if (if_block0)
        if_block0.m(div3, null);
      append(div3, t2);
      if (if_block1)
        if_block1.m(div3, null);
      append(div3, t3);
      append(div3, div1);
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(div1, null);
      }
      append(div3, t4);
      if (if_block2)
        if_block2.m(div3, null);
      append(div3, t5);
      append(div3, div2);
      if (if_block3)
        if_block3.m(div2, null);
      ctx[37](div3);
      current = true;
      if (!mounted) {
        dispose = [
          listen(div3, "click", function() {
            if (is_function(maybeIgnore(ctx[26](ctx[12]))))
              maybeIgnore(ctx[26](ctx[12])).apply(this, arguments);
          }),
          listen(div3, "pointerenter", function() {
            if (is_function(ctx[27](ctx[13])))
              ctx[27](ctx[13]).apply(this, arguments);
          }),
          listen(div3, "pointerleave", function() {
            if (is_function(createPointerLeaveHandler(ctx[13])))
              createPointerLeaveHandler(ctx[13]).apply(this, arguments);
          }),
          listen(div3, "pointerdown", function() {
            if (is_function(ctx[29](ctx[13], ctx[14])))
              ctx[29](ctx[13], ctx[14]).apply(this, arguments);
          })
        ];
        mounted = true;
      }
    },
    p(new_ctx, dirty) {
      ctx = new_ctx;
      if ((!current || dirty[0] & 1) && t0_value !== (t0_value = ctx[0].getUTCDate() + ""))
        set_data(t0, t0_value);
      if (!current || dirty[0] & 2048 && div0_class_value !== (div0_class_value = ctx[11].dayHead)) {
        attr(div0, "class", div0_class_value);
      }
      if (dirty[0] & 5)
        show_if_1 = ctx[2][1] && datesEqual(ctx[2][1].date, ctx[0]);
      if (show_if_1) {
        if (if_block0) {
          if_block0.p(ctx, dirty);
          if (dirty[0] & 5) {
            transition_in(if_block0, 1);
          }
        } else {
          if_block0 = create_if_block_32(ctx);
          if_block0.c();
          transition_in(if_block0, 1);
          if_block0.m(div3, t2);
        }
      } else if (if_block0) {
        group_outros();
        transition_out(if_block0, 1, 1, () => {
          if_block0 = null;
        });
        check_outros();
      }
      if (dirty[0] & 5)
        show_if = ctx[2][0] && datesEqual(ctx[2][0].date, ctx[0]);
      if (show_if) {
        if (if_block1) {
          if_block1.p(ctx, dirty);
          if (dirty[0] & 5) {
            transition_in(if_block1, 1);
          }
        } else {
          if_block1 = create_if_block_22(ctx);
          if_block1.c();
          transition_in(if_block1, 1);
          if_block1.m(div3, t3);
        }
      } else if (if_block1) {
        group_outros();
        transition_out(if_block1, 1, 1, () => {
          if_block1 = null;
        });
        check_outros();
      }
      if (dirty[0] & 10) {
        each_value = ctx[3];
        group_outros();
        each_blocks = update_keyed_each(each_blocks, dirty, get_key, 1, ctx, each_value, each_1_lookup, div1, outro_and_destroy_block, create_each_block$2, null, get_each_context$2);
        check_outros();
      }
      if (!current || dirty[0] & 2048 && div1_class_value !== (div1_class_value = ctx[11].events)) {
        attr(div1, "class", div1_class_value);
      }
      if (ctx[6]) {
        if (if_block2) {
          if (dirty[0] & 64) {
            transition_in(if_block2, 1);
          }
        } else {
          if_block2 = create_if_block_12();
          if_block2.c();
          transition_in(if_block2, 1);
          if_block2.m(div3, t5);
        }
      } else if (if_block2) {
        group_outros();
        transition_out(if_block2, 1, 1, () => {
          if_block2 = null;
        });
        check_outros();
      }
      if (ctx[4].size) {
        if (if_block3) {
          if_block3.p(ctx, dirty);
        } else {
          if_block3 = create_if_block2(ctx);
          if_block3.c();
          if_block3.m(div2, null);
        }
      } else if (if_block3) {
        if_block3.d(1);
        if_block3 = null;
      }
      if (!current || dirty[0] & 2048 && div2_class_value !== (div2_class_value = ctx[11].dayFoot)) {
        attr(div2, "class", div2_class_value);
      }
      if (!current || dirty[0] & 3840 && div3_class_value !== (div3_class_value = ctx[11].day + (ctx[8] ? " " + ctx[11].today : "") + (ctx[9] ? " " + ctx[11].otherMonth : "") + (ctx[10] ? " " + ctx[11].highlight : ""))) {
        attr(div3, "class", div3_class_value);
      }
    },
    i(local) {
      if (current)
        return;
      transition_in(if_block0);
      transition_in(if_block1);
      for (let i = 0; i < each_value.length; i += 1) {
        transition_in(each_blocks[i]);
      }
      transition_in(if_block2);
      current = true;
    },
    o(local) {
      transition_out(if_block0);
      transition_out(if_block1);
      for (let i = 0; i < each_blocks.length; i += 1) {
        transition_out(each_blocks[i]);
      }
      transition_out(if_block2);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div3);
      if (if_block0)
        if_block0.d();
      if (if_block1)
        if_block1.d();
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].d();
      }
      if (if_block2)
        if_block2.d();
      if (if_block3)
        if_block3.d();
      ctx[37](null);
      mounted = false;
      run_all(dispose);
    }
  };
}
function createPointerLeaveHandler(interaction) {
  return interaction.pointer ? interaction.pointer.leave : void 0;
}
function instance$22($$self, $$props, $$invalidate) {
  let $_popupChunks;
  let $_popupDate;
  let $_view;
  let $moreLinkContent;
  let $_hiddenEvents;
  let $highlightedDates;
  let $currentDate;
  let $theme;
  let $dateClick;
  let $_interaction;
  let $selectable;
  let { date } = $$props;
  let { chunks } = $$props;
  let { longChunks } = $$props;
  let { iChunks = [] } = $$props;
  let { date: currentDate, dateClick, dayMaxEvents, highlightedDates, moreLinkContent, theme, _view, _interaction, selectable } = getContext("state");
  component_subscribe($$self, currentDate, (value) => $$invalidate(35, $currentDate = value));
  component_subscribe($$self, dateClick, (value) => $$invalidate(12, $dateClick = value));
  component_subscribe($$self, highlightedDates, (value) => $$invalidate(34, $highlightedDates = value));
  component_subscribe($$self, moreLinkContent, (value) => $$invalidate(32, $moreLinkContent = value));
  component_subscribe($$self, theme, (value) => $$invalidate(11, $theme = value));
  component_subscribe($$self, _view, (value) => $$invalidate(39, $_view = value));
  component_subscribe($$self, _interaction, (value) => $$invalidate(13, $_interaction = value));
  component_subscribe($$self, selectable, (value) => $$invalidate(14, $selectable = value));
  let { _hiddenEvents, _popupDate, _popupChunks } = getContext("view-state");
  component_subscribe($$self, _hiddenEvents, (value) => $$invalidate(33, $_hiddenEvents = value));
  component_subscribe($$self, _popupDate, (value) => $$invalidate(31, $_popupDate = value));
  component_subscribe($$self, _popupChunks, (value) => $$invalidate(38, $_popupChunks = value));
  let el;
  let dayChunks;
  let today2 = setMidnight(createDate());
  let isToday;
  let otherMonth;
  let highlight;
  let hiddenEvents = /* @__PURE__ */ new Set();
  let moreLink = "";
  let showPopup;
  function createClickHandler(fn) {
    return is_function(fn) ? (jsEvent) => fn({
      allDay: true,
      date: toLocalDate(date),
      dateStr: toISOString(date),
      dayEl: el,
      jsEvent,
      view: toViewWithLocalDates($_view)
    }) : void 0;
  }
  function createPointerEnterHandler(interaction) {
    return interaction.pointer ? (jsEvent) => interaction.pointer.enterDayGrid(date, jsEvent) : void 0;
  }
  function showMore() {
    set_store_value(_popupDate, $_popupDate = date, $_popupDate);
  }
  function setPopupChunks() {
    let nextDay = addDay(cloneDate(date));
    let chunks2 = dayChunks.concat(longChunks[date.getTime()] || []);
    if (chunks2.every((chunk) => chunk.ready)) {
      set_store_value(_popupChunks, $_popupChunks = chunks2.map((chunk) => assign2({}, chunk, createEventChunk(chunk.event, date, nextDay), { days: 1, dates: [date] })).sort((a, b) => a.top - b.top), $_popupChunks);
    } else {
      tick().then(setPopupChunks);
    }
  }
  function createPointerDownHandler(interaction, selectable2) {
    return selectable2 && interaction.action ? (jsEvent) => interaction.action.selectDayGrid(date, el, jsEvent) : void 0;
  }
  function pointerdown_handler(event) {
    bubble.call(this, $$self, event);
  }
  function div3_binding($$value) {
    binding_callbacks[$$value ? "unshift" : "push"](() => {
      el = $$value;
      $$invalidate(7, el);
    });
  }
  $$self.$$set = ($$props2) => {
    if ("date" in $$props2)
      $$invalidate(0, date = $$props2.date);
    if ("chunks" in $$props2)
      $$invalidate(30, chunks = $$props2.chunks);
    if ("longChunks" in $$props2)
      $$invalidate(1, longChunks = $$props2.longChunks);
    if ("iChunks" in $$props2)
      $$invalidate(2, iChunks = $$props2.iChunks);
  };
  $$self.$$.update = () => {
    if ($$self.$$.dirty[0] & 1073741849) {
      {
        $$invalidate(3, dayChunks = []);
        hiddenEvents.clear();
        $$invalidate(4, hiddenEvents), $$invalidate(30, chunks), $$invalidate(0, date), $$invalidate(3, dayChunks);
        for (let chunk of chunks) {
          if (datesEqual(chunk.date, date)) {
            dayChunks.push(chunk);
          }
        }
      }
    }
    if ($$self.$$.dirty[0] & 17) {
      set_store_value(_hiddenEvents, $_hiddenEvents[date.getTime()] = hiddenEvents, $_hiddenEvents);
    }
    if ($$self.$$.dirty[0] & 1 | $$self.$$.dirty[1] & 24) {
      {
        $$invalidate(8, isToday = datesEqual(date, today2));
        $$invalidate(9, otherMonth = date.getUTCMonth() !== $currentDate.getUTCMonth());
        $$invalidate(10, highlight = $highlightedDates.some((d) => datesEqual(d, date)));
      }
    }
    if ($$self.$$.dirty[0] & 48 | $$self.$$.dirty[1] & 6) {
      if ($_hiddenEvents && hiddenEvents.size) {
        let text2 = "+" + hiddenEvents.size + " more";
        if ($moreLinkContent) {
          $$invalidate(5, moreLink = is_function($moreLinkContent) ? $moreLinkContent({ num: hiddenEvents.size, text: text2 }) : $moreLinkContent);
          if (typeof moreLink === "string") {
            $$invalidate(5, moreLink = { html: moreLink });
          }
        } else {
          $$invalidate(5, moreLink = { html: text2 });
        }
      }
    }
    if ($$self.$$.dirty[0] & 1 | $$self.$$.dirty[1] & 1) {
      $$invalidate(6, showPopup = $_popupDate && datesEqual(date, $_popupDate));
    }
    if ($$self.$$.dirty[0] & 74) {
      if (showPopup && longChunks && dayChunks) {
        setPopupChunks();
      }
    }
  };
  return [
    date,
    longChunks,
    iChunks,
    dayChunks,
    hiddenEvents,
    moreLink,
    showPopup,
    el,
    isToday,
    otherMonth,
    highlight,
    $theme,
    $dateClick,
    $_interaction,
    $selectable,
    currentDate,
    dateClick,
    highlightedDates,
    moreLinkContent,
    theme,
    _view,
    _interaction,
    selectable,
    _hiddenEvents,
    _popupDate,
    _popupChunks,
    createClickHandler,
    createPointerEnterHandler,
    showMore,
    createPointerDownHandler,
    chunks,
    $_popupDate,
    $moreLinkContent,
    $_hiddenEvents,
    $highlightedDates,
    $currentDate,
    pointerdown_handler,
    div3_binding
  ];
}
var Day = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance$22, create_fragment$22, safe_not_equal, {
      date: 0,
      chunks: 30,
      longChunks: 1,
      iChunks: 2
    }, null, [-1, -1]);
  }
};
function get_each_context$12(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[15] = list[i];
  return child_ctx;
}
function create_each_block$12(ctx) {
  let day;
  let current;
  day = new Day({
    props: {
      date: ctx[15],
      chunks: ctx[1],
      longChunks: ctx[2],
      iChunks: ctx[3]
    }
  });
  return {
    c() {
      create_component(day.$$.fragment);
    },
    m(target, anchor) {
      mount_component(day, target, anchor);
      current = true;
    },
    p(ctx2, dirty) {
      const day_changes = {};
      if (dirty & 1)
        day_changes.date = ctx2[15];
      if (dirty & 2)
        day_changes.chunks = ctx2[1];
      if (dirty & 4)
        day_changes.longChunks = ctx2[2];
      if (dirty & 8)
        day_changes.iChunks = ctx2[3];
      day.$set(day_changes);
    },
    i(local) {
      if (current)
        return;
      transition_in(day.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(day.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      destroy_component(day, detaching);
    }
  };
}
function create_fragment$12(ctx) {
  let div;
  let div_class_value;
  let current;
  let each_value = ctx[0];
  let each_blocks = [];
  for (let i = 0; i < each_value.length; i += 1) {
    each_blocks[i] = create_each_block$12(get_each_context$12(ctx, each_value, i));
  }
  const out = (i) => transition_out(each_blocks[i], 1, 1, () => {
    each_blocks[i] = null;
  });
  return {
    c() {
      div = element("div");
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      attr(div, "class", div_class_value = ctx[4].days);
    },
    m(target, anchor) {
      insert(target, div, anchor);
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(div, null);
      }
      current = true;
    },
    p(ctx2, [dirty]) {
      if (dirty & 15) {
        each_value = ctx2[0];
        let i;
        for (i = 0; i < each_value.length; i += 1) {
          const child_ctx = get_each_context$12(ctx2, each_value, i);
          if (each_blocks[i]) {
            each_blocks[i].p(child_ctx, dirty);
            transition_in(each_blocks[i], 1);
          } else {
            each_blocks[i] = create_each_block$12(child_ctx);
            each_blocks[i].c();
            transition_in(each_blocks[i], 1);
            each_blocks[i].m(div, null);
          }
        }
        group_outros();
        for (i = each_value.length; i < each_blocks.length; i += 1) {
          out(i);
        }
        check_outros();
      }
      if (!current || dirty & 16 && div_class_value !== (div_class_value = ctx2[4].days)) {
        attr(div, "class", div_class_value);
      }
    },
    i(local) {
      if (current)
        return;
      for (let i = 0; i < each_value.length; i += 1) {
        transition_in(each_blocks[i]);
      }
      current = true;
    },
    o(local) {
      each_blocks = each_blocks.filter(Boolean);
      for (let i = 0; i < each_blocks.length; i += 1) {
        transition_out(each_blocks[i]);
      }
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(div);
      destroy_each(each_blocks, detaching);
    }
  };
}
function instance$12($$self, $$props, $$invalidate) {
  let $hiddenDays;
  let $_iEvents;
  let $_events;
  let $theme;
  let { dates } = $$props;
  let { _events, _iEvents, hiddenDays, theme } = getContext("state");
  component_subscribe($$self, _events, (value) => $$invalidate(13, $_events = value));
  component_subscribe($$self, _iEvents, (value) => $$invalidate(12, $_iEvents = value));
  component_subscribe($$self, hiddenDays, (value) => $$invalidate(11, $hiddenDays = value));
  component_subscribe($$self, theme, (value) => $$invalidate(4, $theme = value));
  let chunks, longChunks, iChunks = [];
  let start;
  let end;
  function intersects(event) {
    return event.start < end && event.end > start || datesEqual(event.start, event.end, start);
  }
  $$self.$$set = ($$props2) => {
    if ("dates" in $$props2)
      $$invalidate(0, dates = $$props2.dates);
  };
  $$self.$$.update = () => {
    if ($$self.$$.dirty & 1) {
      {
        $$invalidate(9, start = dates[0]);
        $$invalidate(10, end = addDay(cloneDate(dates[dates.length - 1])));
      }
    }
    if ($$self.$$.dirty & 11778) {
      {
        $$invalidate(1, chunks = []);
        for (let event of $_events) {
          if (event.display !== "background" && intersects(event)) {
            let chunk = createEventChunk(event, start, end);
            chunks.push(chunk);
          }
        }
        $$invalidate(2, longChunks = prepareEventChunks(chunks, $hiddenDays));
      }
    }
    if ($$self.$$.dirty & 7680) {
      $$invalidate(3, iChunks = $_iEvents.map((event) => {
        let chunk;
        if (event && intersects(event)) {
          chunk = createEventChunk(event, start, end);
          prepareEventChunks([chunk], $hiddenDays);
        } else {
          chunk = null;
        }
        return chunk;
      }));
    }
  };
  return [
    dates,
    chunks,
    longChunks,
    iChunks,
    $theme,
    _events,
    _iEvents,
    hiddenDays,
    theme,
    start,
    end,
    $hiddenDays,
    $_iEvents,
    $_events
  ];
}
var Week = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance$12, create_fragment$12, safe_not_equal, { dates: 0 });
  }
};
function get_each_context2(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[14] = list[i];
  return child_ctx;
}
function create_each_block2(ctx) {
  let week;
  let current;
  week = new Week({ props: { dates: ctx[14] } });
  return {
    c() {
      create_component(week.$$.fragment);
    },
    m(target, anchor) {
      mount_component(week, target, anchor);
      current = true;
    },
    p(ctx2, dirty) {
      const week_changes = {};
      if (dirty & 1)
        week_changes.dates = ctx2[14];
      week.$set(week_changes);
    },
    i(local) {
      if (current)
        return;
      transition_in(week.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(week.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      destroy_component(week, detaching);
    }
  };
}
function create_default_slot(ctx) {
  let each_1_anchor;
  let current;
  let each_value = ctx[0];
  let each_blocks = [];
  for (let i = 0; i < each_value.length; i += 1) {
    each_blocks[i] = create_each_block2(get_each_context2(ctx, each_value, i));
  }
  const out = (i) => transition_out(each_blocks[i], 1, 1, () => {
    each_blocks[i] = null;
  });
  return {
    c() {
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      each_1_anchor = empty();
    },
    m(target, anchor) {
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(target, anchor);
      }
      insert(target, each_1_anchor, anchor);
      current = true;
    },
    p(ctx2, dirty) {
      if (dirty & 1) {
        each_value = ctx2[0];
        let i;
        for (i = 0; i < each_value.length; i += 1) {
          const child_ctx = get_each_context2(ctx2, each_value, i);
          if (each_blocks[i]) {
            each_blocks[i].p(child_ctx, dirty);
            transition_in(each_blocks[i], 1);
          } else {
            each_blocks[i] = create_each_block2(child_ctx);
            each_blocks[i].c();
            transition_in(each_blocks[i], 1);
            each_blocks[i].m(each_1_anchor.parentNode, each_1_anchor);
          }
        }
        group_outros();
        for (i = each_value.length; i < each_blocks.length; i += 1) {
          out(i);
        }
        check_outros();
      }
    },
    i(local) {
      if (current)
        return;
      for (let i = 0; i < each_value.length; i += 1) {
        transition_in(each_blocks[i]);
      }
      current = true;
    },
    o(local) {
      each_blocks = each_blocks.filter(Boolean);
      for (let i = 0; i < each_blocks.length; i += 1) {
        transition_out(each_blocks[i]);
      }
      current = false;
    },
    d(detaching) {
      destroy_each(each_blocks, detaching);
      if (detaching)
        detach(each_1_anchor);
    }
  };
}
function create_fragment2(ctx) {
  let header;
  let t;
  let body;
  let current;
  header = new Header({});
  body = new Body({
    props: {
      $$slots: { default: [create_default_slot] },
      $$scope: { ctx }
    }
  });
  return {
    c() {
      create_component(header.$$.fragment);
      t = space();
      create_component(body.$$.fragment);
    },
    m(target, anchor) {
      mount_component(header, target, anchor);
      insert(target, t, anchor);
      mount_component(body, target, anchor);
      current = true;
    },
    p(ctx2, [dirty]) {
      const body_changes = {};
      if (dirty & 131073) {
        body_changes.$$scope = { dirty, ctx: ctx2 };
      }
      body.$set(body_changes);
    },
    i(local) {
      if (current)
        return;
      transition_in(header.$$.fragment, local);
      transition_in(body.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(header.$$.fragment, local);
      transition_out(body.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      destroy_component(header, detaching);
      if (detaching)
        detach(t);
      destroy_component(body, detaching);
    }
  };
}
function instance2($$self, $$props, $$invalidate) {
  let $_viewDates;
  let $dayMaxEvents;
  let $_hiddenEvents;
  let $hiddenDays;
  let $_viewClass;
  let { _viewDates, _viewClass, dayMaxEvents, hiddenDays, theme } = getContext("state");
  component_subscribe($$self, _viewDates, (value) => $$invalidate(7, $_viewDates = value));
  component_subscribe($$self, _viewClass, (value) => $$invalidate(11, $_viewClass = value));
  component_subscribe($$self, dayMaxEvents, (value) => $$invalidate(8, $dayMaxEvents = value));
  component_subscribe($$self, hiddenDays, (value) => $$invalidate(9, $hiddenDays = value));
  let viewState = new State2(getContext("state"));
  setContext("view-state", viewState);
  let { _hiddenEvents } = viewState;
  component_subscribe($$self, _hiddenEvents, (value) => $$invalidate(10, $_hiddenEvents = value));
  set_store_value(_viewClass, $_viewClass = "month", $_viewClass);
  let weeks;
  let days2;
  $$self.$$.update = () => {
    if ($$self.$$.dirty & 961) {
      {
        $$invalidate(0, weeks = []);
        $$invalidate(6, days2 = 7 - $hiddenDays.length);
        set_store_value(_hiddenEvents, $_hiddenEvents = {}, $_hiddenEvents);
        for (let i = 0; i < $_viewDates.length / days2; ++i) {
          let dates = [];
          for (let j = 0; j < days2; ++j) {
            dates.push($_viewDates[i * days2 + j]);
          }
          weeks.push(dates);
        }
      }
    }
  };
  return [
    weeks,
    _viewDates,
    _viewClass,
    dayMaxEvents,
    hiddenDays,
    _hiddenEvents,
    days2,
    $_viewDates,
    $dayMaxEvents,
    $hiddenDays
  ];
}
var View = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance2, create_fragment2, safe_not_equal, {});
  }
};
var index = {
  createOptions(options) {
    options.dayMaxEvents = false;
    options.dayPopoverFormat = { month: "long", day: "numeric", year: "numeric" };
    options.moreLinkContent = void 0;
    options.buttonText.dayGridMonth = "month";
    options.theme.uniform = "ec-uniform";
    options.theme.dayFoot = "ec-day-foot";
    options.theme.month = "ec-month";
    options.theme.popup = "ec-popup";
    options.view = "dayGridMonth";
    options.views.dayGridMonth = {
      component: View,
      dayHeaderFormat: { weekday: "short" },
      displayEventEnd: false,
      duration: { months: 1 },
      monthMode: true,
      titleFormat: { year: "numeric", month: "long" }
    };
  },
  createStores(state) {
    state._intlDayPopover = intl(state.locale, state.dayPopoverFormat);
  }
};

// src/component/RepetitionTaskView.svelte
var import_owlelia7 = __toESM(require_dist());
function get_each_context3(ctx, list, i) {
  const child_ctx = ctx.slice();
  child_ctx[9] = list[i];
  return child_ctx;
}
function create_if_block_13(ctx) {
  let each_1_anchor;
  let each_value = ctx[0];
  let each_blocks = [];
  for (let i = 0; i < each_value.length; i += 1) {
    each_blocks[i] = create_each_block3(get_each_context3(ctx, each_value, i));
  }
  return {
    c() {
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].c();
      }
      each_1_anchor = empty();
    },
    m(target, anchor) {
      for (let i = 0; i < each_blocks.length; i += 1) {
        each_blocks[i].m(target, anchor);
      }
      insert(target, each_1_anchor, anchor);
    },
    p(ctx2, dirty) {
      if (dirty & 11) {
        each_value = ctx2[0];
        let i;
        for (i = 0; i < each_value.length; i += 1) {
          const child_ctx = get_each_context3(ctx2, each_value, i);
          if (each_blocks[i]) {
            each_blocks[i].p(child_ctx, dirty);
          } else {
            each_blocks[i] = create_each_block3(child_ctx);
            each_blocks[i].c();
            each_blocks[i].m(each_1_anchor.parentNode, each_1_anchor);
          }
        }
        for (; i < each_blocks.length; i += 1) {
          each_blocks[i].d(1);
        }
        each_blocks.length = each_value.length;
      }
    },
    d(detaching) {
      destroy_each(each_blocks, detaching);
      if (detaching)
        detach(each_1_anchor);
    }
  };
}
function create_each_block3(ctx) {
  let div;
  let t0_value = ctx[9].name + "";
  let t0;
  let t1;
  let mounted;
  let dispose;
  return {
    c() {
      div = element("div");
      t0 = text(t0_value);
      t1 = space();
      attr(div, "class", "nav-file-title");
      toggle_class(div, "is-active", ctx[1] && ctx[9].name == ctx[1].name);
    },
    m(target, anchor) {
      insert(target, div, anchor);
      append(div, t0);
      append(div, t1);
      if (!mounted) {
        dispose = [
          listen(div, "click", function() {
            if (is_function(ctx[3](ctx[9])))
              ctx[3](ctx[9]).apply(this, arguments);
          }),
          listen(div, "keypress", function() {
            if (is_function(ctx[3](ctx[9])))
              ctx[3](ctx[9]).apply(this, arguments);
          })
        ];
        mounted = true;
      }
    },
    p(new_ctx, dirty) {
      ctx = new_ctx;
      if (dirty & 1 && t0_value !== (t0_value = ctx[9].name + ""))
        set_data(t0, t0_value);
      if (dirty & 3) {
        toggle_class(div, "is-active", ctx[1] && ctx[9].name == ctx[1].name);
      }
    },
    d(detaching) {
      if (detaching)
        detach(div);
      mounted = false;
      run_all(dispose);
    }
  };
}
function create_if_block3(ctx) {
  let div;
  let t_value = ctx[1].name + "";
  let t;
  return {
    c() {
      div = element("div");
      t = text(t_value);
      set_style(div, "padding", "8px");
      set_style(div, "display", "flex");
      set_style(div, "justify-content", "center");
    },
    m(target, anchor) {
      insert(target, div, anchor);
      append(div, t);
    },
    p(ctx2, dirty) {
      if (dirty & 2 && t_value !== (t_value = ctx2[1].name + ""))
        set_data(t, t_value);
    },
    d(detaching) {
      if (detaching)
        detach(div);
    }
  };
}
function create_fragment3(ctx) {
  let h3;
  let t1;
  let div0;
  let t2;
  let div1;
  let calendar;
  let t3;
  let if_block1_anchor;
  let current;
  let if_block0 = ctx[0] && create_if_block_13(ctx);
  calendar = new Calendar({
    props: {
      plugins: ctx[4],
      options: ctx[2]
    }
  });
  let if_block1 = ctx[1] && create_if_block3(ctx);
  return {
    c() {
      h3 = element("h3");
      h3.textContent = "Recurring tasks";
      t1 = space();
      div0 = element("div");
      if (if_block0)
        if_block0.c();
      t2 = space();
      div1 = element("div");
      create_component(calendar.$$.fragment);
      t3 = space();
      if (if_block1)
        if_block1.c();
      if_block1_anchor = empty();
      set_style(div0, "height", "calc(100% - 275px - 50px - 75px)");
      set_style(div0, "overflow", "scroll");
      set_style(div1, "display", "flex");
      set_style(div1, "justify-content", "center");
    },
    m(target, anchor) {
      insert(target, h3, anchor);
      insert(target, t1, anchor);
      insert(target, div0, anchor);
      if (if_block0)
        if_block0.m(div0, null);
      insert(target, t2, anchor);
      insert(target, div1, anchor);
      mount_component(calendar, div1, null);
      insert(target, t3, anchor);
      if (if_block1)
        if_block1.m(target, anchor);
      insert(target, if_block1_anchor, anchor);
      current = true;
    },
    p(ctx2, [dirty]) {
      if (ctx2[0]) {
        if (if_block0) {
          if_block0.p(ctx2, dirty);
        } else {
          if_block0 = create_if_block_13(ctx2);
          if_block0.c();
          if_block0.m(div0, null);
        }
      } else if (if_block0) {
        if_block0.d(1);
        if_block0 = null;
      }
      const calendar_changes = {};
      if (dirty & 4)
        calendar_changes.options = ctx2[2];
      calendar.$set(calendar_changes);
      if (ctx2[1]) {
        if (if_block1) {
          if_block1.p(ctx2, dirty);
        } else {
          if_block1 = create_if_block3(ctx2);
          if_block1.c();
          if_block1.m(if_block1_anchor.parentNode, if_block1_anchor);
        }
      } else if (if_block1) {
        if_block1.d(1);
        if_block1 = null;
      }
    },
    i(local) {
      if (current)
        return;
      transition_in(calendar.$$.fragment, local);
      current = true;
    },
    o(local) {
      transition_out(calendar.$$.fragment, local);
      current = false;
    },
    d(detaching) {
      if (detaching)
        detach(h3);
      if (detaching)
        detach(t1);
      if (detaching)
        detach(div0);
      if (if_block0)
        if_block0.d();
      if (detaching)
        detach(t2);
      if (detaching)
        detach(div1);
      destroy_component(calendar);
      if (detaching)
        detach(t3);
      if (if_block1)
        if_block1.d(detaching);
      if (detaching)
        detach(if_block1_anchor);
    }
  };
}
function instance3($$self, $$props, $$invalidate) {
  let datesInFuture;
  let options;
  var _a;
  let { taskService } = $$props;
  let { tasks } = $$props;
  let { holidays } = $$props;
  let currentTask;
  const handleClickTask = (task) => __awaiter(void 0, void 0, void 0, function* () {
    $$invalidate(1, currentTask = task);
  });
  let plugins = [index];
  $$self.$$set = ($$props2) => {
    if ("taskService" in $$props2)
      $$invalidate(5, taskService = $$props2.taskService);
    if ("tasks" in $$props2)
      $$invalidate(0, tasks = $$props2.tasks);
    if ("holidays" in $$props2)
      $$invalidate(6, holidays = $$props2.holidays);
  };
  $$self.$$.update = () => {
    if ($$self.$$.dirty & 3) {
      $: {
        if ((tasks === null || tasks === void 0 ? void 0 : tasks.length) > 0) {
          $$invalidate(1, currentTask = tasks.find((x) => x.name === (currentTask === null || currentTask === void 0 ? void 0 : currentTask.name)));
        }
      }
    }
    if ($$self.$$.dirty & 98) {
      $:
        $$invalidate(8, datesInFuture = currentTask ? taskService.calcDatesInFuture(currentTask, holidays) : void 0);
    }
    if ($$self.$$.dirty & 448) {
      $:
        $$invalidate(2, options = {
          view: "dayGridMonth",
          highlightedDates: $$invalidate(7, _a = holidays === null || holidays === void 0 ? void 0 : holidays.map((x) => x.displayDate)) !== null && _a !== void 0 ? _a : [],
          events: datesInFuture ? import_owlelia7.DateTime.today().toDate(import_owlelia7.DateTime.today().plusMonths(3)).map((x) => {
            const has = datesInFuture.some((d) => d.equals(x));
            return {
              start: x.date,
              end: x.date,
              title: has ? "o" : "x",
              backgroundColor: has ? "var(--silhouette-calendar-task-mark-on)" : "var(--silhouette-calendar-task-mark-off)"
            };
          }) : [],
          eventContent: (info) => info.event.title
        });
    }
  };
  return [
    tasks,
    currentTask,
    options,
    handleClickTask,
    plugins,
    taskService,
    holidays,
    _a,
    datesInFuture
  ];
}
var RepetitionTaskView = class extends SvelteComponent {
  constructor(options) {
    super();
    init(this, options, instance3, create_fragment3, safe_not_equal, { taskService: 5, tasks: 0, holidays: 6 });
  }
};
var RepetitionTaskView_default = RepetitionTaskView;

// src/ui/RepetitionTaskItemView.ts
var REPETITION_TASK_VIEW_TYPE = "repetition-task";
var RepetitionTaskItemView = class extends import_obsidian4.ItemView {
  constructor(leaf, taskService) {
    super(leaf);
    this.taskService = taskService;
  }
  getViewType() {
    return REPETITION_TASK_VIEW_TYPE;
  }
  getDisplayText() {
    return "Silhouette";
  }
  getIcon() {
    return "cloud-fog";
  }
  async onclose() {
    this.view.$destroy();
  }
  async onOpen() {
    this.view = new RepetitionTaskView_default({
      target: this.containerEl.children[1],
      props: {
        taskService: this.taskService
      }
    });
    await this.refreshData();
  }
  async refreshData() {
    const [tasks, taskError] = await this.taskService.loadRepetitionTasks().then((x) => x.unwrap());
    const [holidays, holidaysError] = (await this.taskService.loadHolidays()).unwrap();
    this.view.$set({
      tasks,
      holidays
    });
  }
};

// src/app/TimerServiceImpl.ts
var import_owlelia8 = __toESM(require_dist());

// src/domain/vo/TimerStatus.ts
function unsafeMatch(str, pattern2) {
  return Array.from(str.matchAll(pattern2))[0].groups;
}
var pad00 = (v) => String(v).padStart(2, "0");
function toHHmmss(seconds) {
  const hour = seconds / (60 * 60) | 0;
  const min = seconds % (60 * 60) / 60 | 0;
  const sec = seconds % 60;
  return `${pad00(hour)}:${pad00(min)}:${pad00(sec)}`;
}
var pattern = {
  neverRecorded: /[-*] \[.] (?<name>.+)/g,
  recording: /[-*] \[.] (?<name>.+)\(⏳\)$/g,
  recorded: /[-*] \[.] (?<name>.+)\(⏲️(?<time>\d\d:\d\d:\d\d)\)$/g
};
function isLineRecording(line) {
  return Boolean(line.match(pattern.recording));
}
var TimerStatus;
((TimerStatus2) => {
  function fromLine(line) {
    if (!line) {
      return new NotTaskStatus();
    }
    if (!Boolean(line.match(pattern.neverRecorded))) {
      return new NotTaskStatus();
    }
    if (isLineRecording(line)) {
      return new RecordingStatus();
    } else if (Boolean(line.match(pattern.recorded))) {
      return new RecordedStatus();
    } else {
      return new NeverRecordedStatus();
    }
  }
  TimerStatus2.fromLine = fromLine;
})(TimerStatus || (TimerStatus = {}));
var NotTaskStatus = class {
  constructor() {
    this.name = "notTask";
  }
};
var NeverRecordedStatus = class {
  constructor() {
    this.name = "neverRecorded";
  }
  parse(line) {
    const { name } = unsafeMatch(line, pattern.neverRecorded);
    return { name };
  }
  getNextStatusLine(line) {
    return `${line} (\u23F3)`;
  }
};
var RecordingStatus = class {
  constructor() {
    this.name = "recording";
  }
  parse(line) {
    const { name } = unsafeMatch(line, pattern.recording);
    return { name };
  }
  getNextStatusLine(line, timer) {
    return line.replace("(\u23F3)", `(\u23F2\uFE0F${toHHmmss(timer.accumulatedSeconds)})`);
  }
};
var RecordedStatus = class {
  constructor() {
    this.name = "recorded";
  }
  parse(line) {
    const { name, time } = unsafeMatch(line, pattern.recorded);
    const [hours, minutes, seconds] = time.split(":").map(Number);
    return { name, seconds: hours * 60 * 60 + minutes * 60 + seconds };
  }
  getNextStatusLine(line) {
    return line.replace(/\(⏲️\d\d:\d\d:\d\d\)/, "(\u23F3)");
  }
};

// src/app/TimerServiceImpl.ts
var import_obsidian5 = require("obsidian");
var TimerServiceImpl = class {
  constructor(appHelper, repository) {
    this.appHelper = appHelper;
    this.repository = repository;
  }
  static create(appHelper, repository) {
    return new TimerServiceImpl(appHelper, repository);
  }
  serRepository(repository) {
    this.repository = repository;
  }
  async getTimer() {
    return (await this.repository.loadTimer()).orThrow();
  }
  hasTimer() {
    return this.repository.hasTimer();
  }
  async hasNotTimer() {
    return !await this.hasTimer();
  }
  async execute(option) {
    const line = this.appHelper.getActiveLine() || "";
    const lineTimeStatus = TimerStatus.fromLine(line);
    if (lineTimeStatus.name === "notTask") {
      return;
    }
    switch (lineTimeStatus.name) {
      case "recording":
        if (await this.hasNotTimer()) {
          new import_obsidian5.Notice("\u8A08\u6E2C\u4E2D\u306E\u30BF\u30B9\u30AF\u304C\u306A\u3044\u305F\u3081\u3001\u30AB\u30FC\u30BD\u30EB\u914D\u4E0B\u306E\u30BF\u30B9\u30AF\u3092\u8A08\u6E2C\u6E08\u306B\u3067\u304D\u307E\u305B\u3093\u3002", 0);
          return;
        }
        this.appHelper.replaceStringInActiveLine(lineTimeStatus.getNextStatusLine(line, (await this.getTimer()).stop(import_owlelia8.DateTime.now())));
        await this.repository.clearTimer();
        break;
      case "neverRecorded":
        if (await this.hasTimer()) {
          new import_obsidian5.Notice("\u8A08\u6E2C\u4E2D\u306E\u30BF\u30B9\u30AF\u304C\u3042\u308B\u305F\u3081\u3001\u65B0\u305F\u306A\u30BF\u30B9\u30AF\u3092\u8A08\u6E2C\u958B\u59CB\u3067\u304D\u307E\u305B\u3093\u3002\u8A08\u6E2C\u4E2D\u306E\u30BF\u30B9\u30AF\u3092\u601D\u3044\u51FA\u305B\u306A\u3044\u5834\u5408\u306F 'Force stop recording' \u30B3\u30DE\u30F3\u30C9\u3092\u5B9F\u884C\u3057\u3001\u5F37\u5236\u7684\u306B\u8A08\u6E2C\u4E2D\u306E\u30BF\u30B9\u30AF\u3092\u8A08\u6E2C\u5B8C\u4E86\u3055\u305B\u3066\u304F\u3060\u3055\u3044\u3002", 0);
          return;
        }
        await this.repository.saveTimer(Timer.of({
          name: lineTimeStatus.parse(line).name,
          startTime: import_owlelia8.DateTime.now(),
          accumulatedSeconds: 0
        }));
        this.appHelper.replaceStringInActiveLine(lineTimeStatus.getNextStatusLine(line));
        if (option.openAfterRecording) {
          this.appHelper.openLinkInActiveLine({ leaf: "same-tab" });
        }
        break;
      case "recorded":
        if (await this.hasTimer()) {
          new import_obsidian5.Notice("\u8A08\u6E2C\u4E2D\u306E\u30BF\u30B9\u30AF\u304C\u3042\u308B\u305F\u3081\u3001\u65B0\u305F\u306A\u30BF\u30B9\u30AF\u3092\u8A08\u6E2C\u958B\u59CB\u3067\u304D\u307E\u305B\u3093\u3002\u8A08\u6E2C\u4E2D\u306E\u30BF\u30B9\u30AF\u3092\u601D\u3044\u51FA\u305B\u306A\u3044\u5834\u5408\u306F 'Force stop recording' \u30B3\u30DE\u30F3\u30C9\u3092\u5B9F\u884C\u3057\u3001\u5F37\u5236\u7684\u306B\u8A08\u6E2C\u4E2D\u306E\u30BF\u30B9\u30AF\u3092\u8A08\u6E2C\u5B8C\u4E86\u3055\u305B\u3066\u304F\u3060\u3055\u3044\u3002", 0);
          return;
        }
        const { name, seconds: accumulatedSeconds } = lineTimeStatus.parse(line);
        this.appHelper.replaceStringInActiveLine(lineTimeStatus.getNextStatusLine(line));
        await this.repository.saveTimer(Timer.of({
          name,
          startTime: import_owlelia8.DateTime.now(),
          accumulatedSeconds
        }));
        if (option.openAfterRecording) {
          this.appHelper.openLinkInActiveLine({ leaf: "same-tab" });
        }
        break;
    }
  }
  async cycleBulletCheckbox(startNextTaskAutomatically) {
    if (!this.appHelper.cycleListCheckList()) {
      return;
    }
    if (!this.appHelper.isCheckedCurrentLineTask()) {
      return;
    }
    const line = this.appHelper.getActiveLine() || "";
    const lineTimeStatus = TimerStatus.fromLine(line);
    if (lineTimeStatus.name !== "recording") {
      return;
    }
    await this.execute({ openAfterRecording: false });
    if (!startNextTaskAutomatically) {
      return;
    }
    const nextLine = this.appHelper.getActiveNextLine() || "";
    const nextLineTimeStatus = TimerStatus.fromLine(nextLine);
    if (nextLineTimeStatus.name === "notTask") {
      return;
    }
    this.appHelper.moveNextLine();
    if (this.appHelper.isCheckedCurrentLineTask()) {
      return;
    }
    await this.execute({ openAfterRecording: false });
  }
  moveToRecording() {
    var _a;
    const content = this.appHelper.getActiveFileContent();
    if (!content) {
      return;
    }
    const recordingLineIndex = content.split("\n").findIndex(isLineRecording);
    if (recordingLineIndex !== -1) {
      (_a = this.appHelper.getActiveMarkdownEditor()) == null ? void 0 : _a.setCursor({ line: recordingLineIndex, ch: 0 });
    }
  }
  async forceStopRecording() {
    await this.repository.clearTimer();
  }
};

// src/commands.ts
var import_owlelia9 = __toESM(require_dist());
var import_obsidian6 = require("obsidian");

// src/utils/parser.ts
function parseMarkdownList(text2) {
  var _a, _b, _c, _d;
  const result = Array.from(text2.matchAll(/^(?<prefix>[ \t\s]*([-*] (\[.] |)|))(?<content>.*)$/g)).at(0);
  return {
    prefix: (_b = (_a = result == null ? void 0 : result.groups) == null ? void 0 : _a.prefix) != null ? _b : "",
    content: (_d = (_c = result == null ? void 0 : result.groups) == null ? void 0 : _c.content) != null ? _d : ""
  };
}

// src/commands.ts
function createCommands(appHelper, settings, taskService, timerService) {
  return [
    {
      id: "insert-tasks",
      name: "Insert tasks",
      checkCallback: (checking) => {
        if (appHelper.getActiveFile() && appHelper.getActiveMarkdownView()) {
          if (!checking) {
            const date = import_owlelia9.DateTime.from(appHelper.getActiveFile().basename, settings.fileDateFormat || void 0);
            taskService.insertTasksToDailyNote(date).then((err) => {
              if (err) {
                new import_obsidian6.Notice(`[Error] ${err.name}: ${err.message}`, 0);
                return;
              }
              new import_obsidian6.Notice(`Insert tasks that should do on ${date.format("YYYY/MM/DD")}`);
            });
          }
          return true;
        }
      }
    },
    {
      id: "push-timer",
      name: "Push timer",
      checkCallback: (checking) => {
        if (appHelper.getActiveFile() && appHelper.getActiveMarkdownView()) {
          if (!checking) {
            timerService.execute({ openAfterRecording: false });
          }
          return true;
        }
      }
    },
    {
      id: "push-timer-and-open",
      name: "Push timer and open",
      checkCallback: (checking) => {
        if (appHelper.getActiveFile() && appHelper.getActiveMarkdownView()) {
          if (!checking) {
            timerService.execute({ openAfterRecording: true });
          }
          return true;
        }
      }
    },
    {
      id: "cycle-bullet-checkbox",
      name: "Cycle bullet/checkbox",
      checkCallback: (checking) => {
        if (appHelper.getActiveFile() && appHelper.getActiveMarkdownView()) {
          if (!checking) {
            timerService.cycleBulletCheckbox(settings.startNextTaskAutomaticallyAfterDone);
          }
          return true;
        }
      }
    },
    {
      id: "move-to-recording",
      name: "Move to recording",
      checkCallback: (checking) => {
        if (appHelper.getActiveFile() && appHelper.getActiveMarkdownView()) {
          if (!checking) {
            timerService.moveToRecording();
          }
          return true;
        }
      }
    },
    {
      id: "force-stop-recording",
      name: "Force stop recording",
      checkCallback: (checking) => {
        if (appHelper.getActiveFile() && appHelper.getActiveMarkdownView()) {
          if (!checking) {
            timerService.forceStopRecording();
          }
          return true;
        }
      }
    },
    {
      id: "insert-current-time",
      name: "Insert current time",
      checkCallback: (checking) => {
        if (appHelper.getActiveFile() && appHelper.getActiveMarkdownView()) {
          if (!checking) {
            const activeLine = appHelper.getActiveLine();
            const { prefix, content } = parseMarkdownList(activeLine);
            appHelper.replaceStringInActiveLine(`${prefix}${import_owlelia9.DateTime.now().format("HH:mm")} ${content}`, { cursor: "last" });
          }
          return true;
        }
      }
    }
  ];
}

// src/main.ts
var SilhouettePlugin = class extends import_obsidian7.Plugin {
  constructor() {
    super(...arguments);
    this.commands = [];
  }
  async onload() {
    await this.loadSettings();
    this.appHelper = new AppHelper(this.app);
    this.taskService = new TaskServiceImpl(this.appHelper, new TaskRepositoryImpl(this.appHelper, this.settings.taskFilePath, this.settings.holidayFilePath));
    this.timerService = TimerServiceImpl.create(this.appHelper, new TimerRepositoryImpl(this.appHelper, this.settings.timerStorageFilePath || `${this.manifest.dir}/timer.json`));
    this.registerView(REPETITION_TASK_VIEW_TYPE, (leaf) => {
      this.repetitionTaskView = new RepetitionTaskItemView(leaf, this.taskService);
      if (this.fileEventRef) {
        this.app.vault.offref(this.fileEventRef);
      }
      this.fileEventRef = this.app.vault.on("modify", async (file) => {
        if (file.path === this.settings.taskFilePath || file.path === this.settings.holidayFilePath) {
          await this.repetitionTaskView.refreshData();
        }
      });
      return this.repetitionTaskView;
    });
    this.addRibbonIcon("cloud-fog", "Activate view", async () => {
      await this.activateView();
    });
    this.commands.forEach((x) => this.appHelper.removeCommand(x.id));
    this.commands = createCommands(this.appHelper, this.settings, this.taskService, this.timerService).map((x) => this.addCommand(x));
    this.addSettingTab(new SilhouetteSettingTab(this.app, this));
  }
  async onunload() {
    if (this.fileEventRef) {
      this.app.vault.offref(this.fileEventRef);
    }
    this.app.workspace.detachLeavesOfType(REPETITION_TASK_VIEW_TYPE);
  }
  async loadSettings() {
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
  }
  async saveSettings() {
    await this.saveData(this.settings);
    await this.reset();
  }
  async reset() {
    this.taskService.serRepository(new TaskRepositoryImpl(this.appHelper, this.settings.taskFilePath, this.settings.holidayFilePath));
    this.timerService.serRepository(new TimerRepositoryImpl(this.appHelper, this.settings.timerStorageFilePath || `${this.manifest.dir}/timer.json`));
    await this.activateView();
  }
  async activateView() {
    this.app.workspace.detachLeavesOfType(REPETITION_TASK_VIEW_TYPE);
    await this.app.workspace.getRightLeaf(false).setViewState({
      type: REPETITION_TASK_VIEW_TYPE,
      active: true
    });
    this.app.workspace.revealLeaf(this.app.workspace.getLeavesOfType(REPETITION_TASK_VIEW_TYPE)[0]);
  }
};
